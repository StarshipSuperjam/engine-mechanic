---
status: accepted
engine_record: true
---

# Resolve the D-181 scope-name correction: landed-text cold audit clean (no blocking/serious), two operator nits folded, control-plane / provisioning / module-system re-locked

*Decided 2026-06-06 in the design workspace.*

## The decision

The [D-181](0181-correct-the-non-existent-github-scope-admin-repo-ruleset-in.md) change-set is landed and **re-locked**. The mandatory landed-text cold audit ran with four independent no-shared-context lenses (technical-feasibility · adversarial · non-engineer-operator · architect) on the as-written [control-plane](../spec/systems/infrastructure/control-plane.md), [provisioning](../spec/systems/infrastructure/provisioning.md), [module-system](../spec/systems/grammar/module-system.md) edits, the non-locked carriers ([constraints.md](../reference/constraints.md), [risks.md](../reference/risks.md) R1, [scenarios/first-run.md](../architecture.md#first-run-provisioning), wbs/stage-0-harness.md §5, the wbs/corpus-audit-ledger.md annotation), and [D-181](0181-correct-the-non-existent-github-scope-admin-repo-ruleset-in.md). **No blocking, no serious findings on any lens.**
  - **Technical-feasibility (the lens [D-077](0077-lock-the-provisioning-system-terminal-foundation-lock-the-bo.md) got wrong by inference — here discharged by direct verification against current GitHub docs).** All five facts re-confirmed: `admin:repo_ruleset` is absent from the OAuth-scopes list; creating/updating a ruleset needs fine-grained **Administration: write** (or classic **`repo`**); a default `gh auth login` grants `repo` (and `gh auth status` on this machine shows `repo`); the `repo` consent label reads "Full control of private repositories"; the default Actions `GITHUB_TOKEN` still cannot create a ruleset (no `administration` representable in workflow `permissions`). The **write-probe** for a fine-grained token's Administration:write is sound (it is a per-repo permission, not an introspectable named scope); `gh auth refresh -s repo` is a valid command/scope. No edited sentence introduces a new false platform claim.
  - **Adversarial.** Over-claim guards held (no "firms R1"; no autonomous/background apply — the actor stays the operator's interactive local `gh`); the contract's four elements (operator-privileged actor, consent-gated, verify-after-refresh, degrade-never-fake) are intact; the §15 residual is widened to true breadth without laundering and without introducing an unguarded vector ([D-051](0051-guardrail-integrity-the-builder-cannot-silently-weaken-its-o.md)'s enumerated weakening-guard untouched); the positive-confirmation addition is a clean mechanism-layer fill that respects the locked control-plane/boot surfacing split. **Adjudicated ([D-181](0181-correct-the-non-existent-github-scope-admin-repo-ruleset-in.md)'s deferred §15 question): no *additional* operator-facing disclosure is owed** — the design-layer breadth disclosure is the correct locus; a standing "you hold broad powers" banner would be alarm-fatigue noise, and the weakening-acknowledgment gate already names what weakens and what the AI could then do.
  - **Non-engineer-operator.** The pre-translation correctly avoids the naïve `repo` swap and pre-empts the label's apparent breadth; the "your review gate is now ON" confirmation closes the silent-skip gap on the no-consent common path; the rare refresh fallback is self-driven and safe; first-run depicts the no-consent path as default. **Two operator nits folded before re-lock:** (a) the pre-translation now matches the label's *felt intensity* ("sweeping, full-control-sounding") rather than a milder paraphrase, so the reassurance survives contact with a screen that says "Full control"; (b) the standing-privilege paragraph's "full repository control" is bridged to "full control over the repositories the operator already owns," preventing a misread that the engine reaches arbitrary repos — neither dilutes the §15 honesty.
  - **Architect.** Carrier sweep complete (the false scope survives only in the append-only [decision-log](README.md) and the one annotated corpus-audit-ledger run-record; no live doc carries it; the false-premise sweep found no residual); cross-doc consistency uniform (the "usually-held / fallback-on-absence" framing identical across all carriers); authoring rules clean (no changelog cruft; the ledger is one annotation, not a body rewrite; no append-only entry edited in place); the alarm→audit→resolve rhythm correct.

  **Carrier-record correction to [D-181](0181-correct-the-non-existent-github-scope-admin-repo-ruleset-in.md) (append-only — D-181 stands; this corrects the prediction):** D-181 listed wbs/stage-0-harness.md §5 as "verify, likely no edit"; it **was edited** (its "admin-scoped actor" phrasing reconciled to "operator-privileged actor holding `repo` / Administration:write") — a genuine improvement, recorded here as an edited non-locked carrier.

  **Matrix close-out:** [engine-architecture.md](../architecture.md) §3 verified still a truthful index (it references the trio by role/link, names no scope — no edit owed); [glossary.md](../reference/glossary.md) and [principles.md](../principles.md) §15 confirmed clean (no scope token); **no [open-questions.md](../reference/open-questions.md) item opened or resolved** (a fact correction, not a new thread); **no [deviations](../reference/prototype-deviations.md) entry** (n/a); [risks.md](../reference/risks.md) R1 reconciled and **stays open** (closes on first-run UX, not at this correction).

  **Re-lock set (3):** [control-plane](../spec/systems/infrastructure/control-plane.md), [provisioning](../spec/systems/infrastructure/provisioning.md), [module-system](../spec/systems/grammar/module-system.md). **Re-lock:** `python3 lock.py --relock` run on all three under this decision; `python3 validate.py` green.

## Why

The lock gate requires a landed-text cold audit with every blocking/serious finding resolved before re-lock; it ran, returned none, and the only folded changes were two operator nits that *strengthened* the operator-facing prose. The correction aligns the canon to the built truth (slice 25a) and to the live platform, with the durable principles in the locked docs and the dated platform strings in the log + build-spec leaf ([D-171](0171-correct-the-falsified-platform-ignores-a-pretooluse-deny-cla.md) discipline). **Supersession:** this entry + [D-181](0181-correct-the-non-existent-github-scope-admin-repo-ruleset-in.md) supersede [D-077](0077-lock-the-provisioning-system-terminal-foundation-lock-the-bo.md)'s "the name is correct … non-issue" dismissal — wrong on the conclusion, circular in corroboration (it cited the claim's own restatement in constraints as authority), and premised on a "locked constraints" that is not in locks.yaml; the lesson — a platform fact dismissed by inference and self-citation, the exact failure [D-171](0171-correct-the-falsified-platform-ignores-a-pretooluse-deny-cla.md) names — is recorded so a future audit verifies, never infers. [D-051](0051-guardrail-integrity-the-builder-cannot-silently-weaken-its-o.md)'s §15 team-closure reasoning-claim keeps its substance under the corrected capability name. The locked **contract is unchanged**; the build's [control-plane](../spec/systems/infrastructure/control-plane.md) bootstrap (slice 25a) needs no rework.

## What we ruled out

**Re-lock on the as-audited text without the two operator nits** (rejected — both strengthen the single most trust-critical first-run beat at trivial cost; the engine exists to make a non-engineer *more* capable, never less). **Add a standing "you hold broad powers" operator banner** (rejected — the adversarial lens adjudicated it alarm-fatigue noise; the design-layer breadth disclosure plus the weakening-acknowledgment gate suffice). **Quote "Full control of private repositories" in the locked doc** (rejected — dated, version-brittle; it lives in the log + build-spec leaf, only the durable principle in the locked doc — [D-171](0171-correct-the-falsified-platform-ignores-a-pretooluse-deny-cla.md)). **Defer the re-lock** (rejected — the audit cleared; leaving three locked docs fingerprint-failing is the silent-drift state the lock gate forbids).
