---
status: accepted
engine_record: true
---

# Resolve: re-lock `boot` (the leftover-license surfacing + the hook-enforced kept-on-purpose intent-exit) — a carrier of [D-302](0302-litigate-engine-template-471-design-the-standing-foreign-lic.md)

*Decided 2026-07-12 in the design workspace.*

## The decision

Re-lock [boot](../spec/systems/lifecycle/boot.md), last in dependency order (the surfacer) and a carrier of [D-302](0302-litigate-engine-template-471-design-the-standing-foreign-lic.md). The landed text — the leftover-license open-finding bullet (plain-language, **leads with private-by-default reassurance**, provenance-framed, jargon-free, offer surfaces the intent-exit invitation, on-consent removal = a small reviewed change the operator approves via the plan-acceptance Build entry, ranked below governance-critical alarms, distinct ledger fingerprint); and the **kept-on-purpose intent-exit** (a bounded third disposition: plain decline → terse never-fully-silent, explicit acknowledgment → retired marker; **retire-eligibility a per-finding-class constant the deterministic hook enforces from the live producing detector — never a ledgered/model label** — so a mis-written or injection-planted marker cannot silence a governance alarm; the model-invoked write realized as an Explore-permitted `.engine/tools/` helper; per-machine reach named) — passed the two-round landed-text cold audit recorded in [D-303](0303-resolve-re-lock-repository-topology-law-2-gains-the-standing.md). `python3 lock.py --relock systems/lifecycle/boot/README.md --decision D-306`; `validate.py` green for this fingerprint. This closes the D-302 re-lock set; **all four fingerprints clear**.

## Why

boot re-locks last (it invokes provisioning's detector and surfaces the finding). The intent-exit resolves the operator-lens category-error (a legitimately-kept license has a terminal state governance alarms lack) while the hook-enforced eligibility keeps the [§15](../principles.md) guard non-falsifiable.

## What we ruled out

**"Never fully silent" with no intent-exit** (rejected — nags a deliberately-kept license forever, the Q18 trap). **Model-side retire-eligibility** (rejected — a model slip or prompt-injection could silence a governance alarm; the honor decision and the class derivation are hook-side and detector-derived).
