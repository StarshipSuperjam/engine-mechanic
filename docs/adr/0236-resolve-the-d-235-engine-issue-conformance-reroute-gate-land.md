---
status: accepted
engine_record: true
---

# Resolve the D-235 engine-Issue conformance reroute gate: landed-text cold re-check clean, [control-plane](../spec/systems/infrastructure/control-plane.md) + [hooks](../spec/systems/infrastructure/hooks.md) + [modes](../spec/systems/lifecycle/modes.md) + [modules/core](../spec/modules/core.md) re-locked; engine-template #208 filed

*Decided 2026-06-22 in the design workspace.*

## The decision

The [D-235](0235-authorize-the-engine-issue-conformance-reroute-gate-promote.md) changes landed in the four locked docs and the propagation set ([glossary](../reference/glossary.md)), and all four docs are **re-locked** in dependency order (control-plane → hooks → modes → modules/core). **(1) [control-plane](../spec/systems/infrastructure/control-plane.md):** the body-contract enforcement bullet now reads *by-construction helper beneath a channel-scoped reroute gate* — the gate denies a non-conforming engine-labeled `gh issue create`/`gh api` and redirects, keying on the body's shape not provenance; the shape's presence is a gated floor, its truthfulness stays posture (a less-truthful body still costs legibility, never a guardrail, so §15 does not bite the truthfulness tier — the [D-235](0235-authorize-the-engine-issue-conformance-reroute-gate-promote.md) anti-choice's anchor, restored); the escalate-upstream draft stays exempt; the CI-harness section gains the `on: issues` backstop. **(2) [hooks](../spec/systems/infrastructure/hooks.md):** the block-budget law admits a **minimal-work-loss redirect** as the one exception to the §6 friction test (loses no work, so it earns block-eligibility without being governance-critical), and the block-eligible inventory registers the engine-Issue-conformance reroute alongside the Explore write-gate. **(3) [modes](../spec/systems/lifecycle/modes.md):** the Explore gate moves a non-conforming engine-channel creation from *allow* to *deny-with-redirect*, scoped to the engine-labeled channel (unlabeled/human Issues and all reads pass), honestly fallible with **no merge wall behind it** so the CI backstop is its only catch-all. **(4) [modules/core](../spec/modules/core.md):** `provides` and the grounding-floor binding gain the reroute-gate hook + the `on:issues` CI backstop. The **verify-and-decide caller docs** ([build-orchestration](../spec/systems/lifecycle/build-orchestration.md):429, [telemetry](../spec/systems/guardrails/telemetry.md), [audits](../spec/systems/guardrails/audits.md)) were left **unedited** — each *"authors via the helper"* / exempt-lane claim stays true because the gate is transport-neutral to a caller; [modules/README](../reference/module-catalog.md), wbs, [risks](../reference/risks.md), and [engine-architecture](../architecture.md) need no edit (no module/dependency/build-order change; the gate is a mechanism within existing systems). The **workspace Stage-0 counterpart gate** (`.claude/hooks/engine-issue-gate.py`, wired in `settings.local.json`; 12/12 unit cases) is in place as immediate relief and the build's reference implementation. The engine build is owed as **engine-template #208** (filed conformantly through the helper + `engine` label — the contract dogfooded). `python3 lock.py --relock systems/infrastructure/control-plane/README.md --decision D-236`; `… systems/infrastructure/hooks/README.md …`; `… systems/lifecycle/modes/README.md …`; `… modules/core/README.md …`; `python3 validate.py` green.

## Why

A **focused two-lens cold landed-text re-check** (faithfulness + adversarial/technical, no shared context) ran against the on-disk edits — the proportionate close after the plan-stage four-lens audit ([D-148](0148-lock-audit-library-the-engine-self-audit-delivery-the-tenth.md)/[D-230](0230-resolve-the-d-229-audit-auth-correction-landed-text-cold-aud.md)/[D-234](0234-resolve-the-d-233-audit-over-audit-re-litigation-landed-text.md) precedent). **Faithfulness returned clean:** every [D-235](0235-authorize-the-engine-issue-conformance-reroute-gate-promote.md) change landed exactly, no scope creep, no authoring-rule-1 change-history cruft, propagation complete, and the three caller docs confirmed non-false. **Adversarial returned one blocking, resolved:** [D-235](0235-authorize-the-engine-issue-conformance-reroute-gate-promote.md)'s anti-choice cited *"control-plane:299's legibility-not-a-guardrail"* as the standing basis for keeping issue truthfulness posture, but the [D-235](0235-authorize-the-engine-issue-conformance-reroute-gate-promote.md) edit had replaced that sentence wholesale, leaving the citation pointing at absent text. Resolved by restoring the framing in control-plane, correctly **scoped to the truthfulness tier** (routing is now gated; a less-truthful body still costs legibility, never a guardrail, §15 untouched) — a more complete statement than the original, and the citation now holds. Re-verified: only the four expected lock-fingerprint failures remained, now cleared by the re-lock.

## What we ruled out

Run a full four-lens audit on the landed text (rejected — the plan-stage four-lens already ran under [D-235](0235-authorize-the-engine-issue-conformance-reroute-gate-promote.md); a focused two-lens re-check of the *landed passages* is the established proportionate close, [D-148](0148-lock-audit-library-the-engine-self-audit-delivery-the-tenth.md)/[D-234](0234-resolve-the-d-233-audit-over-audit-re-litigation-landed-text.md)). Also add the §15-truthfulness clause to the [glossary](../reference/glossary.md) body-contract entry (rejected — the gloss already says *"truthfulness still posture"*; control-plane is the canonical tier doc and [D-235](0235-authorize-the-engine-issue-conformance-reroute-gate-promote.md)'s cited anchor, so the clause lands there alone, minimal-touch). Fold [D-235](0235-authorize-the-engine-issue-conformance-reroute-gate-promote.md) and D-236 into one entry (rejected — the authorize/resolve split is the litigation-protocol convention, [D-233](0233-authorize-the-audit-over-audit-re-litigation-feed-the-period.md)/[D-234](0234-resolve-the-d-233-audit-over-audit-re-litigation-landed-text.md)).
