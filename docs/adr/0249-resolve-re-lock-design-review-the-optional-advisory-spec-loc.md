---
status: accepted
engine_record: true
---

# Resolve: re-lock `design-review` (the optional advisory spec-lock invocation; `product-intent` referent) — fifth carrier of D-244

*Decided 2026-06-23 in the design workspace.*

## The decision

Re-lock [design-review](../spec/modules/design-review.md), the fifth carrier of [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md). Edits: **(1)** `product-intent`'s referent is named **the committed `locked` spec**; **(2)** the disclosed-no-op trigger is **"no `locked` spec"** (none, or only a `draft`), matching [build-orchestration](../spec/systems/lifecycle/build-orchestration.md)/[qa-review](../spec/modules/qa-review.md); **(3)** a **new section + design-commitment** for the **optional advisory spec-lock invocation** — when design-review is installed, [product-design](../spec/modules/product-design.md)'s spec-lock ceremony may invoke the quartet to **advise on the spec** (referent = the spec, the product analogue of the workspace's cold-session audit); the lenses **advise, never gate** — the operator's recorded acceptance locks the spec and the engine never *vetoes* what the product may become (the [wall](../spec/systems/infrastructure/repository-topology.md): engine validates form + advises on content, [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md); operator governs); **optional enrichment bound by presence**, so product-design takes **no hard `depends` edge** ([D-066](0066-the-4-4-review-lens-roster-two-stage-suites-mirroring-the-en.md) separation holds). **The proportionate gate** (a moderate carrier touching the wall + the D-066 separation — a **focused two-lens cold re-check**, faithfulness + architect/wall): **no blocking, no serious. The architect's bottom-line: the wall HOLDS** — advising the operator who requested and governs the spec is the *accepted* side of [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md)'s anti-choice (the engine never vetoes), not R9's forbidden unilateral semantic-audit (R9 scopes only a *standing, engine-owned* drift probe); no product→engine dependency (the spec locks on validation + operator acceptance with or without the suite); the [D-066](0066-the-4-4-review-lens-roster-two-stage-suites-mirroring-the-en.md) separation preserved (no hard edge either way); the second consumer is **additive** (the plan-review gate already consumes all four — no dangling-lens finding); the build-orchestration seam coherent ("two referents, two moments"). **One in-line fix (faithfulness NIT):** the form/content parenthetical had linked to [repository-topology](../spec/systems/infrastructure/repository-topology.md), which frames the wall by *ownership* — reworded to attribute the form/content seam to the [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md) wall re-scope while still naming the wall. **NEW carry-forward to [D-250](0250-resolve-re-lock-product-design-the-full-sdd-module-redesign.md)** (architect, additive to D-247's i–iii): product-design must **(iv)** introduce the named **spec-lock ceremony** + the `stub→draft→locked` lifecycle with the **operator's recorded acceptance** as the lock gate; **(v)** state the ceremony **may invoke the design-review quartet to advise (never gate) when installed**, the lock earned on validation green + operator acceptance alone absent the suite (no hard `depends` edge); **(vi)** **record product-design as the second consumer** of the four plan-review personas, symmetric across both docs. `python3 lock.py --relock modules/design-review/README.md --decision D-249`; `python3 validate.py` green.

## Why

design-review's plan-review invocation is unchanged; the change is the *added* advisory spec-lock invocation, the load-bearing realization of [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md)'s "lenses advise, the operator accepts" — the move that keeps the wall while still giving the operator the four-lens evidence to lock on. The focused two-lens re-check weighted on the wall + separation was the proportionate gate; both held, the architect rendering an explicit HOLDS verdict. The advisory-when-present shape is the [D-065](0065-product-design-front-door-design-the-q14-intake-module-as-a.md) github-projects-sync enrichment precedent (consumed-by, never depended-on-by), so product-design stays `depends: core`.

## What we ruled out

**Make the spec-lock lenses a gate (an engine-blessed lock)** (rejected — [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md)/R9 semantic-audit-on-product breach; the lenses advise, the operator's acceptance gates). **Hard-`depends` product-design on design-review** (rejected — couples the suite to the producer's lifecycle against [D-066](0066-the-4-4-review-lens-roster-two-stage-suites-mirroring-the-en.md)'s falsifiable separation; presence-bound enrichment is the [D-065](0065-product-design-front-door-design-the-q14-intake-module-as-a.md) precedent). **Record the second consumer in design-review** (rejected — product-design owns its spec-lock ceremony, so the consumption record lands at D-250, its home). **Leave the wall-link imprecision** (rejected — the form/content seam is [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md)'s, not the ownership-framed topology wall; cite it correctly). **Run the full four-lens audit** (rejected — a moderate carrier; the focused wall-weighted re-check is proportionate).
