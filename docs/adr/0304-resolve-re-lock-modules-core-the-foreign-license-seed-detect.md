---
status: accepted
engine_record: true
---

# Resolve: re-lock `modules/core` (the foreign-`LICENSE`-seed detector enters `provides`; the never-strand floor stays singular; the fix rides the reviewed-PR path) — a carrier of [D-302](0302-litigate-engine-template-471-design-the-standing-foreign-lic.md)

*Decided 2026-07-12 in the design workspace.*

## The decision

Re-lock [modules/core](../spec/modules/core.md), second in dependency order (after topology) and a carrier of [D-302](0302-litigate-engine-template-471-design-the-standing-foreign-lic.md). The landed text — the **foreign-`LICENSE`-seed detector + consent-gated re-clear** tool added to `provides` (sibling of the strand detector); a detect/relay integration bullet whose **fix diverges** (a reviewed PR the operator merges, authority-anchored in [topology](../spec/systems/infrastructure/repository-topology.md) law 2's standing exception, *not* a boot-time write); and the never-strand-main floor **unchanged from its pre-D-302 singular form** ("the one exception is the operator-consented un-stranding correction") because the re-clear is a reviewed-PR commit the floor leaves out of scope — passed the two-round landed-text cold audit recorded in [D-303](0303-resolve-re-lock-repository-topology-law-2-gains-the-standing.md) (no BLOCKING/SERIOUS surviving; the reverted-singular form reconciled against [D-302](0302-litigate-engine-template-471-design-the-standing-foreign-lic.md)'s plural description in [D-303](0303-resolve-re-lock-repository-topology-law-2-gains-the-standing.md)). `python3 lock.py --relock modules/core/README.md --decision D-304`; `validate.py` green for this fingerprint.

## Why

core re-locks after topology (its authority anchor). The `provides` addition and integration bullet carry only the D-302 detector deltas; the never-strand floor returns to its settled singular text, which is *more* correct than D-302's interim generalization (the re-clear was never a working-tree/strand mutation).

## What we ruled out

**Keep the plural "corrections of the engine's own residue" generalization** (rejected — the reviewed-PR fix means the re-clear is out of the never-strand floor's scope, not an exception to it; the open-category phrasing also invited future self-authorizing members, an adversarial-flagged risk). **Give the detector its own module** (rejected — it is a `core` foundation tool beside the strand detector, not an optional bundle).
