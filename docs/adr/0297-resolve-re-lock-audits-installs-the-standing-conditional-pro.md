---
status: accepted
engine_record: true
---

# Resolve: re-lock `audits` (installs the standing, conditional product-spec-conformance sweep — the conformance-enforcement floor's **standing cadence**: report-only, forward-arm-only, prioritized, per-locked-row, by-presence over product-design's matrix, no `depends`) — a carrier of [D-296](0296-litigate-engine-template-427-residual-three-l1-l2-l3-audits.md)

*Decided 2026-07-11 in the design workspace.*

## The decision

Re-lock [audits](../spec/systems/guardrails/audits.md), the sole carrier of [D-296](0296-litigate-engine-template-427-residual-three-l1-l2-l3-audits.md). The landed text — the standing product-conformance sweep (conditional on a `locked` `docs/spec/`, by-presence matrix read with no `depends`, the audit persona's own forward divergence-hunt, report-only to the reconcile lane), the two-of-three-legs §17 honesty, the forward-arm-only / over-build-inapplicable clause closing the D-292/§20 storm, the prioritized-not-exhaustive bound, the engine-labeled-observation / un-labeled-product-Build finding home + matrix-row dedup, the no-spec-silent vs matrix-missing-actionable split, and the extended §12 leak-guard vocab — passed the mandatory four-lens landed-text cold audit recorded in [D-296](0296-litigate-engine-template-427-residual-three-l1-l2-l3-audits.md) (both BLOCKING resolved; all serious/nits folded). `python3 lock.py --relock systems/guardrails/audits/README.md --decision D-297`; `validate.py` green for this fingerprint.

## Why

audits owns the standing cadence; its re-lock lands after the clean landed-text audit. The change reuses the existing audit machinery and product-design's matrix by presence, adding no dependency and no wall breach.

## What we ruled out

**Re-lock before resolving the over-build-storm and false-glossary-term blockers** (rejected — would lock a doc that reopens a D-292-foreclosed storm and a canonical term the change falsifies; both had to be fixed first). **Fold the audit-library reconciliation into this re-lock** (rejected — reverted as unneeded; the manifest is unchanged, [R6](../reference/risks.md)).
