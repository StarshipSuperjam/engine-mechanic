---
status: accepted
engine_record: true
---

# Resolve: re-lock `build-orchestration` (the filler renders the operator-runnable acceptance steps + the bounded no-op into Review) — second and last carrier of D-252, closing the four-item carry-forward

*Decided 2026-06-27 in the design workspace.*

## The decision

Re-lock [build-orchestration](../spec/systems/lifecycle/build-orchestration.md), the **filler** carrier of [D-252](0252-authorize-binding-the-17-operator-runnable-behavioral-correl.md) and the last, closing [D-253](0253-resolve-re-lock-control-plane-the-review-record-carries-the.md)'s four-item carry-forward. Edits to the **Review record** section: **(1)** Review **surfaces the change's operator-runnable acceptance steps**, rendered **verbatim** from the realized `locked` spec's operator-runnable how-verified rows via the **same [D-247](0247-resolve-re-lock-build-orchestration-committed-spec-referent.md) work Issue → spec → acceptance-criteria path read** the referent uses (consumed, not re-resolved), or a **bounded four-reason no-op** ((i) all criteria engine/CI-internal; (ii) operator-runnable but not runnable in the operator's environment; (iii) no `locked` spec resolves — the referent's existing disclosed no-op; (iv) trivial fast-path) — never a silent absence; **(2)** the `demo`/`demo-*` subcommand is **severed** from the operator-runnable slot (AI-run, the operator cannot run it — [D-231](0231-promote-the-in-tool-demo-subcommand-as-a-governed-ai-run-sta.md)/[D-251](0251-reject-engine-template-255-s-explore-gate-memory-carve-out-t.md) — so a demo-only criterion falls to reason (i), on the engine's account); **(3)** the orchestrator **renders, never authors** (copies the operator-typed rows; never composes, grades, paraphrases, or curates a recipe — a semantic grade would breach the [wall](../spec/systems/infrastructure/repository-topology.md)); **(4)** the corroborability claim is **reconciled** to match the re-locked [control-plane](../spec/systems/infrastructure/control-plane.md) (the **review-judgment** part is the uncorroborable stretch; the **operator-runnable steps** are the corroborable converse), closing the [D-253](0253-resolve-re-lock-control-plane-the-review-record-carries-the.md)(iv) seam transient — the two docs now agree; **(5)** five operator-rendering rules + the no-op honesty rule, registered into the **Review-section-layout build-spec leaf**. **The full four-lens cold audit** (adversarial · faithfulness/propagation · architect/seam · non-engineer-operator, no shared context — the proportionate gate for the substantial keystone filler): **faithfulness CLEAN** (all four carry-forwards closed; exactly four no-op reasons; anti-changelog-clean; agrees with control-plane with no stale whole-Review-uncorroborable duplicate; no contract-shape usurpation — it cites control-plane's tier, never re-mints it); **architect/seam — the WALL HOLDS and the three-way seam HOLDS** (consumes the path read, imposes no validation/product-design re-lock, mints no contract shape; NITs were forward-hygiene only — the seam transient now closed). **Three SERIOUS, all fixed in-line:** **(a, adversarial)** the no-op's reason-class is uncorroborable AI self-judgment but was rendered as a confident *finding* — added the **§7 honesty clause** that the class is the *engine's own account, not an operator-verifiable fact*, surfaced as plain cause, mirroring the review-judgment tier; **(b, operator)** the promise-not-proof + offer-not-duty caveats must render **beside the step list itself** (not only the block preamble, where the over-trust springs) — bound in the build-spec leaf; **(c, operator)** the "things you can confirm yourself" heading could still hand the operator a terminal command they will not run — resolved by naming that **sorting a row by its runnable-surface *kind* (clickable screen vs terminal command) is a mechanical FORM read, not a recipe grade** ([D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md) FORM-vs-SEMANTIC line), so a CLI-only row renders on the engine's account without breaching the verbatim/no-grading wall. Plus one NIT taken: **"paraphrases"** restored to the no-grading verb list ([D-252](0252-authorize-binding-the-17-operator-runnable-behavioral-correl.md) bars it; the realistic "light edit" breach). **The focused two-lens remediation re-check** (non-engineer-operator · architect/engine-product-wall — the [D-250](0250-resolve-re-lock-product-design-the-full-sdd-module-redesign.md)/[D-141](0141-lock-product-design-the-design-front-door-the-fifth-module-l.md) precedent for trust-surface fixes): **both RESOLVED and clean — WALL STILL HOLDS**, all three fixes confirmed visible-to-the-operator (not only true-for-the-engine), the FORM-vs-SEMANTIC clause a faithful application of [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md) that does not smuggle a semantic grade and is consistent with the adjacent verbatim/no-grading sentence, the no-op-honesty clause agreeing with the re-locked control-plane; two cosmetic NITs (em-dash density; the "five rules" + no-op-as-tail reading) noted, not owed. **Phase-5 non-locked propagation owed** (next, not in this entry): [glossary](../reference/glossary.md) (prefer reusing "operator-runnable acceptance steps"/`Acceptance criteria`; **avoid "demonstration"**), [scenarios/build-session.md](../architecture.md#build-session-lifecycle) (the submit step now fills the Review steps), and recorded verify-no-edit dispositions ([engine-architecture](../architecture.md), [scenarios/product-design-intake.md](../architecture.md#product-design-intake), [R9](../reference/risks.md), [modules/README.md](../reference/module-catalog.md), wbs). `python3 lock.py --relock systems/lifecycle/build-orchestration/README.md --decision D-254`; `python3 validate.py` green.

## Why

build-orchestration is the filler — it owns *how* the Review section is filled, so the operator-runnable-steps render, the bounded no-op predicate, and the operator-facing rendering rules land here, atop the now-settled control-plane contract ([D-253](0253-resolve-re-lock-control-plane-the-review-record-carries-the.md)). The full four-lens audit was the proportionate gate for the substantial keystone carrier and earned its keep: the adversarial lens caught that the no-op reason-class — the highest-leverage dodge the whole mechanism exposes — was rendered as a finding rather than the engine's own posture self-report, and the operator lens caught that two operator-protections were true-for-the-engine but not visible where the trap springs (beside the steps) and that the "you can confirm yourself" heading could over-promise a terminal command. All three are the precise [§17](../principles.md)/north-star burden-of-proof failures this binding exists to prevent; remediating them in-line and cold-re-checking (the [D-250](0250-resolve-re-lock-product-design-the-full-sdd-module-redesign.md) discipline, because the fixes touched the operator-trust surface and one touched the wall) keeps the irreversible re-lock behind a clean gate. The teeth stay honest: the steps and no-op are **posture** surfaced at the operator's merge, the only mechanical correlate the pre-existing coverage check — never an agentic verdict dressed as a gate ([D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md)).

## What we ruled out

**Render the no-op reason-class as a finding** (rejected — it is uncorroborable AI self-judgment; rendering it as fact is the [§7](../principles.md) rubber-stamp [D-252](0252-authorize-binding-the-17-operator-runnable-behavioral-correl.md) downgraded the gate to avoid, now disclosed as the engine's own account). **Leave the over-trust caveat in the block preamble only** (rejected — the walk-away operator reads the step list; the caveat must ride beside it, the [§17](../principles.md) "in the artifact" rule). **Let the orchestrator demote a CLI-only row by grading it** (rejected — a content grade breaches the wall; the realizable form is the mechanical surface-*kind* sort, [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md) FORM-vs-SEMANTIC). **Drop "paraphrases" from the no-grading list** (rejected — "light editing" is the realistic verbatim-wall breach [D-252](0252-authorize-binding-the-17-operator-runnable-behavioral-correl.md) named). **Skip the remediation re-check** (rejected — three SERIOUS on the operator-trust surface, one adding a wall clause, warrant the [D-250](0250-resolve-re-lock-product-design-the-full-sdd-module-redesign.md)/[D-141](0141-lock-product-design-the-design-front-door-the-fifth-module-l.md) focused cold re-check before the irreversible lock). **Fold the Phase-5 propagation into this entry** (rejected — non-locked downstream edits; Phase 5 lands them, the standard authorized transient).
