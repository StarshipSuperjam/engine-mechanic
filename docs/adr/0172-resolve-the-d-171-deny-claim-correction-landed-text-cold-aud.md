---
status: accepted
engine_record: true
---

# Resolve the D-171 deny-claim correction: landed-text cold audit clean (docs), carrier accounting corrected, capability-discovery analyzed, modes + hooks re-locked

*Decided 2026-06-05 in the design workspace.*

## The decision

The [D-171](0171-correct-the-falsified-platform-ignores-a-pretooluse-deny-cla.md) change-set is landed and **re-locked**. The mandatory landed-text cold audit (four independent lenses — adversarial · technical-feasibility · non-engineer-operator · architect) ran on the as-written [modes](../spec/systems/lifecycle/modes.md) + [hooks](../spec/systems/infrastructure/hooks.md); the **two locked-doc edits cleared on their own text** (no leg upgraded to "reliable"; the §6/§7 closer survives verbatim; hooks `:77` reconciled with its own contract section `:123`; fail-open intact). Findings resolved before re-lock:
  - **(blocking — carrier accounting inside [D-171](0171-correct-the-falsified-platform-ignores-a-pretooluse-deny-cla.md))** D-171's named set of append-only carriers of the retracted deny-claim was **wrong twice** and is corrected here (append-only — D-171 stands as authored; this entry supersedes its accounting). The **true carrier set is {[D-046](0046-harness-comparative-mining-a-guardrail-integrity-gap-a-contr.md), [D-051](0051-guardrail-integrity-the-builder-cannot-silently-weaken-its-o.md), [D-070](0070-lock-the-modes-system-wave-3-head-three-stances-on-two-axes.md), [D-071](0071-re-lock-modes-doc-level-cold-audit-corrects-an-as-written-re.md)}**, ground-truthed against the text: D-171 (i) **mis-named [D-031](0031-integration-debt-is-a-telemetry-owned-register-not-a-knowled.md)** — a line-attribution slip across the 14 intervening decisions (the "harness-disposition deny caveat" is in **[D-046](0046-harness-comparative-mining-a-guardrail-integrity-gap-a-contr.md)** ("Harness comparative mining"), which states "the platform currently ignores `deny` for `Edit`/`Write` on some paths"; **D-031** is the integration-debt-register decision and carries no deny text), and (ii) **wrongly excluded [D-071](0071-re-lock-modes-doc-level-cold-audit-corrects-an-as-written-re.md)**, which *does* carry it (D-071's rationale: "`permissionDecision: deny` is honored unevenly for edit tools ([D-051], live)" + "the two remaining platform-reliability quirks (deny-ignored-for-edits; operator-typed-verb honor rate)"). **All four carriers' decisions stand unaffected:** D-046's "the durable gate cannot be a purely local hook" holds on the *stronger* grounds (fail-open + self-disarmability, not deny-reliability); D-051's standalone-member drop on its three other grounds; D-070's modes laws; D-071's resume-resurrection fix + dangling-link repoint (neither rests on the deny fact). **D-071's "two remaining platform-reliability quirks" is now ONE** — the operator-typed-verb honor rate ([R11](../reference/risks.md)); the deny-ignored-for-edits quirk is retracted.
  - **(capability-discovery — the operator's sharper question)** Before re-lock the operator asked what the capability *working* (vs. assumed-broken) means for the whole plan — a capability discovery, not just an error correction. Assessed against every locked choice that leaned on deny-unreliability: **no gate placement changes**, because "a local gate is a nudge, not the wall" is **over-determined by grounds independent of deny-format reliability** — fail-open (a crashed hook lets the action through; applies to *every* leg, including an honored-deny leg), self-disarmability (the builder controls `.claude/settings.json`; only the frozen-name, ruleset-bound, trusted-source merge-gate check resists), and [§6](../principles.md) (a local hard-block buys friction without proportional trust) — with the protected-branch merge the actual gate (the consecutive-block release reinforces "not a wall" for [`Stop`](../spec/systems/infrastructure/hooks.md) and is *not* relied on for `PreToolUse`). The working deny makes the existing **nudges fire dependably** (a *stronger nudge*, not a new wall). The one option it unlocks — **re-adding [D-051](0051-guardrail-integrity-the-builder-cannot-silently-weaken-its-o.md)'s dropped in-session §15 weakening-guard `PreToolUse` member as defense-in-depth** — was put to the operator and **declined**: it adds no guarantee (still fail-open + self-disarmable), re-creates the orphaned-ownership problem (its natural owner [validation](../spec/systems/guardrails/validation.md) is locked; modes' stance-scoped gate would *permit* the write in Build/Routine where weakening happens), and adds contagious core surface ([§12](../principles.md)). All four lenses concurred "optimal design unchanged" is sound and found no missed locus. **Optimal outcome: the merge-gate check remains the §15 wall; no locked design changes beyond the false-claim correction.**
  - **(nits dispositioned)** wbs/core-build-roadmap.md:295 trimmed to *point to* D-171 for the dated platform finding rather than restate it inline — the same quarantine D-171 applies to locked docs (dated platform facts live in the log; the WBS re-confirms at the slice plan gate). The pre-existing operator-jargon in [modes](../spec/systems/lifecycle/modes.md) ("aliases / `eval` / substitution / chaining") is **left as-is** — out of scope for this correction; a separate optional legibility cleanup. The #37210/#18312/#33106 issue-triage provenance is **secondary-source corroboration**; the load-bearing evidence is the two live operator-run tests ([§17](../principles.md)), which stand independently.
  **Re-lock set (2):** [modes](../spec/systems/lifecycle/modes.md), [hooks](../spec/systems/infrastructure/hooks.md). **Re-lock:** `python3 lock.py --relock` run on both under this decision; `python3 validate.py` green.

## Why

The lock gate requires a landed-text cold audit with every blocking/serious finding resolved before re-lock; it ran, the two docs cleared on their own text, and the single blocking finding lived in D-171's append-only accounting — correctable only by a superseding entry, which this is (the [D-168](0168-resolve-the-d-167-operator-policy-override-re-litigation-lan.md)-corrects-[D-167](0167-take-up-q17-component-a-authorize-a-five-foundation-re-litig.md) / [D-170](0170-resolve-the-d-169-foundational-eadr-canon-re-litigation-land.md)-corrects-[D-169](0169-add-the-foundational-eadr-canon-the-engine-ships-its-own-why.md) pattern). Ground-truthing the carrier set against the text — rather than trusting the original single-doc framing *or* the audit lenses, which disagreed and one of which mis-quoted D-071's line — is the §17 discipline turned on the corpus itself. The capability-discovery analysis answers the operator's question on the record: a discovered capability is examined for what it *unlocks*, not only the falsehood it retracts; here it unlocks one optional defense-in-depth the operator declined, leaving the optimal design unchanged because the wall's placement never depended on deny-reliability.

## What we ruled out

**Edit D-171 to fix the carrier set** (rejected — append-only; supersede, never edit). **Leave the carrier accounting wrong** (rejected — a mis-statement inside the entry that corrects a mis-statement is the exact auditability hit D-171 exists to prevent). **Trust the audit lenses' carrier claims directly** (rejected — they disagreed and the architect lens mis-quoted D-071's line number while the adversarial lens mis-cleared it; the text read in full is the authority). **Add the in-session §15 DiD member because deny now works** (rejected by the operator — no guarantee added, self-disarmable, orphaned owner, §12 contagion; the merge-gate check is the wall). **Restate the dated "deny honored" platform fact in the WBS** (rejected — keep dated platform facts in the log per D-171's own principle; the WBS points and re-confirms).
