---
status: accepted
engine_record: true
---

# Resolve: re-lock `state` (the milestone honest-bound) — first carrier of the D-244 product-design SDD re-litigation

*Decided 2026-06-23 in the design workspace.*

## The decision

Re-lock [state](../spec/systems/cognitive/state.md), the first (most upstream) carrier of the [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md) re-litigation. The edit is **bounded**: the "standing-situation is assembled, not advanced" design-commitment bullet now names `milestone` as **engine-plan-derivable when a [product-design](../spec/modules/product-design.md) build-plan drives the build** (strong, like `phase` — build-orchestration emits the Milestones the build-plan groups), **else** operator-plan-derivable / `none set` (best-effort, the prior behavior). The load-bearing state laws are **untouched**: "No session advances it, and build-orchestration owns none of it," build-orchestration "merely produces" the Milestone artifacts, state "only reads the projection," and the offline-cache + staleness posture. This carries [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md)'s milestone-ownership resolution into state (build-orchestration produces; the build-plan is a *consumed grouping input*, never a competing author) and corrects the locked honest-bound that had characterized `milestone` as *only* operator-plan-derivable/`none set` — incomplete once the engine drives milestones from a committed build-plan. **The proportionate gate** for a bounded locked-doc clarification is a **focused two-lens cold re-check** (faithfulness/propagation + architect/seam, no shared context — the [D-141](0141-lock-product-design-the-design-front-door-the-fifth-module-l.md)/[D-242](0242-resolve-the-d-241-audit-memory-read-enablement-the-landed-fo.md) precedent, not the full four-lens reserved for the substantial carriers): **no blocking, no serious finding.** Both lenses confirmed faithfulness to [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md), the producer/reader split preserved, the strong/best-effort fallback exhaustive (build-plan-driven → strong; operator-kept Milestone → best-effort; bare/spec-less → `none set`), anti-changelog-clean prose, and the two un-edited milestone lines ("What it is"; "Degrades with GitHub") still true as-is (no companion edit owed). **Carry-forward to [D-247](0247-resolve-re-lock-build-orchestration-committed-spec-referent.md) (build-orchestration):** state makes two honest in-pass forward references the build-orchestration edit must close symmetrically — (a) build-orchestration must **explicitly produce native GitHub Milestones, consuming the product-design build-plan as a grouping input**; (b) **"build-plan" must be defined once** (product-design's carrier) and used identically across state/build-orchestration, not blurred with the "spec Issue." `python3 lock.py --relock systems/cognitive/state/README.md --decision D-245`; `python3 validate.py` green.

## Why

state is the cognitive cursor every session reads first, so it re-locks first in the dependency-ordered chain. The change is a one-bullet honest-bound clarification, not a redesign, so the focused two-lens re-check is the proportionate cold gate ([D-141](0141-lock-product-design-the-design-front-door-the-fifth-module-l.md)/[D-242](0242-resolve-the-d-241-audit-memory-read-enablement-the-landed-fo.md)) — a full four-lens audit on a bounded clarification is the disproportionate ceremony lock discipline and [R10](../reference/risks.md) caution against, and the substantial carriers ([build-orchestration](../spec/systems/lifecycle/build-orchestration.md), [product-design](../spec/modules/product-design.md)) get the full audit. The forward references to the not-yet-edited build-orchestration are honest and conditional ("*when* a build-plan drives the build"), so state asserts nothing false in the interim; [D-247](0247-resolve-re-lock-build-orchestration-committed-spec-referent.md) closes them.

## What we ruled out

**Edit the un-edited milestone lines** to mention the build-plan (rejected — both stay literally true: the projection reads "the active GitHub Milestone, or `none set`" whoever produced it, and Milestones still degrade with GitHub; the derivation-strength two-tier lives only where the honest bound is named, and over-propagating is churn). **Run the full four-lens audit** (rejected — disproportionate for a bounded one-bullet clarification; the focused two-lens re-check is the [D-141](0141-lock-product-design-the-design-front-door-the-fifth-module-l.md)/[D-242](0242-resolve-the-d-241-audit-memory-read-enablement-the-landed-fo.md) gate). **Let the build-plan author milestones directly** (rejected — [D-244](0244-re-litigate-product-design-into-a-first-class-spec-driven-de.md): collides with state's "build-orchestration owns none of it"). **Defer state to after build-orchestration** to dodge the forward reference (rejected — state is the more upstream carrier and locks first; the conditional forward reference is honest and [D-247](0247-resolve-re-lock-build-orchestration-committed-spec-referent.md) closes it, the standard in-pass transient).
