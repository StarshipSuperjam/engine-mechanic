---
status: accepted
engine_record: true
---

# Resolve: re-lock `product-design` (installs the conformance-enforcement floor's coverage leg — the derived, criterion-granular spec-obligation matrix — and produces the referent + operator-runnable rows the qa-review judgment/demo legs run; conditional on a `locked` `docs/spec/`) — a carrier of [D-287](0287-litigate-engine-template-427-make-the-sdd-spec-drive-the-bui.md)

*Decided 2026-07-06 in the design workspace.*

## The decision

Re-lock [product-design](../spec/modules/product-design.md), a carrier of [D-287](0287-litigate-engine-template-427-make-the-sdd-spec-drive-the-bui.md). The landed text — the spec-obligation matrix (derived-committed artifact + coverage check, criterion-cell digest at validated table position, per-doc lock, criterion-granular, migration-discipline-shaped, self-removing), the three-leg floor with [D-066](0066-the-4-4-review-lens-roster-two-stage-suites-mirroring-the-en.md)-clean ownership, the re-derive-from-span-not-matrix honest tier, and the conditional-on-locked-rows gate — passed the mandatory two-round landed-text cold audit recorded in [D-287](0287-litigate-engine-template-427-make-the-sdd-spec-drive-the-bui.md) (feasibility BLOCKING on span-identity resolved; all serious/nits folded; the change-narration phrase at *Re-litigation propagates* corrected to current-state). `python3 lock.py --relock modules/product-design/README.md --decision D-288`; `validate.py` green for this fingerprint.

## Why

product-design owns the coverage leg + referent; its re-lock lands after the clean landed-text audit. The floor extends the existing "two complementary floors" to full rigor without a hard dependency or a wall breach.

## What we ruled out

**Re-lock before the feasibility span-identity fix** (rejected — would lock an undeliverable "stale-flags exactly its rows" claim; the primitive had to be named first). **Attribute the judgment/demo legs to product-design** (rejected — [D-066](0066-the-4-4-review-lens-roster-two-stage-suites-mirroring-the-en.md): product-design ships no lenses; qa-review owns them, consumed-by).
