---
status: accepted
engine_record: true
---

# Resolve the D-221 first-run LICENSE-clear re-litigation: landed-text cold audit, two docs re-locked

*Decided 2026-06-19 in the design workspace.*

## The decision

The [D-221](0221-authorize-the-first-run-license-clear-re-litigation-reconcil.md) change-set is landed and **re-locked**. The mandatory landed-text cold audit ran with **four** independent no-shared-context lenses (adversarial · technical-feasibility · non-engineer-operator · architect) on the as-written edits in [provisioning](../spec/systems/infrastructure/provisioning.md) and [repository-topology](../spec/systems/infrastructure/repository-topology.md), plus the [risks](../reference/risks.md) [R29](../reference/risks.md) / [scenarios/first-run](../architecture.md#first-run-provisioning) / [engine-architecture](../architecture.md) carriers. **Outcome: no fatal flaw and no blocking or serious findings; the behavior verified byte-consistent (provisioning definition-of-record, the others by reference), the recognizer correctly the conjunction — not the README marker — the disclosure factual-not-advisory, the law-2 reconcile reframe clean; only nits, all dispositioned.** Re-lock set: **2** (both already locked → re-locked, none added).

  **The audit + dispositions.**
  - **Adversarial — no finding above nit.** Byte-consistency clean across the five homes (provisioning the definition-of-record; topology/first-run/engine-architecture reference, never re-define); the recognizer never implies the README marker or a body-only match; the disclosure never states "all rights reserved" as fact (provisioning actively forbids it, naming it false for a public repo); law 2 reads *reconcile, not a claim* with no stale "seeds once"; no anti-changelog cruft in the design docs. **Nit:** first-run compresses the public-repo caveat the definition-of-record holds — accepted (a walkthrough deferring to provisioning, never stating a flat legal claim).
  - **Technical-feasibility — re-verified against engine-template `origin/main`; all load-bearing claims confirmed.** The live `LICENSE` is stock MIT with a recognizable `StarshipSuperjam` holder line; the **body ∧ author-line conjunction** is decidable and conservative (the README recognizer at `_is_marketing_seed` proves the positive-match-or-preserve shape); brownfield excludes LICENSE **by construction** (`FOUNDATION_INFRA` membership — LICENSE absent); **nothing depends on the root LICENSE** (the sole reference is the marketing README's link, cleared in the **same** `_apply_substrates` greenfield phase, so no dangling link); the GitHub no-license / public-repo facts are accurate. **Nit:** nothing in the current instantiator rewrites the LICENSE copyright line (the only identity render is `_apply_codeowners`), so "evaluate before identity rendering" is **forward-defensive** — correct conservatism, kept as a build guard against a future renderer.
  - **Non-engineer-operator — the plan-audit's blocking disclosure concern is closed in the landed text.** "All rights reserved" is named and forbidden as a false legal conclusion; "private by default" carries the *until they choose to share it* nuance; the engine offers only to **add** a license the operator picks, never to choose; judgment routes to `choosealicense.com` + a human. The deletion **leads with reassurance** and **pre-empts the "No license" label**; the maintainer's gate is **behavioral** (generate → see no LICENSE → see the message), never a legal certification; both residuals carry a non-stranding remedy. **Nits:** first-run copy-parity (it compresses the human-escalation and No-license-label legs) — build-leaf, accepted.
  - **Architect — propagation complete and minimal.** The re-lock set is exactly **2** ([repository-topology](../spec/systems/infrastructure/repository-topology.md) ownership anchor, [provisioning](../spec/systems/infrastructure/provisioning.md) definition-of-record); the carriers (R29, first-run, engine-architecture) are all present; [glossary](../reference/glossary.md) correctly **verify-no-edit** with logged rationale; [modules/core](../spec/modules/core.md), [control-plane](../spec/systems/infrastructure/control-plane.md), [ontology](../spec/systems/grammar/ontology.md), [validation](../spec/systems/guardrails/validation.md)/[validators-core](../spec/modules/validators-core.md) all genuinely exempt; the law-2 reconcile reframe landed as a single named set with per-file verbs (scalable, not a two-verb special case); engine-architecture truthful; every link resolves.

  **Folds applied before re-lock:** none required — every finding was a nit. The forward-defensive "before identity rendering" clause and the first-run walkthrough compressions are kept as-is (correct conservatism / legitimate deferral to the definition-of-record), recorded here rather than edited.

  **Re-lock set (2):** [repository-topology](../spec/systems/infrastructure/repository-topology.md), [provisioning](../spec/systems/infrastructure/provisioning.md). **Re-lock:** `python3 lock.py --relock` run on both under this decision; `python3 validate.py` green. **Carriers edited (non-locked):** [risks](../reference/risks.md) ([R29](../reference/risks.md)), [scenarios/first-run](../architecture.md#first-run-provisioning), [engine-architecture](../architecture.md). **Verify-no-edit (confirmed by the audit):** [glossary](../reference/glossary.md) (a one-off deletion with no dual-life concept — R6 over-touch), [modules/core](../spec/modules/core.md) (LICENSE not a `FOUNDATION_INFRA`/`provides` artifact), [control-plane](../spec/systems/infrastructure/control-plane.md) (product territory), [ontology](../spec/systems/grammar/ontology.md) (a platform root file is not a surface), [validation](../spec/systems/guardrails/validation.md)/[validators-core](../spec/modules/validators-core.md) (no check). **No [open-questions](../reference/open-questions.md) opened/resolved; no [deviations](../reference/prototype-deviations.md) entry.** Still **42 docs locked** (2 re-locked, none added).

  **Build-owes (engine-template, downstream — not authored here):** carried from [D-221](0221-authorize-the-first-run-license-clear-re-litigation-reconcil.md), confirmed feasible by the audit: (1) realize the clear in `_apply_substrates` with the **body ∧ template-author-copyright-line conjunction** recognizer (not the README HTML marker — it breaks SPDX detection), preserve-on-doubt; (2) the template keeps shipping its stock MIT LICENSE (the recognizer keys off its content + holder line); (3) the **factual-not-advisory disclosure copy** (build-leaf); (4) the **sequencing gate** — the clear ships with/before any committed template LICENSE (the live state today *is* the unsafe pre-clear window the gate closes); (5) the **pre-fix-adopter boot detect-and-offer** (the [R20](../reference/risks.md) stranded-checkout pattern) — build-or-log the operator's call; (6) the **deliberate-maintainer authored opt-in**; (7) regenerate `graph.json` last, full suite + `validate --suite CI` green, one `guardrail-ack`.

## Why

The lock gate requires a landed-text cold audit with every blocking/serious resolved before re-lock; it ran (four lenses), found no fatal flaw and **no blocking or serious findings** — the conjunction recognizer and brownfield-by-construction were re-verified against the live `origin/main`, the disclosure-advice defect the plan-audit caught is closed, and the law-2 reconcile reframe is byte-consistent with provisioning as definition-of-record. Only nits surfaced (first-run copy compression; the forward-defensive ordering clause), each dispositioned as acceptable, so no fold was owed before the re-locks. The landed text is faithful to [D-221](0221-authorize-the-first-run-license-clear-re-litigation-reconcil.md).

## What we ruled out

**Re-lock without the landed-text audit** (rejected — HARD lock gate). **Fold the public-repo caveat into first-run** (rejected — first-run is a walkthrough deferring to provisioning, the definition-of-record, which holds the full caveat; expanding the bullet would bloat it and the lenses judged the deferral sound). **Soften the "before identity rendering" clause to match current code** (rejected — it is forward-defensive against a future renderer that touches the LICENSE; the design is correctly more conservative than the current instantiator requires, never less). **Add a glossary term for symmetry with the README** (rejected — a one-off deletion earns no dual-life term; R6 over-touch).
