---
status: accepted
engine_record: true
---

# Resolve: re-lock `build-orchestration` (roster +`divergence-hunter`, nine lenses / qa-review quintet; the mirror grounded-in-not-bijection; the paired-judgment-lenses reassignment; `over-build` in the §12 leak-guard) — a carrier of D-291

*Decided 2026-07-11 in the design workspace.*

## The decision

Re-lock [build-orchestration](../spec/systems/lifecycle/build-orchestration.md), a carrier of [D-291](0291-litigate-engine-template-427-follow-up-q-a-q-b-split-build-c.md). The landed text — the pre-submission roster list gains `divergence-hunter`; the *v1 lens roster* paragraph reads **nine lenses** (design-review quartet / qa-review quintet), the mirror **grounded in** the cold-session audit (a floor not a ceiling, the adversarial lane carried as its own standing lens — **not** a 1:1 bijection claim); the former `spec-conformance`-as-posture referent text reassigned to the **paired judgment lenses**; and **`over-build`** added to the [§12](../principles.md) maintainer-vocabulary leak-guard — passed the mandatory **landed-text cold audit**. **Architect/seam CLEAN:** control-plane is **not** a coupled carrier (the consumed-set is build-orchestration's own statement, the [D-262](0262-authorize-the-disposition-issue-resolution-check-a-17-7-mech.md) read-the-contract shape, not the [D-253](0253-resolve-re-lock-control-plane-the-review-record-carries-the.md) contract-recognition trap — corroborated by `validate.py` moving no control-plane fingerprint); the [agents](../spec/systems/surfaces/agents.md) surface stays verify-no-edit (open/additive `lens` axis, file-drop, membership enforced by this consumed-set + the §14 coherence check); **no dry-run owed** (no module-graph edge moves — qa-review's `depends: core`/`wires: none` unchanged, `provides` four→five is not a graph edge; the [D-288](0288-resolve-re-lock-product-design-installs-the-conformance-enfo.md)/[D-290](0290-resolve-re-lock-qa-review-the-spec-conformance-lens-carries.md) roster-enrichment precedent). **Nit folded before re-lock:** the one diff-register phrase ("deepens … rather than folding") the faithfulness lens flagged is rewritten purely structural ("carries the adversarial lane as its own standing lens"), holding authoring-rule 1. `python3 lock.py --relock systems/lifecycle/build-orchestration/README.md --decision D-293`; `validate.py` green.

## Why

build-orchestration runs the roster at the gates and holds the §12 leak-guard; its re-lock lands after the clean audit. The re-lock diff carries **only** D-291 floor-split deltas ([R6](../reference/risks.md) surgical scope). The mirror reframe keeps the index truthful at 4+5 without claiming a bijection the roster no longer has.

## What we ruled out

**Keep "deepens … rather than folding"** (rejected — the faithfulness lens flagged it as the one construction brushing the diff-register; a re-locked doc must read authored-complete-today, the [D-278](0278-resolve-re-lock-provisioning-boot-provisioned-verdict-on-ins.md)/[D-282](0282-resolve-re-lock-provisioning-the-engine-manifest-records-the.md) discipline). **Pull control-plane in as a coupled carrier** (rejected — the pre-submission gate adds no contract field/section; the [D-262](0262-authorize-the-disposition-issue-resolution-check-a-17-7-mech.md) shape, mechanically corroborated by the unmoved fingerprint).
