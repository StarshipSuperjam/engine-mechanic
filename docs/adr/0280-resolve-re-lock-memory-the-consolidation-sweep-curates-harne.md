---
status: accepted
engine_record: true
---

# Resolve: re-lock `memory` (the consolidation sweep curates harness-injected pseudo-turns out of its fuel + pending-detection — a capture-side axis independent of recall-exclusion) — the carrier of D-279; bundle confirmed verify-no-edit

*Decided 2026-07-04 in the design workspace.*

## The decision

Re-lock [memory](../spec/systems/cognitive/memory.md), the carrier of [D-279](0279-litigate-engine-template-360-the-memory-sweep-reads-the-raw.md). The landed text — the *Durability* fuel/sweep-visibility scoping + the detection-clause refinement + the new capture-side fuel-curation bullet (D-279 laws 1–5), the *Retrieval* carve-out + the "unfiltered"→"recall-exclusion never narrows the sweep" reword, and the *Build-spec leaves* recognition-predicate line — passed its mandatory **landed-text cold audit** (the irreversible-relock gate, CLAUDE.md) before this re-lock. **The four-lens audit** (faithfulness/propagation · adversarial+feasibility · non-engineer-operator · architect/seam, no shared context): **all four CLEAN — zero blocking, zero serious.**

  **Faithfulness — CLEAN:** the word "unfiltered" is gone (0 occurrences), the universal "always sweep-visible" re-scoped to the causal "recall-exclusion never removes a delta from the sweep … on that account" ([D-279](0279-litigate-engine-template-360-the-memory-sweep-reads-the-raw.md) B1 fix), all five laws landed faithfully, the recall ⊥ sweep orthogonality reads TRUE and un-vagued, internally consistent with *Capture*'s "episodic curation," *Active forgetting*'s "demotion can never reach erasure" firewall, the untyped-delta *Typing* model, and the memory↔knowledge "ambient deltas do not participate" line; authoring rules held (no changelog cruft — the [D-278](0278-resolve-re-lock-provisioning-boot-provisioned-verdict-on-ins.md)-class "restored" leak did **not** recur; history lives only in D-279); all new anchors resolve. **Adversarial+feasibility — CLEAN, "PROCEED":** every occurrence of the old false universal was made precise (the [D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md) B1 residual-universal failure does **not** recur), the mechanical/salience boundary weld is load-bearing (an adversarial cold builder cannot read a "drop what looks noisy" license out of "the only content withheld is … never a salience judgment … may never extend to skipping 'noisy'/'low-signal' genuine turns"), the firewall is preserved on the new axis, the all-injected-session narrowing is honestly owned via the ground-truth argument, and the landed description matches the merged #359 code exactly. **Non-engineer-operator — CLEAN, "SAFE":** the wording reads as *machinery the operator never authored is withheld from the summary* (not "memory thins my content"), the resident-and-recoverable guarantee (incl. the [D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md) recall disclosure-floor) is intact and un-contradicted, and **no new content-floor disclosure is owed** (nothing the operator observes changes; the change *removes* the trust hit of machinery narrated as operator speech — a disclosure here would be [§7](../principles.md) clutter). **Architect/seam — CLEAN:** single-ownership correct (fuel-curation masters in the memory system doc per [D-061](0061-lock-the-memory-system-wave-2-the-episodic-ledger-store-q3-b.md)), propagation complete (a corpus-wide grep for the stale universal outside memory-README+glossary found none — [boot](../spec/systems/lifecycle/boot.md)/wbs enumerate capture at the same implementation altitude as the bundle and assert no fuel-granularity), scope surgical (no concrete sentinel strings in canon), no contradiction with [D-273](0273-litigate-engine-template-332-ambient-turn-deltas-dominate-me.md)/[D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md) and no law duplicated across two homes.

  **The bundle is verify-no-edit — memory re-locks alone.** The architect lens confirmed against the landed bundle text the [D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md) disanalysis holds: [memory-substrate-sqlite-fts5](../spec/modules/memory-substrate-sqlite-fts5.md) `provides` enumerates "the capture code (turn-delta append, episodic consolidation, the abandoned-session sweep)" at the **what-ships implementation altitude** and its Design-commitments already route the laws to the system doc; #359's injected-recognition + sweep-skip is a refinement *inside* the capture code, adding no provided artifact/seam/hook/contract — so no bundle sentence shifts. `validate.py` flags only the memory fingerprint.

  **Nits (non-blocking, dispositioned — not folded, the audited text is what re-locks):** (adversarial) the tag-vs-recompute recognition is *decided* by #359 (both — durable tag + text-recompute back-compat) but the doc frames it as an open build-spec leaf — **acceptable as-built**: the *design* correctly does not mandate a representation, and "which/whether" is genuinely the build's call ([§16](../principles.md)); (adversarial) glossary *Deferred consolidation* says injected pseudo-turns are "never consolidated" (absolute) vs the softer README/*Ambient capture* phrasing — true against the build, harmless asymmetry; (operator) the "no reflection to recover — its records stay resident" sentence leads with the loss-sounding clause — **acceptable** for a maintainer-layer system doc (not operator-facing copy); (architect, **pre-existing, not a D-279 miss**) wbs/dry-run-ledger.md:381 cites "memory lines 68–69" for the abandonment-heuristic form and the landed edit shifted that text to ~lines 77–79 — the **quoted phrase still resolves verbatim** so the citation is not broken, only the line-number pointer is stale (the corpus's standing line-number-citation fragility, an out-of-scope hygiene item surfaced for awareness, not owed here).

  `python3 lock.py --relock systems/cognitive/memory/README.md --decision D-280`; the non-locked propagation ([glossary](../reference/glossary.md) *Ambient capture* / *Deferred consolidation*) landed under [D-279](0279-litigate-engine-template-360-the-memory-sweep-reads-the-raw.md); `python3 validate.py` green. The verify-no-edit dispositions [D-279](0279-litigate-engine-template-360-the-memory-sweep-reads-the-raw.md) recorded (bundle, [risks](../reference/risks.md) R24, [scenarios/](../architecture.md#build-session-lifecycle), [engine-architecture](../architecture.md), wbs/) stand confirmed against the landed text (the architect grep found no stale universal outside the edited files).

## Why

memory is D-279's only carrier (the bundle holds the implementation, not the fuel-curation law), so the re-lock lands here after its landed-text audit. All four lenses returned CLEAN with zero blocking/serious — the plan-stage audit's five laws and B1/B2/S-fold reshaping (the Durability detection clause, the operative sentinel boundary, the owned all-injected narrowing, the untyped-delta reconciliation, the required glossary amendment) had already been folded into the landed text, so the landed-text pass confirmed rather than reshaped. The authorize→resolve-after-landed-text-audit split kept the irreversible re-lock behind its own clean gate ([D-273](0273-litigate-engine-template-332-ambient-turn-deltas-dominate-me.md)/[D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md)).

## What we ruled out

**Re-lock the bundle too** (rejected — the landed bundle text does not shift; the architect lens confirmed fuel-curation is the system doc's law alone, the [D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md) implementation-altitude disanalysis). **Fold the operator sentence-ordering nit before re-locking** (rejected — a post-audit edit would re-lock text the four lenses did not review; the nit is non-blocking on a maintainer-layer doc, so the audited text re-locks and the nit is dispositioned acceptable-as-built). **Fix the stale wbs/dry-run-ledger.md line-number citation in this litigation** (rejected — pre-existing, not introduced by D-279, and the quoted text resolves verbatim; scope-creeping an unrelated hygiene fix into a locked-doc re-litigation violates R6 surgical-scope — surfaced for a separate pass).
