---
status: accepted
engine_record: true
---

# Resolve: re-lock `build-orchestration` (the submit-time close-linkage consistency pre-flight in the Review-record fill) — the carrier of D-283

*Decided 2026-07-05 in the design workspace.*

## The decision

Re-lock [build-orchestration](../spec/systems/lifecycle/build-orchestration.md), the sole carrier of [D-283](0283-litigate-engine-template-361-a-pr-accidentally-auto-closes-a.md). The landed text — the close-linkage consistency pre-flight in *The Review record* + its *Review-section layout* build-spec leaf — passed its mandatory **landed-text cold audit** (the irreversible-relock gate, CLAUDE.md) before this re-lock. **The four-lens audit** (adversarial · technical-feasibility/GitHub-facts · non-engineer-operator · architect, no shared context): **architect CLEAN, feasibility + operator no-blocking, one convergent adversarial SERIOUS folded before the lock.**

  **The KEY SEAM, resolved decisively — control-plane does NOT become a coupled carrier, no re-lock owed.** The architect lens adjudicated the seam [D-283](0283-litigate-engine-template-361-a-pr-accidentally-auto-closes-a.md) flagged for the landed audit in the **negative**: the pre-flight is **[D-262](0262-authorize-the-disposition-issue-resolution-check-a-17-7-mech.md)-shape fill** into the already-existing Review section (the [control-plane](../spec/systems/infrastructure/control-plane.md) contract fixes the section roster + the presence/non-empty gate + the honest tier, and **delegates *how* Review is filled to build-orchestration** — the pre-flight adds no section, no mandated field, no completeness-gate change), **not [D-253](0253-resolve-re-lock-control-plane-the-review-record-carries-the.md)-shape contract recognition** (which re-locked control-plane because [D-252](0252-authorize-binding-the-17-operator-runnable-behavioral-correl.md) refined a contract *claim* control-plane owns — the "one uncorroborable stretch" taxonomy — and introduced a new content *class* the contract had to name). The pre-flight touches neither; it inherits Review's tier rather than minting one. control-plane stays **verify-no-edit**.

  **The convergent SERIOUS finding (adversarial + operator, independently): the silent auto-edit branch.** [D-283](0283-litigate-engine-template-361-a-pr-accidentally-auto-closes-a.md)'s landed design let the orchestrator "**correct its own PR body before opening**" on an unambiguously-accidental keyword. The **adversarial** lens found this a **coupled three-face defect**: **(F1)** an unconstrained license to rewrite the operator-read narrative (no minimal-defang clause — the counterpart of the [D-254](0254-resolve-re-lock-build-orchestration-the-filler-renders-the-o.md) "renders, never authors" wall was missing); **(F2)** the accidental/deliberate call is a scope-**intent** judgment dressed as a machine-decidable read, whose *silent* path introduces the symmetric **false-negative** (a *wanted* close silently defanged → the issue silently left open), the false-witness direction [D-263](0263-resolve-re-lock-validators-core-the-disposition-issue-resolu.md) required be named; **(F3, [§17](../principles.md))** the "bounded by the operator's independent GitHub view" reassurance is **false on the auto-edit branch** — the engine removed the linkage before the PR opened, so GitHub's merge UI shows nothing for the operator to catch. The **non-engineer-operator** lens independently flagged the same axis (**S-3**: a silent self-correction of *what closes* trains over-trust on the exact backlog-truth axis burned four times; the doc's own auto-regeneration quiet-disclosure is the precedent).

  **Folded before re-lock (the [D-263](0263-resolve-re-lock-validators-core-the-disposition-issue-resolu.md)/[D-278](0278-resolve-re-lock-provisioning-boot-provisioned-verdict-on-ins.md) fold-before-relock practice):** the branch is recast **detect-and-surface, never silent-and-unilateral** — detection is mechanical, but acting never silently changes what an operator-visible PR will close; the **default** (and the only path where the close might have been intended) is to **surface** the contradiction in the Review record; only an **unambiguously-accidental** keyword may be **neutralized** (a minimal keyword-only defang, **never** a narrative rewrite), and any pre-open defang is **disclosed** in Review; the **false-negative residual** (a wanted close wrongly neutralized) is **named** as disclosed + operator-recoverable, never silent; the [§17](../principles.md) bound is made honest on **both** paths (GitHub's unresolved "will close #N" for the surface path; the disclosure for the defang path). The surface + disclosed-correction strings land in the paragraph; the leak-prone **comma-trap** and **could-not-read** strings are pinned in the build-spec leaf with their honesty rules (the comma-trap states the plain consequence — "the second issue will stay open" — without teaching keyword syntax; the could-not-read line points the operator to GitHub's own "will close" list and never asserts an edit the engine could not verify was needed).

  **Technical-feasibility — all GitHub premises CONFIRMED against current docs; two edge-bounds folded.** The commit-message/`closingIssuesReferences` blind spot (the reason the pre-flight also scans the integrated commits — a commit-message close auto-closes on merge yet stays absent from the field), the nine closing keywords, case-insensitivity, prose-embedding, the comma-trap under-link, one-keyword-per-issue, the `gh pr view --json closingIssuesReferences` field + `gh api graphql` fallback, and pre-merge readability on an open PR were each verified. **Two SERIOUS unbounded edges FOLDED:** cross-repo (`owner/repo#N`) closes and fork/[external-contribution](../spec/systems/lifecycle/external-contribution.md) cross-fork PRs are will-close entries the orchestrator **can see but has no local Scope line to adjudicate** — the pre-flight's adjudication is now explicitly **same-repo**, and such entries are **surfaced-and-named, never silently passed or defanged**. **NITs folded:** the `gh ≥ 2.72.0` floor recorded; the private-repo `issues: read` sub-scope named so a missing scope degrades to the could-not-read line rather than a false "nothing will close."

  **Non-engineer-operator — no blocking; SERIOUS resolved:** S-1 (the leak-prone comma-trap/could-not-read strings, deferred while the easy one was drafted, are now pinned with honesty rules), S-2 (the could-not-read line gives a fallback action and never asserts an unverified edit), S-3 (the disclosed-correction trace on the defang path), N-1 (the example rephrased "part of the work for #171," dropping the same-number-twice stumble). **Adversarial NITs folded:** F4 (the pre-flight line is conditional and **not** part of the Review completeness check's non-empty requirement — now stated), F5 (the **structured deliberate-close grammar** the pre-flight parses is now named as an owed build-spec leaf, so "escaped the fuzziness" is fully established). **Architect NITs (law/leaf read-composition redundancy, em-dash density, forward-reference to Review's tier) dispositioned acceptable-as-built** — the audited text is what re-locks.

  **The verify-no-edit dispositions [D-283](0283-litigate-engine-template-361-a-pr-accidentally-auto-closes-a.md) recorded stand CONFIRMED against landed text** (the [D-251](0251-reject-engine-template-255-s-explore-gate-memory-carve-out-t.md) discipline): [control-plane](../spec/systems/infrastructure/control-plane.md) (no re-lock — adjudicated above), [validation](../spec/systems/guardrails/validation.md)/[check](../spec/systems/surfaces/check.md)/[validators-core](../spec/modules/validators-core.md) (untouched — not a check rule, so the `hard`-scoped fixture machinery never engages), [scenarios/build-session.md](../architecture.md#build-session-lifecycle) (its submit-altitude — "fill the PR contract incl. the Review record → submit" — already encompasses the pre-flight; **no edit owed**, architect-confirmed), [risks](../reference/risks.md) [R30](../reference/risks.md) + the standing §17 self-report bound (**no new R-number**), [glossary](../reference/glossary.md) (no new term), [engine-architecture](../architecture.md)/[modules/README.md](../reference/module-catalog.md)/wbs (no module-graph edge, no re-sort — **no dry-run owed**, the [D-278](0278-resolve-re-lock-provisioning-boot-provisioned-verdict-on-ins.md)/[D-282](0282-resolve-re-lock-provisioning-the-engine-manifest-records-the.md) system-doc-law-enrichment precedent).

  `python3 lock.py --relock systems/lifecycle/build-orchestration/README.md --decision D-284`; `python3 validate.py` green.

## Why

build-orchestration is D-283's only carrier, so the re-lock lands here after its landed-text audit. The audit earned its keep: the **adversarial+operator convergence** caught that the silent auto-edit branch — added to reach [§6](../principles.md)'s self-correct audience — reintroduced the exact scope-intent judgment and silent backlog-change the pre-flight exists to prevent, and broke its own [§17](../principles.md) bound on the very path where the engine acted alone; the fold to **detect-and-surface + disclosed-minimal-defang** keeps the §6 nudge while restoring §17 honesty on both paths and the operator's legible account; the **feasibility** lens bounded the cross-repo/fork edges the same-repo framing left open; the **architect** lens resolved the control-plane seam in the negative, holding the blast radius to one locked doc. The teeth stay honest — a mechanical detection surfaced in the artifact the operator reads, no gate, the residual named. Authorize ([D-283](0283-litigate-engine-template-361-a-pr-accidentally-auto-closes-a.md)) → resolve-after-landed-text-audit (D-284) kept the irreversible re-lock behind its own clean gate ([D-273](0273-litigate-engine-template-332-ambient-turn-deltas-dominate-me.md)/[D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md)).

## What we ruled out

**Re-lock [D-283](0283-litigate-engine-template-361-a-pr-accidentally-auto-closes-a.md)'s silent auto-edit branch as landed** (rejected — the adversarial+operator convergence: a silent intent-judged edit of *what closes* is the false-witness/over-trust the pre-flight exists to prevent and breaks the §17 bound on its own auto-edit path; the four authoring rules and the [D-263](0263-resolve-re-lock-validators-core-the-disposition-issue-resolu.md) false-witness discipline demand the fold before the irreversible lock). **Drop the auto-edit branch entirely, always surface** (considered — the adversarial lens's cleaner option (b) — **not taken**: a minimal, disclosed, keyword-only defang preserves §6's fix-at-the-source value without the silent-edit harm, and the disclosure makes any misjudgment operator-recoverable; the narrow branch bounded-and-disclosed is honest where the blanket auto-edit was not). **Re-lock control-plane as a coupled carrier** (rejected — the architect lens adjudicated the pre-flight as [D-262](0262-authorize-the-disposition-issue-resolution-check-a-17-7-mech.md)-shape fill, not [D-253](0253-resolve-re-lock-control-plane-the-review-record-carries-the.md)-shape contract recognition; the contract already delegates *how* Review is filled to build-orchestration, so no contract claim changes). **Widen the pre-flight to adjudicate cross-repo/upstream closes** (rejected — the declared Scope describes this repo's backlog; a cross-repo/upstream will-close entry has no local scope line to contradict, so it is surfaced-and-named as out of reach, not adjudicated on a guess — the honest bound, not a silent pass). **Mint a new term or risk entry** (rejected — [R6](../reference/risks.md)/[D-255](0255-sharpen-the-post-v1-revisit-triggers-for-product-knowledge-g.md) anti-sprawl; [R30](../reference/risks.md) + the standing §17 bound carry the residuals).
