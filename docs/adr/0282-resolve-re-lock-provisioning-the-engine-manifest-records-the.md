---
status: accepted
engine_record: true
---

# Resolve: re-lock `provisioning` (the engine manifest records the home repository; the updater resolves the release source from it, three-state, never `origin`; the home is §15-guarded) — the carrier of D-281

*Decided 2026-07-05 in the design workspace.*

## The decision

Re-lock [provisioning](../spec/systems/infrastructure/provisioning.md), the carrier of [D-281](0281-litigate-engine-template-367-a-detached-deployed-repo-resolv.md). The landed text — *Upgrading the engine* step 1 (the manifest-home resolution, three-state, never `origin`, the §12-leak-guarded refusal copy, the staleness bound), the degrade paragraph (the home as a [§15](../principles.md) guardrail-integrity file, weakening-class repoint), and *The instantiator* confirm (the manifest records + carries the home forward) — passed its mandatory **landed-text cold audit** (the irreversible-relock gate, CLAUDE.md) before this re-lock. **The four-lens audit** (faithfulness/propagation · architect/seam · adversarial+feasibility · non-engineer-operator, no shared context): **three CLEAN/SAFE/PROCEED, one BLOCKING folded.**

  **The BLOCKING finding, folded before re-lock (the [D-278](0278-resolve-re-lock-provisioning-boot-provisioned-verdict-on-ins.md)/[D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md) fold-before-relock practice — the "restored"-class recurrence caught again):** **(faithfulness, authoring-rule 1)** provisioning step 1 closed with *"(a generated repo's origin is its own release-less repo — **the divergence this resolution closes**)"* — the trailing clause narrated the fix-event (deictic to the D-281 litigation act; presupposes a prior divergent state), a diff-against-the-past in a document that must read authored-complete-today → **"— the divergence this resolution closes" deleted; the parenthetical is a clean present-tense fact that stands alone** (the honest phrasing the scenario already uses at [scenarios/upgrade-the-engine.md](../architecture.md#upgrading-the-engine)). Confined to the one clause; the other four D-281 laws landed faithfully.

  **Faithfulness — otherwise CLEAN:** all five laws present and accurate (preserved-not-overlaid; seeded+carried; three-state; the named staleness bound; §15-guarded); **the plan-draft "§15 cannot-weaken-silently" overclaim did not survive** (the degrade paragraph frames an honest weakening-acknowledgment, and *Guardrail integrity* states solo = "not airtight — the engine holds the operator's token"); no doc still claims `engine.json` is overlaid wholesale; all anchors resolve. **Architect/seam — CLEAN:** single-ownership holds (the home owned in the manifest, two readers — updater + escalate-upstream); the §15 addition is **property-derived** ([D-268](0268-adjudicate-engine-template-250-the-15-weakening-guard-s-guar.md)), not a path append, and coheres with *Weakening acknowledgment*; scope minimal (fork-engine-lines not designed, the §15 set gained exactly the home, no new term/R-number). **Adversarial+feasibility — PROCEED:** the three-state walks every repo state without misfire; the "refuse loudly, naming the home" (resolution axis) and "degrade: source unreachable" (transport axis) are distinct non-contradictory paths; no bootstrap self-reference (the construction repo never dereferences its own home — the named inductive gap never runs at `0.0.0-dev`); every code seam re-verified verbatim (`engine.json` excluded from the overlay at `module_manager.py:527`/`FOUNDATION_CODE`, bumped in place preserving other keys at `_bump_engine_manifest`, the `repo=` threading seam real, a `_existing_home` carry the clean sibling of `_existing_release`); the "one coordinate, two readers" holds in every state including the engine-mechanic (its home and its product-upstream co-name the same GitHub repo but are two semantically-distinct manifest coordinates — non-reflexivity preserved, [D-102](0102-cross-repo-external-contribution-as-a-first-class-v1-operati.md)/[D-103](0103-cross-repo-design-cold-audit-resolutions-engine-clean-branch.md) untouched); the §15 guard makes a repoint to un-ratified output a **named** weakening act, hardening the ratification gate without re-litigating it. **Non-engineer-operator — SAFE:** the one verbatim operator-facing string (the §15 surface, *"this changes where your engine's code is fetched from — the AI could then pull code from a source you have not reviewed"*) leaks no maintainer vocabulary and is honest+non-alarming; both refusal cases promise reason + next step with the copy deferred under the §12 leak-guard; the staleness bound is a clean §7 named residual; the acknowledgment is proportionate (one act beyond the merge, gating only a repoint, never a code-read).

  **Nits (non-blocking, dispositioned — the audited text is what re-locks):** (architect) [systems/guardrails/audits/README.md](../spec/systems/guardrails/audits.md) is a genuine **second reader** of the home coordinate and already states the escalate-upstream coordinates are "known from the engine manifest" — **confirmed verify-no-edit** (fully consistent with D-281; recorded here for accounting completeness, it was not in D-281's enumerated list); (operator) the *rendered* name in the release-less "naming the home" refusal must itself stay plain (not a raw `org/repo` slug) — already routed through the leak-guarded build-spec leaf, a build-spec reminder; (feasibility) the build must **split** the release-less-`404` (name the home) from the transport error (degrade) rather than merely thread `repo=` and keep the current conflated message — already inside the [D-281](0281-litigate-engine-template-367-a-detached-deployed-repo-resolv.md) build-owe. The **verify-no-edit dispositions** [D-281](0281-litigate-engine-template-367-a-detached-deployed-repo-resolv.md) recorded stand **confirmed against landed text** (the architect discharge, [D-251](0251-reject-engine-template-255-s-explore-gate-memory-carve-out-t.md) discipline): [engine-architecture](../architecture.md) (generic row), [repository-topology](../spec/systems/infrastructure/repository-topology.md) (law 5 = category, the home is another preserved config key), [module-system](../spec/systems/grammar/module-system.md) (version-state "lockfile" altitude; the home is provenance, reasoning recorded), [external-contribution](../spec/systems/lifecycle/external-contribution.md) (*Upstream two senses* reaffirmed), [glossary](../reference/glossary.md) *Derived-committed artifact* (manifest still a [§19](../principles.md) non-member).

  `python3 lock.py --relock systems/infrastructure/provisioning/README.md --decision D-282`; the non-locked propagation ([glossary](../reference/glossary.md) *Engine manifest*/*Engine updater*/*Update source*/*Escalate-upstream*/*Guardrail integrity*, [risks](../reference/risks.md) R7, [scenarios/upgrade-the-engine.md](../architecture.md#upgrading-the-engine)) landed under [D-281](0281-litigate-engine-template-367-a-detached-deployed-repo-resolv.md) (with the one BLOCKING fold above); `python3 validate.py` green.

## Why

provisioning is D-281's only locked carrier (glossary/risks/the scenario are non-locked propagation), so the re-lock lands here after its landed-text audit. The audit earned its keep: the faithfulness lens caught the same diff-against-the-past class the D-278 audit caught (a trailing "closes the divergence" clause) before the irreversible lock; the adversarial+feasibility lens re-verified every code seam and confirmed the resolution/transport axes do not collide and the engine-mechanic case does not force the single coordinate wrong; the operator lens confirmed the one verbatim operator string is §12-clean and §7-honest. Three structural confirmations (property-derived §15 membership, one-coordinate-two-readers across all states, no overclaim survived) held the blast radius where D-281 scoped it. The authorize→resolve-after-landed-text-audit split kept the irreversible re-lock behind its own clean gate ([D-273](0273-litigate-engine-template-332-ambient-turn-deltas-dominate-me.md)/[D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md)).

## What we ruled out

**Re-lock without folding the "— the divergence this resolution closes" clause** (rejected — the four authoring rules are HARD; a lock gate must not fingerprint diff-against-the-past narration, even a trailing sub-clause, the [D-278](0278-resolve-re-lock-provisioning-boot-provisioned-verdict-on-ins.md) precedent). **Edit [systems/guardrails/audits/README.md](../spec/systems/guardrails/audits.md) to "add" the home reader** (rejected — it already attributes the escalate coordinates to the engine manifest; it is a confirmed verify-no-edit, and editing faithful text to match an accounting list is churn). **Edit [D-281](0281-litigate-engine-template-367-a-detached-deployed-repo-resolv.md) to record audits/README.md in its verify-no-edit list** (rejected — the decision-log is append-only; this resolve entry records the confirmed disposition). **Split the resolution/transport message in canon** (rejected — the message strings are a build-spec leaf; the design names the two axes, and the build-owe carries the split).
