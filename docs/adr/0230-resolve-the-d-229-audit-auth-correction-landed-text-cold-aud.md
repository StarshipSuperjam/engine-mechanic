---
status: accepted
engine_record: true
---

# Resolve the D-229 audit-auth correction: landed-text cold audit, `audit-library` re-locked; engine-template #175 discharged

*Decided 2026-06-21 in the design workspace.*

## The decision

The [D-229](0229-authorize-correcting-the-falsified-agent-sdk-credit-auth-cla.md) change-set is landed in [audit-library](../spec/modules/audit-library.md) and **re-locked**. The mandatory landed-text cold-session design audit ran with **four** independent no-shared-context lenses (adversarial · technical-feasibility · non-engineer-operator · architect) on the as-written auth subsection. **Outcome: adversarial PASS, technical-feasibility CONFIRMED, architect PASS, non-engineer-operator FIX-FIRST — its one in-scope serious fold applied, its second serious finding routed to the build-owes, two nits folded, the rest rejected-with-rationale.** Re-lock set: **1** (still **42 docs locked**; 1 re-locked, none added).

  **The audit + dispositions.**
  - **Technical-feasibility — CONFIRMED.** Independently ground-truthed every claim against the live [authentication](https://code.claude.com/docs/en/authentication.md) / [headless](https://code.claude.com/docs/en/headless.md) / [cli-reference](https://code.claude.com/docs/en/cli-reference.md) / [github-actions](https://code.claude.com/docs/en/github-actions.md) docs: the `CLAUDE_CODE_OAUTH_TOKEN` from `claude setup-token` is a **one-year** token that *"requires a Pro, Max, Team, or Enterprise plan"* and *"authenticates with your Claude subscription"*; **no** Agent-SDK credit allowance, separate/monthly pool, or account opt-in/toggle exists anywhere in the platform docs (the removed claim was genuinely fabricated); `--bare` *"does not read `CLAUDE_CODE_OAUTH_TOKEN`"* (the doc's never-`--bare` rule is verbatim-correct); the usage-limit framing is faithful (cross-surface usage shares one subscription pool; a limited run waits for reset).
  - **Architect — PASS.** Independently re-grepped the whole corpus for every false-claim phrase and auth detail: `Agent-SDK` / `monthly allowance` / `credit allowance` / `account opt-in` / `2026-06-15` match **nothing** outside the append-only log; `CLAUDE_CODE_OAUTH_TOKEN` / `setup-token` / `ANTHROPIC_API_KEY` match **only** this doc. Propagation provably complete; scope subtractive (no new field/capability/surface/gate, [R6](../reference/risks.md)); the three seam docs ([audits](../spec/systems/guardrails/audits.md), [agents](../spec/systems/surfaces/agents.md), [modules/core](../spec/modules/core.md)) carry only substrate-agnostic grammar the edit preserved; [R14](../reference/risks.md)/[R17](../reference/risks.md) never named the removed model (no edit owed); D-229's verify-no-edit list accurate.
  - **Adversarial — PASS.** Authored-complete-today (no scar where the deleted sentence sat, no dangling reference to the removed opt-in step anywhere in the doc); internally coherent; no other section still assumes the credit/opt-in model; no new false/unverifiable claim; deletion-mandate clean. One nit (folded, below).
  - **Non-engineer-operator — FIX-FIRST.** **(S1, folded)** "one step plus the secret" under-counted the operator's actual two hand-actions (run a terminal command; paste a named secret in GitHub) — reworded to **"two short steps"**, which is both clearer and truer to the correction's own anti-miscounting spirit. **(S2, routed to the build-owes)** `claude setup-token` carries an unstated prerequisite (Claude Code installed; a place to run it). Not a design-doc blocker: it is **pre-existing** (true of the whole `setup-token` approach since [D-148](0148-lock-audit-library-the-engine-self-audit-delivery-the-tenth.md), unrelated to the credit/opt-in correction) and the design doc already delegates plain, exact, complete step-naming to the **shipped walkthrough** (a build-spec leaf) — so the obligation is to have that walkthrough name the prerequisite, a downstream build-owes, not an in-scope locked-doc edit ([R6](../reference/risks.md)).

  **Folds applied before re-lock** (all in the auth subsection): the **two-short-steps** recount (S1); and a wording nit shared by the adversarial + technical-feasibility lenses — "draws down the plan's usage limits" → **"consumes the plan's usage"** and "produces no digest until the limit resets" → "until **it** resets" (a *limit* is the ceiling, not the thing depleted).

  **Rejected-with-rationale (logged, not buried).** **(feasibility nit)** the pre-existing hedge that the platform "does not reliably apply" a persona's frontmatter `model:` to a top-level `--agent` session (lines outside the auth subsection, untouched by this edit) is **unverifiable from the current docs but harmless** — the design's behavior (pin `--model` at the invocation, the documented authoritative override) is correct regardless; editing that locked sentence is scope creep beyond the authorized auth-credit correction ([R6](../reference/risks.md)), so it stays as the [D-148](0148-lock-audit-library-the-engine-self-audit-delivery-the-tenth.md) lock settled it. **(feasibility nit)** "no digest until reset" omits the optional purchase-usage-credits / spend-limit escape — deliberately, as the safe-default framing for a non-engineer; no change. **(feasibility note)** the Cloud-Routine "daily routine allowance" figure was not re-verified this pass — pre-existing, outside the auth subsection, out of scope.

  **Re-lock (1):** [audit-library](../spec/modules/audit-library.md). `python3 lock.py --relock modules/audit-library/README.md --decision D-230`; `python3 validate.py` green. **No carriers edited** (propagation contained to this doc + the append-only log, confirmed by two independent re-greps). **No [open-questions.md](../reference/open-questions.md) opened/resolved; no [deviations](../reference/prototype-deviations.md) entry.** **Supersedes** only the [D-148](0148-lock-audit-library-the-engine-self-audit-delivery-the-tenth.md)/[D-146](0146-resolve-q33-github-actions-stays-the-default-audit-substrate.md) auth-credit sub-claim. engine-template **#175** is **discharged** — the deployed page was corrected by #179 and the canon now matches reality — and closed with a reference to this decision; the S2 walkthrough-prerequisite build-owe is filed against the build, not authored here.

## Why

The lock gate requires a landed-text cold audit with every blocking/serious finding resolved or rejected-with-rationale before re-lock; it ran (four lenses), feasibility independently confirmed the platform premise against the live docs, adversarial and architect passed clean, and the operator lens's in-scope serious finding (S1) was folded while its second (S2) was ground-truthed as a pre-existing build-spec-leaf concern and routed to the build-owes rather than silently dropped. The design is faithful to [D-229](0229-authorize-correcting-the-falsified-agent-sdk-credit-auth-cla.md): the auth claim is now current-truth (paid-plan-funded one-year token, no phantom credit allowance or opt-in), the change is subtractive ([R6](../reference/risks.md)), and the substrate/invocation/Cloud-Routine/memory-backup grammar D-146/D-148 settled is untouched.

## What we ruled out

**Re-lock without the landed-text audit** (rejected — HARD lock gate). **Treat the operator S2 (CLI prerequisite) as a design-doc blocker** (rejected — pre-existing and true of the whole `setup-token` approach independent of this correction; the design already delegates exact step-naming to the shipped walkthrough, so the fix is a build-owes, [R6](../reference/risks.md), not an in-scope locked-doc edit). **Fold the frontmatter-`model:` hedge correction** (rejected — untouched pre-existing locked text; the pin-`--model` behavior is correct and doc-supported, so editing it is scope creep beyond the authorized auth-credit fix). **Edit carriers to capture the change** (rejected — two independent re-greps prove the auth claim lives only in this doc + the append-only log; there is nothing to propagate).
