---
status: accepted
engine_record: true
---

# Resolve: re-lock `control-plane` (the Review record carries the operator-runnable acceptance steps) — first carrier of D-252

*Decided 2026-06-27 in the design workspace.*

## The decision

Re-lock [control-plane](../spec/systems/infrastructure/control-plane.md), the first (contract-owner) carrier of [D-252](0252-authorize-binding-the-17-operator-runnable-behavioral-correl.md). The edit is **bounded** — the **Review** paragraph of the pull-request contract: **(1)** Review now also surfaces the change's **operator-runnable acceptance steps** (rendered from the realized `locked` spec's operator-runnable acceptance criteria) or a **plain reason-named no-op**; **(2)** the [D-073](0073-lock-build-orchestration-wave-3-terminal-and-re-litigate-con.md) "**one** section whose subject the operator cannot independently corroborate" claim is **refined** — that property attaches to the **review-judgment** part, while the **operator-runnable steps** are its converse, the one part the operator *can* corroborate by running ([§17](../principles.md) behavioral correlate at the merge); **(3)** the honest tier is unchanged (posture, "never dressed as a passed check"); **(4)** the *how* (verbatim render, bounded no-op, plain-language rendering) is deferred to [build-orchestration](../spec/systems/lifecycle/build-orchestration.md) (the seam below). No new section, no new field, no new check — the eight-section roster, the completeness-check paragraph, and the seam note are unchanged and stay truthful. **The proportionate gate** for a bounded edit to an operator-read trust contract — a **focused three-lens cold audit** (faithfulness/propagation · architect/seam · non-engineer-operator, no shared context — the [D-245](0245-resolve-re-lock-state-the-milestone-honest-bound-first-carri.md)/[D-248](0248-resolve-re-lock-qa-review-the-spec-conformance-referent-is-t.md) bounded-carrier gate, with the operator lens added because the paragraph governs operator trust): **faithfulness CLEAN** (faithful to [D-252](0252-authorize-binding-the-17-operator-runnable-behavioral-correl.md)'s contract clause, no build-detail leak, anti-changelog-clean, internally consistent, no unmet same-doc propagation); **seam HOLDS** (control-plane stops at the shape and disclaims the how; no validation/product-design re-lock implied; no new PR-contract field — the steps ride the existing Review section; all findings NIT). **Two SERIOUS (non-engineer-operator), fixed in-line:** D-252's two operator-protection laws lived only in the rationale, not anchored in the contract paragraph that binds the filler — **(a)** the **offer-not-duty** law (the steps are a check the operator may run when the change warrants it, not a duty on every merge — the under-trust guard) and **(b)** the **no-op honesty** law (the reason line states the cause in plain language and never leans on a passed mechanical check as if it were the operator seeing the change work — the over-trust guard on the no-op path). Both folded as bounded, audit-prescribed clauses ([goals §6](../reference/goals-and-quality.md) low-ceremony cited for (a)), the [D-242](0242-resolve-the-d-241-audit-memory-read-enablement-the-landed-fo.md) fold-before-relock practice, no separate re-check. **Carry-forward to [D-254](0254-resolve-re-lock-build-orchestration-the-filler-renders-the-o.md) (build-orchestration, the filler):** **(i)** its Review-record section must gain the operator-runnable-steps fill (the verbatim render via the [D-247](0247-resolve-re-lock-build-orchestration-committed-spec-referent.md) path read) + the **bounded four-reason no-op predicate** (control-plane's parenthetical is illustrative, not the closed set); **(ii)** it renders the promise-not-proof / offer-not-duty / plain-cause **copy** without re-asserting the contract tier (defer to control-plane, its existing "the honest tier is exactly control-plane's own" pattern); **(iii)** the plain-language buckets ("things you can confirm yourself" / "things I checked for you") bound by the same leak-guard that bars "acceptance criteria" from operator strings; **(iv)** the seam note's "(the Review section especially)" + per-turn-capture clause runs slightly ahead of build-orchestration's current text — D-254 closes the transient by landing the steps fill there. `python3 lock.py --relock systems/infrastructure/control-plane/README.md --decision D-253`; `python3 validate.py` green.

## Why

control-plane owns the required-section contract, so the contract-shape change (Review carries the steps + the corroborability refinement) lands here first, before the filler ([D-252](0252-authorize-binding-the-17-operator-runnable-behavioral-correl.md) dependency order). The focused three-lens gate was proportionate for a bounded one-paragraph edit ([D-245](0245-resolve-re-lock-state-the-milestone-honest-bound-first-carri.md)/[D-248](0248-resolve-re-lock-qa-review-the-spec-conformance-referent-is-t.md)) and earned its keep: the operator lens caught that both of D-252's operator-protection laws had not actually landed as *contract laws* (they sat in the rationale), so the filler would have been free to render the steps as a per-merge chore or to dress a passed check as proof on the no-op path — the exact over/under-trust traps the binding exists to avoid; anchoring them in the paragraph binds [build-orchestration](../spec/systems/lifecycle/build-orchestration.md). The seam held with only forward-hygiene NITs, which are D-254's to close, the authorized authorize-then-resolve cost.

## What we ruled out

**Leave the offer-not-duty and no-op-honesty laws in the rationale only** (rejected — the contract paragraph is what binds the filler; an operator-protection that lives only in the log lets a future build-orchestration pass render the steps as a chore or borrow "tests pass" as proof, both the [§17](../principles.md) traps). **Run the full four-lens audit** (rejected — a bounded one-paragraph edit; the focused three-lens gate is the proportionate [D-245](0245-resolve-re-lock-state-the-milestone-honest-bound-first-carri.md)/[D-248](0248-resolve-re-lock-qa-review-the-spec-conformance-referent-is-t.md) precedent, with the operator lens added for the trust-paragraph). **Edit build-orchestration now to close the seam-note transient** (rejected — build-orchestration is its own carrier needing its own audit; D-254 is next in this pass, the correct close). **Specify the closed four-reason no-op set in control-plane** (rejected — the predicate is the filler's; control-plane's reason examples are illustrative shape, D-254 owns the closed set).
