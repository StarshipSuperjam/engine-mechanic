---
status: accepted
engine_record: true
---

# Resolve: re-lock `memory` (the Layer-2 erasure grammar is one-or-more records per single-purpose PR; the batched-consent floor — each record's cost, never a bare total, all-or-nothing, ill-formed-batch-rejected-whole) — the carrier of D-285

*Decided 2026-07-05 in the design workspace.*

## The decision

Re-lock [memory](../spec/systems/cognitive/memory.md), the sole carrier of [D-285](0285-litigate-engine-template-363-the-layer-2-erasure-grammar-was.md). The landed text — the *Layer 2* bullet's one-or-more cardinality, the per-named-target marker under the shared merge SHA, `single-purpose`-binds-purpose-not-count + the anti-laundering fence, and the batched-consent floor (each record's plain-language cost, never a bare total, all-or-nothing, ill-formed-batch-rejected-whole) — passed its mandatory **landed-text cold audit** (the irreversible-relock gate, CLAUDE.md) before this re-lock. **The four-lens audit** (faithfulness/propagation · adversarial+feasibility · non-engineer-operator · architect/seam, no shared context): **all four CLEAN — zero blocking, zero serious.**

  **Faithfulness — CLEAN:** all six [D-285](0285-litigate-engine-template-363-the-layer-2-erasure-grammar-was.md) laws landed accurately; **no authoring-rule-1 violation** — a grep for change-narration (`now supports`/`widened`/`previously`/`used to`/`the divergence this closes`) found zero hits in the edited region (the [D-278](0278-resolve-re-lock-provisioning-boot-provisioned-verdict-on-ins.md)/[D-282](0282-resolve-re-lock-provisioning-the-engine-manifest-records-the.md) "restored"/"closes-the-divergence" failure class is **absent**); every enumerated in-region singular is resolved as [D-285](0285-litigate-engine-template-363-the-layer-2-erasure-grammar-was.md) claimed (`memory`:235 "one or an enumerated batch," :241 per-named-target, :242/:245 "removes each"/"names **each** target"; glossary :72/:73 plural) while :238 "the operator pointing at a record" is correctly **left** singular (a class contrast, not a count); a corpus grep found **no surviving singular universal** outside the intended edits; [Q40](../reference/open-questions.md) is clean present-tense with the build-slice vocabulary correctly confined to it and the log (never the locked carrier); the section still coheres with the crash-safe-swap law, [D-007](0007-memory-data-is-local-and-gitignored-substrate-ships-empty.md) content-freeness, and the "demotion can never reach erasure" firewall. **Adversarial+feasibility — PROCEED:** no residual universal made false by batching (the [D-279](0279-litigate-engine-template-360-the-memory-sweep-reads-the-raw.md)-B1/[D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md)-B1 missed-path pattern is not present); the consent floor is the **operative, code-bound** clause (`_pr_body`/`_collapse` never render a bare total; `_read_targets` whole-batch-rejects on any invalid id or `targets`/`costs` length mismatch; the age/never-recalled/merge gates bound selection independent of batch size); **no firewall breach** (per-record selection + adjudication, only the consent act batches); the D-209 laundering fence is structurally enforced (`_open_erasure_pr` PUTs exactly one proposal file); ground-truthed against merged **#362** — `compact.py`/`records.py` **unchanged**, `_collapse` groups identical costs into explicitly-counted rows (so "never a bare **total**" is faithful and "one line per record" would have overstated), the marker stays singular per target under the shared `merge_commit_sha`. **Non-engineer-operator — SAFE:** the per-record cost floor is stated as a real guarantee with a **non-AI correlate** (the `targets`/`costs` 1:1 length-check + whole-batch reject), all-or-nothing consent is honest and visible, the declined-batch residual is **disclosed not buried** ([Q40](../reference/open-questions.md) + [D-285](0285-litigate-engine-template-363-the-layer-2-erasure-grammar-was.md)'s §7 anti-choice), no mandated operator-facing string leaks engine jargon (the wording is a floor-altitude build-spec leaf); the change reads as the engine **earning trust** (a widened convenience with its irreversible cost made *more* legible; erasure authority unchanged). **Architect/seam — CLEAN, "memory re-locks alone":** single-ownership confirmed ([D-061](0061-lock-the-memory-system-wave-2-the-episodic-ledger-store-q3-b.md); erasure grammar homed in memory), [audits](../spec/systems/guardrails/audits.md) (:192/:319) and [boot](../spec/systems/lifecycle/boot.md) (:109) are **count-agnostic gate consumers, not coupled carriers** (the [D-253](0253-resolve-re-lock-control-plane-the-review-record-carries-the.md)-shape trap cleared — no contract claim changes; the firewall's per-record adjudication holds); propagation complete (independent grep found no omitted singular); the consent floor is proportionate (the batched realization of D-209's anti-rubber-stamp posture) and homed in the right doc; **no module-graph edge moved ⇒ no dry-run owed** ([D-279](0279-litigate-engine-template-360-the-memory-sweep-reads-the-raw.md)/[D-280](0280-resolve-re-lock-memory-the-consolidation-sweep-curates-harne.md) precedent); id allocation correct. The mechanical backstop corroborates single-ownership: `validate.py` flagged **exactly one** moved fingerprint — `memory` — and no other locked doc's.

  **The bundle and gate-consumers are verify-no-edit — memory re-locks alone.** Confirmed against landed text ([D-251](0251-reject-engine-template-255-s-explore-gate-memory-carve-out-t.md) discipline): [memory-substrate-sqlite-fts5](../spec/modules/memory-substrate-sqlite-fts5.md):28 enumerates the observer at the **what-ships altitude** (the batch is a refinement *inside* the enumerated capture code, adding no provided artifact/seam — the [D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md)/[D-280](0280-resolve-re-lock-memory-the-consolidation-sweep-curates-harne.md) disanalysis); [audits](../spec/systems/guardrails/audits.md)/[boot](../spec/systems/lifecycle/boot.md) assert the gate/readout, count-agnostic; wbs/memory-build-plan.md the Layer-boundary invariant; [risks](../reference/risks.md) R24 the resurrection-guard (marker singular, removal set already id-set-based); [scenarios/](../architecture.md#build-session-lifecycle) + [engine-architecture](../architecture.md) carry no erasure-cardinality prose.

  **Nits (non-blocking, dispositioned — the audited text is what re-locks):** (operator) the **decline-consequence** copy ("closing to keep one note forgoes erasing the rest") is owed at the operator-facing PR body — a downstream **audits/boot build-spec-leaf reminder** for the engine-template build, **no edit owed to the locked carrier** (correct layering; the carrier mandates the enumeration floor, not the copy); (adversarial ×3) the "the merged erasure-PR" singular reads fine per-merge-identity, the key-vs-carry phrasing in glossary :73 is harmless, and the `_collapse` mechanism correctly stays out of the system doc — all below fix-threshold; (architect, **pre-existing, not a D-285 miss**) wbs/dry-run-ledger.md:381's "memory lines 68–69" line-number pointer is the standing citation-fragility [D-280](0280-resolve-re-lock-memory-the-consolidation-sweep-curates-harne.md) already surfaced — the D-285 edits shift the region again, still out-of-scope hygiene, not owed here.

  `python3 lock.py --relock systems/cognitive/memory/README.md --decision D-286`; the non-locked propagation ([glossary](../reference/glossary.md) *Ledger compaction* / *Operator-adjudicated erasure*, [open-questions](../reference/open-questions.md) [Q40](../reference/open-questions.md)) landed under [D-285](0285-litigate-engine-template-363-the-layer-2-erasure-grammar-was.md); `python3 validate.py` green. The verify-no-edit dispositions [D-285](0285-litigate-engine-template-363-the-layer-2-erasure-grammar-was.md) recorded stand confirmed against the landed text (the architect grep found no stale singular outside the edited files).

## Why

memory is D-285's only carrier (glossary/open-questions are non-locked propagation; audits/boot are count-agnostic gate consumers), so the re-lock lands here after its landed-text audit. All four lenses returned CLEAN/SAFE/PROCEED with zero blocking/serious — the plan-stage audit's laws and folds (the :235 singular, the operative consent floor, the "never a bare total" `_collapse` fix, the D-209 laundering fence, the Q40 park) had already been folded into the landed text, so the landed-text pass confirmed rather than reshaped. The authorize→resolve-after-landed-text-audit split kept the irreversible re-lock behind its own clean gate ([D-273](0273-litigate-engine-template-332-ambient-turn-deltas-dominate-me.md)/[D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md)).

## What we ruled out

**Re-lock the bundle or a gate-consumer too** (rejected — the architect lens confirmed the batch adds no provided artifact/seam to [memory-substrate-sqlite-fts5](../spec/modules/memory-substrate-sqlite-fts5.md) and no contract claim to [audits](../spec/systems/guardrails/audits.md)/[boot](../spec/systems/lifecycle/boot.md); the arity is memory's grammar alone, the [D-274](0274-resolve-re-lock-memory-ambient-turn-deltas-excluded-from-rec.md)/[D-280](0280-resolve-re-lock-memory-the-consolidation-sweep-curates-harne.md) implementation-altitude disanalysis, and `validate.py` moved exactly one fingerprint). **Fold the operator decline-consequence nit into the carrier before re-locking** (rejected — a post-audit edit would re-lock text the four lenses did not review; the disclosure is correctly a floor-altitude operator-copy build-spec leaf, not locked-carrier law, so the audited text re-locks and the nit is a build-owe reminder). **Fix the stale wbs/dry-run-ledger.md:381 line-number in this re-lock** (rejected — pre-existing, [D-280](0280-resolve-re-lock-memory-the-consolidation-sweep-curates-harne.md) precedent; scope-creeping an unrelated hygiene fix into a locked-doc re-litigation violates [R6](../reference/risks.md) surgical-scope — surfaced for a separate pass). **Leave `memory` edited-but-not-relocked** (rejected — the litigation-alarm protocol closes only when the carrier is re-locked after a clean landed-text audit; a lingering fingerprint flag is an unfinished re-litigation, not a resting state).
