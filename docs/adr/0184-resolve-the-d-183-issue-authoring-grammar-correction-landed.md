---
status: accepted
engine_record: true
---

# Resolve the D-183 issue-authoring grammar correction: landed-text cold audit + focused re-audit clean, build-orchestration joined the re-lock set, four docs re-locked

*Decided 2026-06-06 in the design workspace.*

## The decision

The [D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md) change-set is landed and **re-locked**. The mandatory landed-text cold audit ran with four independent no-shared-context lenses (technical-feasibility · adversarial · non-engineer-operator · architect) on the as-written [control-plane](../spec/systems/infrastructure/control-plane.md) / [external-contribution](../spec/systems/lifecycle/external-contribution.md) / [modules/core](../spec/modules/core.md) edits + the non-locked carriers (wbs/core-build-roadmap.md slices IT/IH, [glossary.md](../reference/glossary.md), [risks.md](../reference/risks.md) R13) + [D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md); a **focused two-lens re-audit** (architect · adversarial) then ran on the folds. **Outcome: one BLOCKING and four SERIOUS findings, all resolved in the as-written text; [build-orchestration](../spec/systems/lifecycle/build-orchestration.md) joined the re-lock set — exactly the arbitration [D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md) deferred to this entry.** Re-lock set: **4**.

  **The audit findings + dispositions.**
  - **Technical-feasibility — PASS (no blocking/serious).** All four version-brittle platform facts re-verified true against current GitHub docs: issue templates travel as committed files; templates apply only via the web form (REST/`gh` set the body directly — established by the API surface carrying no template parameter + the standing open feature request, GitHub community discussion #4423); GitHub cannot gate issue creation (rulesets target branches/tags/pushes; `blank_issues_enabled:false` is not enforced for direct creation, #14287); template `labels:` auto-apply is web-only and `labels` is silently dropped without push access — so the A3 correction ("never a web-only issue-template default") is TRUE. The locked docs hold durable principles; the dated facts live in [D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md) + build-spec leaves cited to durable evidence, not a non-existent docs sentence (the [D-171](0171-correct-the-falsified-platform-ignores-a-pretooluse-deny-cla.md) discipline).
  - **Architect — BLOCKING B1, resolved.** [build-orchestration](../spec/systems/lifecycle/build-orchestration.md):374 still attributed the build-Issue body's concrete shape to **the human issue-template scaffold** — false, since the build Issue is engine-authored and **bypasses** the web templates ([D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md) platform fact). Resolved: the build-Issue body now realizes the **control-plane engine-authored-issue body contract via the issue-authoring helper** (the checklist + scope-lock are the build's "what happens next"); build-orchestration accordingly **joins the re-lock set** (the [D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md) "joins only if its locked language does not cleanly fill the skeleton" arbitration — it did not cleanly fill it). SERIOUS S1 (the wbs/core-build-roadmap.md slice-24 entry lacked the reciprocal `owes ← IH`, risking two issue-authoring paths) — resolved: slice 24 now routes the build Issue's creation through IH. The re-audit confirmed the re-lock set of 4 correct + complete and no carrier newly affected.
  - **Adversarial — SERIOUS (round 1) + SERIOUS (re-audit), both resolved.** Round 1: control-plane's "every engine-authored Issue" over-broadly governed the [audits](../spec/systems/guardrails/audits.md) **escalate-upstream draft** (a bug report the engine drafts for the *un-owned* template repo, which [external-contribution](../spec/systems/lifecycle/external-contribution.md) governs) — a contradiction between two docs re-locking together. Resolved: the binding tightened to "the Issues the engine authors **in its own repo's engine-labeled channel**," with a clause routing un-owned-upstream drafts to the host's conventions — the channel partition now tiles cleanly with external-contribution (no gap, no double-governance). Re-audit: "**pins their required sections**" for the *human* templates overloaded "required" (enforced-presence, as it legitimately is for the PR contract) against the same paragraph's "guide, do not gate" — a [§7](../principles.md) honesty brush. Resolved: "**pins their sections** (the prescribed shape, guidance not a gate)."
  - **Non-engineer-operator — 2 SERIOUS, resolved.** (1) The forbidden-engineer-idiom floor lived only in [D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md), not the locked doc a builder reads (the cited plain-language law forbids dumbing-down, not engineer-idiom) — resolved: the floor ("no 'scope'/'environment'/'expected vs. actual'/'acceptance criteria'/'reproduction steps'/'surface'") now lands in control-plane as the leaf's durable *boundary*. (2) "Do not gate" stated the intent but not the mechanism that makes it un-shippable — resolved: the locked doc now says the templates **ship as markdown templates, never issue forms with required-field validation**. Nits folded: the `engine-fault` prompt now reassures the filer they need not know the cause; skeleton item (1) is bound to the operator-communication law directly so a future producer inherits a plainness floor.

  **Matrix close-out — verified-no-edit (logged, not silent):** [telemetry](../spec/systems/guardrails/telemetry.md):86-92 ("plain-language operator contract … what the operator must decide") and [audits](../spec/systems/guardrails/audits.md):186-192 ("what was probed, found, recommended" + the pinned plain-prose exemplar, which the *loose* skeleton accommodates) both fill the body contract by the [§16](../principles.md) owner-side declaration without contradiction (the label-scheme inherit-by-reference pattern); [product-design](../spec/modules/product-design.md) spec Issue is the named realization exception ([D-141](0141-lock-product-design-the-design-front-door-the-fifth-module-l.md)), declared owner-side; [engine-architecture.md](../architecture.md):43 ("PR/issue templates") stays a truthful index. **Edited revisable carriers:** wbs/core-build-roadmap.md (slices IT/IH + the slice-18/24 reconciliation + the Next line), [glossary.md](../reference/glossary.md) (the new term, pinned here), [risks.md](../reference/risks.md) (the R13 follow-the-host clause). **No [open-questions.md](../reference/open-questions.md) opened or resolved** (a grammar fill, not a new thread); **no [deviations](../reference/prototype-deviations.md) entry**. The **build-repo follow-up** (engine-template issues #62/#63 → reference [D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md)/[D-184](0184-resolve-the-d-183-issue-authoring-grammar-correction-landed.md) + slices IT/IH; the template *copy*, helper *code*, and producer routing are build-layer work the slices schedule) is **not** authored in this workspace.

  **Re-lock set (4):** [control-plane](../spec/systems/infrastructure/control-plane.md), [external-contribution](../spec/systems/lifecycle/external-contribution.md), [modules/core](../spec/modules/core.md), [build-orchestration](../spec/systems/lifecycle/build-orchestration.md). **Re-lock:** `python3 lock.py --relock` run on all four under this decision; `python3 validate.py` green.

## Why

The lock gate requires a landed-text cold audit with every blocking/serious finding resolved before re-lock; it ran (four lenses + a focused two-lens re-audit on the folds), surfaced one blocking + four serious, and all were resolved in the as-written text. The build-orchestration blocking is exactly what the landed-text gate exists to catch — a locked doc asserting a now-false platform attribution — and its resolution expanded the re-lock set from the [D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md)-authorized 3 to **4**, precisely the arbitration [D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md) deferred here. The grammar the locked docs implied but never wrote is now pinned (the [D-122](0122-enumerate-the-branch-ruleset-protection-floor-in-control-pla.md)/[D-181](0181-correct-the-non-existent-github-scope-admin-repo-ruleset-in.md) "complete a claim the role implies" pattern), with the dated platform facts confined to the log + build-spec leaves ([D-171](0171-correct-the-falsified-platform-ignores-a-pretooluse-deny-cla.md)). Closes engine-template build issues #62/#63 at the design layer; the build proceeds per slices IT/IH.

## What we ruled out

**Re-lock the 3 docs [D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md) authorized and defer the build-orchestration fix** (rejected — the landed-text audit found build-orchestration carries a now-false attribution; leaving it ships a locked doc contradicting the very platform fact this correction establishes; the arbitration expanded the set to 4, as [D-183](0183-authorize-the-issue-authoring-grammar-correction-build-issue.md) anticipated). **Also edit telemetry / audits / product-design** (rejected — re-verified each fills the loose skeleton by the [§16](../principles.md) owner-side declaration with no contradiction; editing them would breach the inherit-by-reference pattern the label scheme already uses and add churn for no semantic gain). **Keep "pins their required sections"** (rejected — the human templates are guidance-not-gate; "required" reads as enforced-presence and brushes [§7](../principles.md)'s dress-posture-as-enforced line). **Leave the forbidden-idiom floor / markdown mechanism in the log only** (rejected — the builder reads the locked doc, not the log; these are the leaf's durable boundary, not brittle copy, so the operator-protection law belongs where it is read). **Defer the re-lock** (rejected — the audit cleared; leaving four locked docs fingerprint-failing is the silent-drift state the lock gate forbids).
