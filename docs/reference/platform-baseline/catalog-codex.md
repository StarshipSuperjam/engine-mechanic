# Platform capability baseline — catalog: Codex (OpenAI) platform capabilities

*Authored 2026-08-02 by the baseline audit required by
[engine-template #657](https://github.com/StarshipSuperjam/engine-template/issues/657), under the method in
[comparison-rules.md](comparison-rules.md). 55 capability records. Every observational field was extracted
from live vendor documentation fetched on 2026-08-02 (sources cited per record, allowlisted origins only);
every reconciliation field was read from the engine-template checkout at `cdbbc335` (its HEAD, the same
commit the product spec describes); dispositions and assessments are the orchestrating session's judgment,
assigned per record through an ordered rule table with per-record rationale, and adversarially verified.
Assessment fields are stated per record from the record's disposition class with reconciliation-flagged
overrides — a bounded method, disclosed here, not a per-field independent derivation. Dispositions in this
document are recommendations until the migration decision record ratifies them.*

<!-- generated by assemble_catalog.py from Wave B/C/D artifacts; reviewed by the orchestrating session -->


## Family: approvals-sandbox

### `codex/approvals-sandbox/approval-policy`

```yaml
id: codex/approvals-sandbox/approval-policy
provider: codex
family: codex / approvals-sandbox
name: Approval policy (approval_policy)
what: Controls when Codex pauses to ask the user before running a command or applying a change. Values are "untrusted" (approve everything but a trusted set), "on-request" (the agent asks when it wants to escalate), and "never" (no interactive prompts). A granular object form exposes per-category toggles.
surfaces: [cli, desktop, cloud-web]
ownership: user
persistence: local-config
mechanics: >
  Set via config.toml key `approval_policy` = "untrusted" | "on-request" | "never",
  or a `{ granular = { ... } }` object whose booleans include `sandbox_approval`,
  `rules`, `mcp_elicitations`, `request_permissions`, and `skill_approval`. The CLI
  flag `--ask-for-approval` takes the same three enum values; `/permissions` sets it
  interactively. The ChatGPT desktop app surfaces this as presets "Ask for approval"
  (pairs on-request with workspace-write) and "Approve for me"/Auto-review. Distinct
  from sandbox_mode: approvals govern the prompt, the sandbox governs the boundary.
sources:
  - url: https://developers.openai.com/codex/config-file/config-reference
    retrieved: 2026-08-02
    note: Lists approval_policy values untrusted/on-request/never and the granular sub-keys.
  - url: https://developers.openai.com/codex/permission-modes
    retrieved: 2026-08-02
    note: Desktop-app presets mapping on-request approvals to the workspace-write sandbox.
  - url: https://developers.openai.com/codex/cli/reference
    retrieved: 2026-08-02
    note: --ask-for-approval flag accepts untrusted/on-request/never.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: >
  No top-level approval_policy is set in the committed .codex/config.toml (which contains only two
  [mcp_servers.*] blocks). The routine runbook instructs the operator to set approval_policy = "never"
  by hand for a Codex Automation (README.md:280-286) — it is operator-configured, not shipped.
overlap: >
  The engine's own approval posture for MCP tools is expressed per-server as
  default_tools_approval_mode = "auto" on both engine-memory and engine-knowledge-graph
  (.codex/config.toml) — that is the MCP granular approval surface, distinct from the global
  approval_policy the record describes.
note: >
  The engine's stance system (explore/build/routine, modes.py) plays the role a Codex approval policy
  would, but is provider-neutral engine code, not Codex's native approval_policy. The granular object
  form and its sub-keys (sandbox_approval, rules, mcp_elicitations, request_permissions, skill_approval)
  are unused.
```

**Assessment:** action mode blocks/intercepts; enforcement deterministic where the platform honors it; the engine treats it as best-effort; failure mode open; portability provider-specific behind the engine's adapter seam.

**Disposition: `HOST CONFIGURATION`** — approval_policy is operator-set for Automations (never), not committed; the engine's stance system is the neutral layer above.

### `codex/approvals-sandbox/cloud-internet-access`

```yaml
id: codex/approvals-sandbox/cloud-internet-access
provider: codex
family: codex / approvals-sandbox
name: Cloud sandbox internet access
what: Codex cloud runs each task in a per-environment sandbox that, unlike local workspace-write, blocks internet during the agent phase by default while still allowing internet during the setup-script phase. Network reach is governed by an environment-level allowlist and HTTP-method restrictions rather than the local sandbox_mode keys.
surfaces: [cloud-web]
ownership: org
persistence: cloud
mechanics: >
  Per-environment internet setting has Off (blocked), On (allowed, restrictable), and
  All/unrestricted. Allowlist presets are None (empty, add your own), Common dependencies
  (curated for package managers/build tools), or All; custom domains can be added.
  HTTP methods can be limited to GET/HEAD/OPTIONS, blocking POST/PUT/PATCH/DELETE. Setup
  scripts always run with internet regardless of agent-phase setting, so dependencies
  install before the restricted agent phase. In ChatGPT Work, admins gate this via
  Settings > Data controls > Work network access (public internet vs. managed allowlist).
  Docs flag prompt-injection/exfiltration risk when agent internet is enabled.
sources:
  - url: https://learn.chatgpt.com/docs/cloud/internet-access
    retrieved: 2026-08-02
    note: Cloud default-off during agent phase, setup-phase internet, allowlist presets, HTTP-method limits.
  - url: https://developers.openai.com/codex/concepts/sandboxing/
    retrieved: 2026-08-02
    note: ChatGPT Work admin control (Data controls > Work network access) for managed allowlists.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  Cloud internet-access controls are per-environment / org-level (ChatGPT Work Data controls), not
  committable repo state — nothing in engine-template configures them. The engine's cloud story is a
  Codex Automation on a background worktree (README.md:280-293) whose network is granted in the
  Automation UI, outside any repo file.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `HOST CONFIGURATION`** — Platform/OS enforcement and cloud network controls sit with the host and org, not the repo.

### `codex/approvals-sandbox/platform-sandbox-mechanisms`

```yaml
id: codex/approvals-sandbox/platform-sandbox-mechanisms
provider: codex
family: codex / approvals-sandbox
name: Platform-native sandbox enforcement
what: Codex enforces the sandbox using OS-native mechanisms rather than a single implementation, so the same modes behave consistently across macOS, Linux, WSL2, and native Windows. The enforcement layer is what actually confines filesystem and network access for the chosen sandbox_mode.
surfaces: [cli]
ownership: vendor-managed
persistence: none
mechanics: >
  macOS uses the built-in Seatbelt framework. Linux and WSL2 use bubblewrap (user
  namespaces) plus seccomp filtering, with Landlock available as a compatibility
  fallback. Native Windows uses dedicated lower-privilege sandbox users, filesystem
  permission boundaries, and firewall rules (PowerShell path); WSL2 defers to the
  Linux mechanisms. Enforcement is automatic per OS and is not user-configured beyond
  selecting the sandbox_mode.
sources:
  - url: https://developers.openai.com/codex/concepts/sandboxing/
    retrieved: 2026-08-02
    note: Names Seatbelt (macOS), bubblewrap+seccomp (Linux/WSL2), and Windows sandbox users.
  - url: https://developers.openai.com/codex/permissions
    retrieved: 2026-08-02
    note: Confirms per-OS enforcement (Seatbelt / bubblewrap+seccomp / elevated Windows sandboxing).
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  Vendor-managed OS enforcement (Seatbelt / bubblewrap+seccomp / Windows sandbox users) — nothing in
  the repo configures or depends on it beyond naming sandbox_mode="read-only" on the review agents.
  hook-runner.sh's per-OS branching is only venv-interpreter resolution, unrelated to Codex sandbox
  enforcement.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `HOST CONFIGURATION`** — Platform/OS enforcement and cloud network controls sit with the host and org, not the repo.

### `codex/approvals-sandbox/sandbox-modes`

```yaml
id: codex/approvals-sandbox/sandbox-modes
provider: codex
family: codex / approvals-sandbox
name: Sandbox modes (sandbox_mode)
what: Sets the filesystem/command boundary the agent operates inside. "read-only" lets it inspect files but not edit or run commands without approval; "workspace-write" (the default for local work) lets it read, edit within the workspace, and run routine local commands; "danger-full-access" removes all sandbox restrictions.
surfaces: [cli, desktop, cloud-web]
ownership: user
persistence: local-config
mechanics: >
  config.toml key `sandbox_mode` = "read-only" | "workspace-write" | "danger-full-access";
  CLI flag `--sandbox` takes the same three values. Three built-in permission profiles map
  to these: `:read-only`, `:workspace` (writes inside active workspace roots plus system temp),
  and `:danger-full-access`. Profiles also define per-path `read`/`write`/`deny` access levels,
  where specificity wins (deny > write > read). `--full-auto` is a deprecated compatibility
  flag (prefer `--sandbox workspace-write`); `--dangerously-bypass-approvals-and-sandbox`
  runs every command with no approvals and no sandbox.
sources:
  - url: https://developers.openai.com/codex/concepts/sandboxing/
    retrieved: 2026-08-02
    note: Defines the three sandbox_mode values and workspace-write as the default local mode.
  - url: https://developers.openai.com/codex/permissions
    retrieved: 2026-08-02
    note: Built-in profiles :read-only/:workspace/:danger-full-access and read/write/deny precedence.
  - url: https://developers.openai.com/codex/cli/reference
    retrieved: 2026-08-02
    note: --sandbox enum values and the --full-auto / --dangerously-bypass flags.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: >
  sandbox_mode = "read-only" is emitted into all 10 generated agent files (.codex/agents/*.toml).
  It is HARDCODED — codex_gen.render_agent writes the literal 'sandbox_mode = "read-only"'
  (.engine/tools/codex_gen.py:128), not mapped from the Claude persona's permissions field. No
  session-level sandbox_mode is set in .codex/config.toml; the workspace-write mode needed for the
  unattended routine is left to the operator to set by hand (README.md:282-283).
overlap: >
  These read-only agent sandboxes reinforce the same read-only posture the Claude personas declare via
  permissions: read-only + disallowedTools (.claude/agents/*.md), regenerated in parity by codex_gen.py.
note: >
  Divergence from the surface-inventory's claim that sandbox_mode "maps from Claude permissions" — in
  the built code it is an unconditional literal (every persona is a report-only reviewer;
  codex_gen.py:20). The built-in profiles (:read-only/:workspace/:danger-full-access), per-path
  read/write/deny levels, and the --sandbox / --full-auto / --dangerously-bypass flags are unused.
```

**Assessment:** action mode blocks/intercepts; enforcement deterministic where the platform honors it; the engine treats it as best-effort; failure mode open; portability provider-specific behind the engine's adapter seam.

**Disposition: `ADAPTER`** — sandbox_mode=read-only is hardcoded into every generated persona (a divergence from mapping-from-permissions worth noting); session posture for routines is operator-set.

### `codex/approvals-sandbox/workspace-write-network-and-roots`

```yaml
id: codex/approvals-sandbox/workspace-write-network-and-roots
provider: codex
family: codex / approvals-sandbox
name: Workspace-write network access and writable roots
what: Within the workspace-write sandbox, outbound network access is off by default and is toggled independently of the filesystem boundary; the set of writable roots can be widened or narrowed. This is the local (non-cloud) network control.
surfaces: [cli, desktop]
ownership: user
persistence: local-config
mechanics: >
  Under the `[sandbox_workspace_write]` table: `network_access` (boolean) "Allow
  outbound network access inside the workspace-write sandbox"; `writable_roots`
  (array of strings) adds writable directories; `exclude_tmpdir_env_var` (boolean)
  drops `$TMPDIR` from writable roots; `exclude_slash_tmp` (boolean) drops `/tmp`.
  Reaching network without approval prompts otherwise requires `--sandbox
  danger-full-access` or the `--dangerously-bypass-approvals-and-sandbox` flag. A
  known caveat (GitHub issue) is that on macOS Seatbelt can silently ignore
  network_access = true.
sources:
  - url: https://developers.openai.com/codex/config-file/config-reference
    retrieved: 2026-08-02
    note: sandbox_workspace_write.network_access / writable_roots / exclude_tmpdir_env_var / exclude_slash_tmp keys and descriptions.
  - url: https://developers.openai.com/codex/concepts/sandboxing/
    retrieved: 2026-08-02
    note: Network restriction is tied to sandbox mode; workspace-write bounds network by default.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  No [sandbox_workspace_write] table in .codex/config.toml — network_access, writable_roots,
  exclude_tmpdir_env_var, exclude_slash_tmp are all unset. The routine runbook tells the operator to
  "grant the run network access" through the Automation UI and to set sandbox_mode="workspace-write"
  by hand (README.md:282-286), so the writable-roots/network keys are never committed. The documented
  macOS Seatbelt network_access caveat is therefore not something the engine relies on.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `HOST CONFIGURATION`** — Platform/OS enforcement and cloud network controls sit with the host and org, not the repo.


## Family: automations-scheduling

### `codex/automations-scheduling/feature-maturity-levels`

```yaml
id: codex/automations-scheduling/feature-maturity-levels
provider: codex
family: automations-scheduling
name: Feature maturity levels
what: OpenAI publishes a four-level maturity framework that classifies ChatGPT and Codex features by stability, so users can judge whether a given capability is safe to depend on. The levels are "Under development," "Experimental," "Beta," and "Stable." The reference page defines the levels but does not itself enumerate per-feature assignments.
surfaces: [cli, desktop, ide, cloud-web, sdk-api]
ownership: vendor-managed
persistence: none
mechanics: >
  Four levels with vendor-stated meanings: "Under development" — not ready for use, "Don't use";
  "Experimental" — unstable, OpenAI may remove or change it, "Use at your own risk"; "Beta" — ready for
  broad testing, complete in most respects but some aspects may change on user feedback, recommended for
  "evaluation and pilots; expect small changes"; "Stable" — fully supported, documented, ready for broad
  use, behavior and configuration remain consistent over time. This is a documentation/labeling
  convention (no config surface), applied across Codex and ChatGPT features. The maturity page does not
  list specific features with labels, but concrete maturity states appear across the changelog this run,
  e.g.: Goal mode "exits experimental status" (2026-05-21); GPT-5.4 "1M context window experimental
  support" (2026-03-05); GPT-5.3-Codex-Spark "research preview" (2026-02-12) and Chronicle "research
  preview" (2026-06-16); Sites in "preview" (2026-06-02); "Sign in with ChatGPT (beta)" (2026-07-29).
sources:
  - url: https://developers.openai.com/codex/feature-maturity
    retrieved: 2026-08-02
    note: >
      Feature-maturity reference (308-redirects to https://learn.chatgpt.com/docs/feature-maturity,
      fetched this run). Evidences the four level names and their verbatim definitions; confirms the page
      is a framework, not a per-feature status inventory.
  - url: https://developers.openai.com/codex/changelog
    retrieved: 2026-08-02
    note: >
      Changelog (via redirect, fetched this run). Supplies the concrete maturity-transition data points
      (Goal mode leaving experimental, 1M-context experimental, research previews, beta sign-in, Sites
      preview) that the maturity page itself does not enumerate.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: A vendor documentation/labeling convention with no config surface; nothing in engine-template consumes or references the four-level maturity framework.
```

**Assessment:** action mode observes; enforcement none — signal only; failure mode n/a; portability provider-specific.

**Disposition: `OBSERVATION ONLY`** — Vendor labeling convention; useful reading signal for future platform-currency runs.

### `codex/automations-scheduling/scheduled-tasks`

```yaml
id: codex/automations-scheduling/scheduled-tasks
provider: codex
family: automations-scheduling
name: Scheduled tasks (Automations)
what: Codex lets a user schedule recurring tasks that run in the background unattended on a defined schedule. Two task types exist — standalone tasks that start an independent chat per run, and chat-embedded ("thread") automations that wake the same thread on schedule while preserving conversation context. Runs and their findings collect in a "Scheduled" management inbox.
surfaces: [desktop, cloud-web]
ownership: mixed
persistence: cloud
mechanics: >
  Created and managed from the ChatGPT desktop app and from web ("ChatGPT Work"); the Codex CLI and
  IDE extension can only view scheduled tasks, not create/manage them (use web or desktop to create).
  Scheduling supports fixed minute-based intervals, daily/weekly time-based schedules, and custom
  RFC 5545 recurrence rules (RRULE), e.g. `RRULE:FREQ=MONTHLY;BYMONTHDAY=1;BYHOUR=9;BYMINUTE=0`,
  editable directly. Two task types: standalone (new chat per run, can run across one or more projects)
  and chat-embedded/thread automations (return to an existing chat with accumulated context; suited to
  monitoring deployments or polling connected services). Execution environment: for Git repos a run
  targets either the local project checkout (changes hit the main checkout) or a Codex-managed worktree
  (keeps scheduled-task changes separate from unfinished local work); non-version-controlled projects run
  in the project directory. Desktop runs need local files present — "Keep the computer on and the app
  running when a scheduled task needs local files." Web runs use uploaded files, connected tools, skills,
  and plugins with no persistent local folder. Users can explicitly select model and reasoning effort, or
  use defaults; templates exist to seed new automations. Runs unattended with `approval_policy = "never"`
  when organization policy allows, and respect the sandbox mode (read-only, workspace-write, or full
  access); admins can enforce restrictions via `requirements.toml`. Outputs/notifications: the "Scheduled"
  inbox lists active, paused, and completed tasks; an unread indicator flags runs needing attention;
  bulk actions allow marking every run as read or archiving eligible runs. File/app mentions supported in
  the automation composer; automation titles/icons show in the sidebar.
sources:
  - url: https://developers.openai.com/codex/automations
    retrieved: 2026-08-02
    note: >
      Canonical automations doc (308-redirects to https://learn.chatgpt.com/docs/automations, fetched this
      run). Evidences the scheduled-tasks feature, standalone vs chat-embedded task types, RRULE/interval/
      daily-weekly scheduling, local-vs-worktree execution, desktop+web create / CLI+IDE view-only,
      approval_policy="never", sandbox modes, requirements.toml admin enforcement, model/reasoning
      selection, and the "Scheduled" inbox with unread indicators.
  - url: https://developers.openai.com/codex/changelog
    retrieved: 2026-08-02
    note: >
      Codex changelog (308-redirects to https://learn.chatgpt.com/docs/changelog, fetched this run).
      Dates the capability's evolution: 2026-03-12 revamped Automations (local/worktree choice, custom
      reasoning/models, templates); 2026-04-01 "heartbeat automations" and file/app mentions in the
      automation composer; 2026-05-14 thread automations that wake the same thread on schedule preserving
      context; 2026-06-18 bulk actions on automation run history.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: This is the Codex-side host for the engine's unattended routine. /Users/shanekidd/Developer/engine-template/.claude/skills/engine-routine/SKILL.md names "a Codex **Automation**" as an alternate host running `$engine-routine`; /Users/shanekidd/Developer/engine-template/.engine/operations/routine-entry.md is the shared entry procedure; /Users/shanekidd/Developer/engine-template/.engine/operations/codex-validation.md item 10 is the live acceptance test (configure a Codex Automation with `approval_policy="never"` + `workspace-write`, a dedicated background worktree, pointed at a scope-locked build Issue; confirm it enters Routine, advances one chunk, never merges). Item 11 additionally uses a `sandbox_mode="read-only"` Automation to host the from-Codex self-review persona.
overlap: modes.py `set-routine` isolation gate + routine-entry.md + operating-modes.md Routine stance are the same engine layer used for both providers; codex-validation.md is the Codex acceptance runbook.
note: The engine relies on two unverifiable-from-repo Codex platform facts (codex-validation.md item 10 "item zero"): that the installed Codex build supports Automations AND that a scheduled Automation fires SessionStart and its "dedicated background worktree" is a git-linked worktree the isolation gate recognizes. routine-entry.md (step 1, line 23) also encodes a Codex-specific caveat: hooks stay OFF after any engine update that changed them until the operator re-trusts via `/hooks` — so the routine refuses to write until hooks are confirmed. Whether a Codex Automation provides single-flight is noted as unverified (routine-entry.md line 87). `requirements.toml` admin enforcement and the "Scheduled" inbox are not engine-managed.
```

**Assessment:** action mode blocks/intercepts; enforcement deterministic where the platform honors it; the engine treats it as best-effort; failure mode open; portability provider-specific behind the engine's adapter seam.

**Disposition: `CORE`** — The Codex-side unattended host; the engine layers its isolation gate and acceptance runbook on it, with two platform facts recorded as unverifiable from the repo.

### `codex/automations-scheduling/thread-automations`

```yaml
id: codex/automations-scheduling/thread-automations
provider: codex
family: automations-scheduling
name: Thread automations (scheduled check-ins / heartbeat)
what: A scheduled-task mode in which an automation wakes up the same existing conversation thread on a schedule and continues it with the prior context preserved, rather than starting a fresh chat each run. Shipped as "heartbeat automations" / "scheduled check-ins" and later branded thread automations.
surfaces: [desktop, cloud-web]
ownership: mixed
persistence: cloud
mechanics: >
  A mode of the Scheduled-tasks feature (see codex/automations-scheduling/scheduled-tasks) rather than a
  separate subsystem; distinguished by re-entering an existing thread and carrying accumulated
  conversation context forward on each scheduled run. Documented under the automations page anchor
  "schedule work from a task." Well suited to ongoing/stateful workflows such as monitoring a deployment
  or polling a connected service on a recurring cadence. Inherits the parent feature's scheduling
  (intervals / daily-weekly / RRULE), environment choice (local vs worktree), unattended-run policy, and
  the "Scheduled" inbox surfacing. Recorded separately here because it is an independently meaningful
  behavior (stateful, context-preserving recurrence) that a team could rely on or ignore distinct from
  fire-and-forget standalone runs.
sources:
  - url: https://developers.openai.com/codex/changelog
    retrieved: 2026-08-02
    note: >
      Changelog (via redirect to learn.chatgpt.com/docs/changelog, fetched this run). 2026-05-14
      "[Thread automations] can wake up the same thread on a schedule while preserving the conversation
      context"; 2026-04-16 introduced "Thread automations for scheduled check-ins"; 2026-04-01 references
      "heartbeat automations."
  - url: https://developers.openai.com/codex/automations
    retrieved: 2026-08-02
    note: >
      Automations doc (via redirect to learn.chatgpt.com/docs/automations, fetched this run). Describes
      chat-embedded tasks that "return to existing chats" using accumulated context, and the
      "schedule-work-from-a-task" flow.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: The engine's Codex Automations are standalone-per-run (each fire is a fresh scope-locked build advance reading state from git + the build Issue, per routine-entry.md; the self-review is likewise fire-and-forget). The context-preserving thread/heartbeat mode is not used — the engine deliberately reconstructs state from git each run rather than relying on a persisted thread.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — The engine reconstructs state from git each run; context-preserving threads are deliberately not relied on.


## Family: cli

### `codex/cli/config-toml`

```yaml
id: codex/cli/config-toml
provider: codex
family: cli
name: config.toml configuration and load hierarchy
what: Codex reads a TOML configuration file that sets model, provider, history, and session behavior. Team/shared configuration is layered from multiple `.codex/` locations from the working directory up through user and system scopes.
surfaces: [cli]
ownership: mixed
persistence: local-config
mechanics: >
  User config lives at `$CODEX_HOME/config.toml` (default `~/.codex/config.toml`). The team-config
  load hierarchy (changelog 2026-01-23) resolves `.codex/` folders in order: current directory →
  parent folders → repo root → user (`~/.codex/`) → system (`/etc/codex/`), each contributing
  `config.toml` defaults (and a `rules/` directory for command controls, which sits at the
  approvals/rules seam). Top-level keys in scope for this family include `model`, `model_provider`
  (default `openai`), `model_context_window`, `model_catalog_json` (path to a JSON model catalog
  loaded at startup), `model_instructions_file`, `service_tier` (e.g. `fast`),
  `model_auto_compact_token_limit` and `model_auto_compact_token_limit_scope`
  (`total | body_after_prefix`), and the `history.*` keys. `codex exec --ignore-user-config`
  skips loading `$CODEX_HOME/config.toml`. Approval/sandbox keys (`approval_policy`,
  `sandbox_mode`, `sandbox_workspace_write.*`, `default_permissions`, `permissions.<name>`) and
  MCP keys (`mcp_servers.<id>.*`) appear in the same file but belong to other families.
sources:
  - url: https://developers.openai.com/codex/config-file/config-reference
    retrieved: 2026-08-02
    note: Config key catalog including model/provider/history/service_tier/auto-compact keys and the approvals/MCP seams.
  - url: https://developers.openai.com/codex/changelog
    retrieved: 2026-08-02
    note: 2026-01-23 team-config load hierarchy across .codex/ folders from cwd to /etc/codex/.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: Actively used. /Users/shanekidd/Developer/engine-template/.codex/config.toml holds two engine-managed MCP server blocks (engine-knowledge-graph, engine-memory) with `default_tools_approval_mode="auto"`, inside BEGIN/END managed-block markers, generated by /Users/shanekidd/Developer/engine-template/.engine/tools/codex_gen.py.
overlap: This is the Codex counterpart of the Claude `.mcp.json`; both register the same two in-repo MCP servers. Parity enforced by provider_parity_check.py.
note: Only MCP-server and approval-mode keys are set; model/history/profile keys are not managed by the engine.
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `CORE`** — Carries the engine-managed MCP blocks; the load hierarchy is host behavior the engine works within.

### `codex/cli/context-auto-compaction`

```yaml
id: codex/cli/context-auto-compaction
provider: codex
family: cli
name: Context auto-compaction
what: Codex can automatically compact conversation history once it crosses a token threshold, and the same summarization can be triggered manually to free tokens.
surfaces: [cli]
ownership: user
persistence: local-config
mechanics: >
  `model_auto_compact_token_limit` sets the token threshold that triggers automatic history
  compaction; `model_auto_compact_token_limit_scope` (`total | body_after_prefix`) controls what
  the threshold is measured against. The `/compact` slash command summarizes the visible chat on
  demand.
sources:
  - url: https://developers.openai.com/codex/config-file/config-reference
    retrieved: 2026-08-02
    note: model_auto_compact_token_limit and its scope key.
  - url: https://developers.openai.com/codex/cli/slash-commands
    retrieved: 2026-08-02
    note: `/compact` summarizes visible chat to free tokens.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: The Claude-side PreCompact hook (memory/compact.py) covers compaction-time memory handling, but no Codex equivalent is wired — .codex/config.toml sets no model_auto_compact_token_limit keys, and Codex has no PreCompact hook in .codex/hooks.json.
note: Compaction-triggered memory capture is a Claude-only path today; the Codex side is a parity gap, not a configured feature.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — TUI conveniences (threads, profiles, images, web search, completions, auto-compaction keys) with no engine role; auto-compaction absence on Codex is a recorded parity gap.

### `codex/cli/exec-noninteractive`

```yaml
id: codex/cli/exec-noninteractive
provider: codex
family: cli
name: Non-interactive mode (codex exec)
what: `codex exec` runs Codex from scripts and CI pipelines without opening the TUI. It streams progress to stderr and prints only the final agent message to stdout, so results pipe cleanly into other tools.
surfaces: [cli, github-ci]
ownership: user
persistence: none
mechanics: >
  Invoked as `codex exec "<prompt>"`. Input modes: prompt-as-argument; prompt-plus-stdin
  (e.g. `npm test 2>&1 | codex exec "summarize failing tests"`); or stdin-as-prompt via the
  `-` sentinel (`cat prompt.txt | codex exec -`). Flags include `--json` (JSON Lines stream on
  stdout with event types like `thread.started`, `turn.completed`, `item.*`, `error`),
  `-o`/`--output-last-message <path>`, `--output-schema <path>` (structured JSON per a schema),
  `--ephemeral` (do not persist rollout files), `--ignore-user-config`, `--ignore-rules`, and
  `--skip-git-repo-check`. Reuses saved CLI auth by default; a single run can override with an
  inline `CODEX_API_KEY=<api-key>`. The `--sandbox <level>` flag exists here but belongs to the
  approvals/sandbox seam.
sources:
  - url: https://developers.openai.com/codex/noninteractive
    retrieved: 2026-08-02
    note: Full exec syntax, flags, input modes, stdout/stderr handling, JSON event types, and CODEX_API_KEY auth override.
  - url: https://developers.openai.com/codex/cli
    retrieved: 2026-08-02
    note: Confirms exec as the automation/CI path distinct from the interactive TUI.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused (not scripted in-repo)
overlap: The Claude-side headless equivalent (`claude -p`) IS used in audit-prep.yml, but there is no `codex exec` invocation in any workflow or tool.
note: Codex is driven interactively / via Automations, not via scripted `codex exec`. The Codex headless CI path is not wired.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `OPTIONAL INTEGRATION`** — Unused; the Codex headless CI path would be the analog of the Claude -p audit if ever wanted.

### `codex/cli/image-input`

```yaml
id: codex/cli/image-input
provider: codex
family: cli
name: Image input
what: Screenshots, diagrams, or design mockups can be supplied as additional context to a Codex prompt.
surfaces: [cli]
ownership: user
persistence: session
mechanics: Pass an image with the `codex --image` flag, or paste an image into the composer, to include it as context for the current prompt.
sources:
  - url: https://developers.openai.com/codex/cli
    retrieved: 2026-08-02
    note: Documents `--image` and paste-into-composer image input.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — TUI conveniences (threads, profiles, images, web search, completions, auto-compaction keys) with no engine role; auto-compaction absence on Codex is a recorded parity gap.

### `codex/cli/interactive-tui`

```yaml
id: codex/cli/interactive-tui
provider: codex
family: cli
name: Interactive terminal session (TUI)
what: The primary interactive mode, launched with the bare `codex` command, opens a terminal UI where you prompt Codex in natural language to explore code, edit files, and run commands in the current project directory. It shows the active model, working directory, remaining context percentage, and a keyboard-shortcut helper via `?`.
surfaces: [cli]
ownership: vendor-managed
persistence: session
mechanics: Invoked as `codex` inside a project (a Git repository is expected). The composer accepts natural-language prompts and `/` slash commands; proposed file edits are shown as diffs before being written to the working tree; shell commands run against the local environment with output shown inline. Longer prompts can be opened in an external editor via the `VISUAL`/`EDITOR` environment variables. Approval/permission gating of edits and commands is a separate capability (seam noted below).
sources:
  - url: https://developers.openai.com/codex/cli
    retrieved: 2026-08-02
    note: Documents the interactive TUI, diff-before-apply editing, local command execution, context-left indicator, and `?` shortcut helper.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: The engine supports Codex as a co-equal runtime and operates inside its TUI. The Codex instruction surface is /Users/shanekidd/Developer/engine-template/AGENTS.md (engine-managed-block structure), the analog of CLAUDE.md. Operator commands are exposed as `$`-prefixed skills (/Users/shanekidd/Developer/engine-template/.agents/skills/, 13 mirrors) rendered by engine-help.
overlap: AGENTS.md floor block; provider-parity tooling (providers.py, provider_parity_check.py) keeps the Claude and Codex surfaces in lockstep.
note: No TUI sub-feature is configured; the engine only relies on the session existing and firing lifecycle hooks.
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `CORE`** — Codex is a co-equal runtime; the engine operates inside its TUI via AGENTS.md + $-skills.

### `codex/cli/model-selection`

```yaml
id: codex/cli/model-selection
provider: codex
family: cli
name: Model selection
what: Users choose the inference model at launch, mid-session, or via config, and can point Codex at a custom model catalog or provider.
surfaces: [cli]
ownership: user
persistence: local-config
mechanics: >
  Set the model with the `model` config key (e.g. `gpt-5.5`), the `--model <name>` launch flag
  (e.g. `codex --model gpt-5.4`, `codex --model gpt-5.4-mini`), or the `/model` slash command
  during a session. `model_provider` selects a provider id (default `openai`) from
  `model_providers`; `model_catalog_json` points to an optional JSON model catalog loaded at
  startup; `model_context_window` overrides the active model's token window. For ChatGPT sign-in
  the CLI has defaulted to `gpt-5.2-codex` (changelog 2025-12-18), overridable via config.
sources:
  - url: https://developers.openai.com/codex/changelog
    retrieved: 2026-08-02
    note: Model launch/switch entries — `codex --model gpt-5.4`, gpt-5.4-mini, gpt-5.3-codex-spark, /model in-session; gpt-5.2-codex default for ChatGPT sign-in.
  - url: https://developers.openai.com/codex/config-file/config-reference
    retrieved: 2026-08-02
    note: `model`, `model_provider`, `model_catalog_json`, `model_context_window` keys.
  - url: https://developers.openai.com/codex/cli/slash-commands
    retrieved: 2026-08-02
    note: `/model` chooses the active model (and reasoning effort where available).
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: Deliberately NOT set per-agent. /Users/shanekidd/Developer/engine-template/.engine/tools/codex_gen.py (lines 21-22) states `model` is NEVER emitted into `.codex/agents/*.toml` because "a pinned model id in a persona file rots — eADR-0034"; the generated toml keys are name/description/sandbox_mode/model_reasoning_effort/developer_instructions only (confirmed on engine-audit.toml).
overlap: model-bindings.json governs the Claude side; on Codex only the effort half is mapped (see reasoning-effort).
note: Asymmetry — Claude agents get a bound `model`, Codex agents get none (model left to the Codex session default). An intentional divergence, recorded in codex_gen.py.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Deliberately never emitted on Codex (a pinned id rots — eADR-0034); model is the session's choice.

### `codex/cli/profiles`

```yaml
id: codex/cli/profiles
provider: codex
family: cli
name: Configuration profiles
what: Named configuration layers that can be selected per run, letting a user switch between saved sets of config overrides from the command line.
surfaces: [cli]
ownership: user
persistence: local-config
mechanics: >
  Selected with `--profile <profile-name>` (or the `profile` config key). Codex loads
  `~/.codex/config.toml` and then overlays `$CODEX_HOME/<profile-name>.config.toml`
  (i.e. `~/.codex/<profile-name>.config.toml`), one TOML file per profile. Profile names may
  contain letters, numbers, hyphens, and underscores.
sources:
  - url: https://developers.openai.com/codex/config-file/config-reference
    retrieved: 2026-08-02
    note: `profile` key and `$CODEX_HOME/profile-name.config.toml` overlay location.
  - url: https://developers.openai.com/codex/cli
    retrieved: 2026-08-02
    note: Corroborates --profile as the CLI selector overlaying user config with the named profile file.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: No `--profile` or `<name>.config.toml` overlay usage.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — TUI conveniences (threads, profiles, images, web search, completions, auto-compaction keys) with no engine role; auto-compaction absence on Codex is a recorded parity gap.

### `codex/cli/reasoning-effort`

```yaml
id: codex/cli/reasoning-effort
provider: codex
family: cli
name: Reasoning effort and output verbosity
what: For supported models, Codex exposes a tunable reasoning-effort level, a reasoning-summary detail level, and an optional output verbosity, adjustable in config or during a session.
surfaces: [cli]
ownership: user
persistence: local-config
mechanics: >
  `model_reasoning_effort` accepts `minimal | low | medium | high | xhigh`;
  `plan_mode_reasoning_effort` adds `none` for a plan-mode-specific override
  (`none | minimal | low | medium | high | xhigh`). `model_reasoning_summary`
  (`auto | concise | detailed | none`) sets summary detail, `model_supports_reasoning_summaries`
  (boolean) forces whether reasoning metadata is sent, and `model_verbosity`
  (`low | medium | high`) is an optional GPT-5 Responses-API verbosity override. The `/model`
  slash command lets you adjust reasoning effort in-session when the model supports it.
sources:
  - url: https://developers.openai.com/codex/config-file/config-reference
    retrieved: 2026-08-02
    note: model_reasoning_effort, plan_mode_reasoning_effort, model_reasoning_summary, model_supports_reasoning_summaries, model_verbosity and their allowed values.
  - url: https://developers.openai.com/codex/cli/slash-commands
    retrieved: 2026-08-02
    note: `/model` adjusts reasoning effort when available.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: Used. /Users/shanekidd/Developer/engine-template/.codex/agents/*.toml carry `model_reasoning_effort`, mapped from the Claude tier by /Users/shanekidd/Developer/engine-template/.engine/tools/codex_gen.py (_EFFORT_BY_TIER: judgment→high, mechanical→low, line 53).
overlap: Same model-bindings.json tier vocabulary as the Claude effort stamp; codex_gen.py is the Codex stamper.
note: model_reasoning_summary, model_verbosity, plan_mode_reasoning_effort are not used. `xhigh`/`minimal` levels are not exercised (only high/low).
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `CORE`** — model_reasoning_effort is the one model-adjacent knob the engine stamps on Codex.

### `codex/cli/session-resume-persistence`

```yaml
id: codex/cli/session-resume-persistence
provider: codex
family: cli
name: Session resume and transcript persistence
what: Codex persists session transcripts (rollout files) so a prior conversation can be reloaded with its history intact, both interactively and in exec mode. A separate input-history file records prompts entered at the composer.
surfaces: [cli, github-ci]
ownership: user
persistence: local-config
mechanics: >
  Interactive: `codex resume` reopens a session; `/resume` opens a saved-session picker and
  reloads the chosen conversation's transcript. Non-interactive: `codex exec resume --last
  "<next instruction>"` continues the most recent session from the current directory, or
  `codex exec resume <SESSION_ID> "<instruction>"` targets a specific session. Rollout
  persistence can be suppressed per run with `codex exec --ephemeral`. Config keys:
  `history.persistence` (`save-all | none`) controls whether the composer input history is
  written to `history.jsonl`, and `history.max_bytes` caps that file's size by dropping oldest
  entries.
sources:
  - url: https://developers.openai.com/codex/noninteractive
    retrieved: 2026-08-02
    note: Documents `codex exec resume`, `--last`, `<SESSION_ID>`, and `--ephemeral` rollout suppression.
  - url: https://developers.openai.com/codex/cli/slash-commands
    retrieved: 2026-08-02
    note: Documents `/resume` reloading a selected saved session with original history intact.
  - url: https://developers.openai.com/codex/config-file/config-reference
    retrieved: 2026-08-02
    note: history.persistence (save-all|none) and history.max_bytes for history.jsonl.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: The engine reads Codex session transcripts (rollout files) for memory capture. /Users/shanekidd/Developer/engine-template/.engine/tools/memory/capture.py carries a dedicated, provider-routed Codex transcript recognizer (`_codex_messages`, `_extract_records`, the known-`type` allow-set around lines 207-300) that fails CLOSED on an unrecognized shape rather than using the tolerant Claude parser, because "Codex's transcript format is EXPLICITLY unstable."
overlap: capture.py is the single memory-capture mechanism for both providers; the Codex path is a distinct recognizer within it.
note: The engine depends on the Codex rollout/transcript format and treats format drift as a defect (codex-validation.md item 7: a "conversation wasn't saved" line means "the transcript reader needs updating"). `codex resume`/`codex exec resume` and history.persistence config are not otherwise used.
```

**Assessment:** action mode observes; enforcement none — signal only; failure mode n/a; portability provider-specific.

**Disposition: `CORE`** — Memory capture reads Codex rollouts through a fail-closed recognizer because the format is explicitly unstable — a load-bearing, version-sensitive dependence.

### `codex/cli/shell-integration`

```yaml
id: codex/cli/shell-integration
provider: codex
family: cli
name: Shell completions and editor integration
what: Codex generates shell completion scripts and integrates with an external editor for composing longer prompts.
surfaces: [cli]
ownership: user
persistence: local-config
mechanics: `codex completion` generates shell completion scripts for terminal integration; the `VISUAL`/`EDITOR` environment variables select the editor Codex opens for longer prompt composition.
sources:
  - url: https://developers.openai.com/codex/cli
    retrieved: 2026-08-02
    note: Documents `codex completion` and VISUAL/EDITOR editor integration.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: No `codex completion` or VISUAL/EDITOR reliance.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — TUI conveniences (threads, profiles, images, web search, completions, auto-compaction keys) with no engine role; auto-compaction absence on Codex is a recorded parity gap.

### `codex/cli/slash-commands`

```yaml
id: codex/cli/slash-commands
provider: codex
family: cli
name: Developer (slash) commands
what: A keyboard-first command system inside the TUI. Typing `/` in the composer opens a popup of built-in commands that perform in-session actions without leaving the terminal.
surfaces: [cli]
ownership: vendor-managed
persistence: session
mechanics: >
  Built-in commands documented include `/model`, `/fast`, `/personality`, `/status`, `/init`
  (scaffold an `AGENTS.md`), `/clear` (clear terminal, fresh chat), `/compact` (summarize visible
  chat to free tokens), `/diff` (show Git diff including untracked files), `/mention` (attach
  files/folders to the next prompt), `/copy` (copy latest output), `/review`, `/rename`,
  `/archive`, `/delete`, `/resume`, `/new`, `/fork`, `/side`, `/vim`, `/keymap` (persistent
  keybinding remap), `/experimental`, `/feedback`, `/exit` (alias `/quit`), and `/logout`.
  Several listed commands are the entry points to capabilities owned by other families and are
  noted at the seam below: `/permissions`, `/approve` (approvals/sandbox); `/mcp` (MCP);
  `/agent`, `/subagents`; `/memories`; `/plugins`; `/apps`; `/hooks`; and Windows sandbox helpers
  `/setup-default-sandbox`, `/sandbox-add-read-dir`. User-defined/custom slash commands were not
  documented on the fetched pages (recorded as a gap).
sources:
  - url: https://developers.openai.com/codex/cli/slash-commands
    retrieved: 2026-08-02
    note: Enumerates the built-in slash command set and each command's action.
  - url: https://developers.openai.com/codex/cli
    retrieved: 2026-08-02
    note: Confirms the `/`-triggered command popup as a core TUI interaction.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: The engine ships its own operator commands as Codex/AGENTS skills under /Users/shanekidd/Developer/engine-template/.agents/skills/ (13 mirrors of the Claude skills), invoked as `$engine-*`. codex-validation.md item 9 verifies engine-help renders the `$` prefix. Native built-in slash commands are consumed as-is but not extended.
overlap: skill mirroring is kept coherent by /Users/shanekidd/Developer/engine-template/.engine/tools/codex_skill_coherence_check.py.
note: The `$` vs `/` prefix difference between runtimes is an engine-tracked parity concern.
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `ADAPTER`** — $-prefixed skills are the Codex face of the engine's command surface; parity-tracked.

### `codex/cli/thread-management`

```yaml
id: codex/cli/thread-management
provider: codex
family: cli
name: Multi-thread session management (new / fork / side)
what: The TUI supports naming, pinning, and running multiple side-by-side conversation threads, forking a thread's history (including temporary forks that stay out of listings), and switching between side conversations without closing them.
surfaces: [cli]
ownership: user
persistence: local-config
mechanics: >
  `/new` starts a fresh named chat and `/clear` starts a fresh chat clearing the terminal;
  `/rename` names a saved session; `/fork` branches the current session preserving its transcript
  (with paginated history and temporary forks that don't appear in listings); `/side` opens a
  parallel/side thread; `/delete` permanently removes the current session and exits; `/archive`
  archives the current session and exits. Introduced in the 0.146.0 release (July 29, 2026) as
  session naming, thread pinning, forking, and side-conversation switching.
sources:
  - url: https://github.com/openai/codex/releases
    retrieved: 2026-08-02
    note: Release 0.146.0 (2026-07-29) — session naming via /new and /clear, thread pinning, forking with paginated/temporary history, side-conversation switching.
  - url: https://developers.openai.com/codex/cli/slash-commands
    retrieved: 2026-08-02
    note: /new, /fork, /side, /rename, /archive, /delete command semantics.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: `/new`, `/fork`, `/side` thread machinery is not relied on.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — TUI conveniences (threads, profiles, images, web search, completions, auto-compaction keys) with no engine role; auto-compaction absence on Codex is a recorded parity gap.

### `codex/cli/web-search`

```yaml
id: codex/cli/web-search
provider: codex
family: cli
name: Web search
what: Codex can perform live web searches during a session to pull in current information, with the search activity shown in the transcript.
surfaces: [cli]
ownership: user
persistence: session
mechanics: Enabled with the `codex --search` flag; search activity is visible inline in the session transcript.
sources:
  - url: https://developers.openai.com/codex/cli
    retrieved: 2026-08-02
    note: Documents the `--search` flag and transcript-visible search activity.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: `codex --search` is not enabled; the audit/build workflows are explicitly no-web-access.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — TUI conveniences (threads, profiles, images, web search, completions, auto-compaction keys) with no engine role; auto-compaction absence on Codex is a recorded parity gap.


## Family: cloud-web

### `codex/cloud-web/agent-internet-access`

```yaml
id: codex/cloud-web/agent-internet-access
provider: codex
family: codex / cloud-web
name: Cloud agent internet access
what: Per-environment control of the cloud agent's internet access during the agent phase, with three states — Off, On (unrestricted), or On with restrictions (domain allowlist plus optional HTTP-method limits). Setup scripts always retain internet regardless of the agent setting.
surfaces: [cloud-web]
ownership: project
persistence: cloud
mechanics: States Off / On / On with restrictions. Allowlist presets: "None" (specify domains manually), "Common dependencies" (~80 package/registry/source-control domains), "All (unrestricted)". Optional restriction to GET/HEAD/OPTIONS (blocking POST/PUT/PATCH/DELETE). All traffic routes through an HTTP/HTTPS proxy. Docs enumerate security risks (prompt injection, code/secret exfiltration, malware, license contamination). Setting is environment-specific and persists until changed.
sources:
  - url: https://learn.chatgpt.com/docs/cloud/internet-access
    retrieved: 2026-08-02
    note: Off/On/restricted states, allowlist presets, HTTP-method limits, proxy, risk list.
  - url: https://learn.chatgpt.com/docs/environments/cloud-environment
    retrieved: 2026-08-02
    note: Confirms setup phase keeps internet while agent phase defaults off.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  No Codex-cloud internet-access states. Network posture in the engine is workflow `permissions:` least-privilege
  and Codex sandbox_mode (`.codex/agents/*.toml`, e.g. read-only) — not the cloud agent Off/On/restricted
  allowlist.
note: none
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Codex cloud environments/tasks are unused; local worktrees + scheduled Automations are the engine's story.

### `codex/cloud-web/cloud-tasks`

```yaml
id: codex/cloud-web/cloud-tasks
provider: codex
family: codex / cloud-web
name: Codex cloud tasks (web surface)
what: Codex cloud runs delegated coding tasks in isolated cloud environments from the web surface at chatgpt.com/codex. Each task runs in the background — many can run in parallel — produces a summary and diff you review, accepts follow-ups, and can open a pull request when ready. The web surface organizes work into Chats, Code reviews, and Archive sections.
surfaces: [cloud-web]
ownership: mixed
persistence: cloud
mechanics: Submit a prompt against a chosen repository/environment; "watch the task logs or let the task run in the background"; then "Inspect the summary and diff, request a follow-up, or open a pull request." History persists under Chats / Code reviews / Archive.
sources:
  - url: https://developers.openai.com/codex/cloud
    retrieved: 2026-08-02
    note: Parallel isolated environments, background execution, diff review, follow-up, PR creation, and the Chats/Code reviews/Archive sections.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: No chatgpt.com/codex cloud-task usage; not repo-configurable and not referenced.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Codex cloud environments/tasks are unused; local worktrees + scheduled Automations are the engine's story.

### `codex/cloud-web/env-vars-and-secrets`

```yaml
id: codex/cloud-web/env-vars-and-secrets
provider: codex
family: codex / cloud-web
name: Environment variables and secrets
what: Cloud environments support environment variables that persist across both the setup and agent phases, and secrets that receive an additional encryption layer and are available only to setup scripts (removed before the agent phase).
surfaces: [cloud-web]
ownership: project
persistence: cloud
mechanics: Env vars persist for the full chat including setup and agent phases (added via ~/.bashrc or environment settings). Secrets are "only available to setup scripts" and stripped before the agent runs; changing either invalidates the container cache.
sources:
  - url: https://learn.chatgpt.com/docs/environments/cloud-environment
    retrieved: 2026-08-02
    note: Distinguishes env-var persistence from setup-only, extra-encrypted secrets.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  Codex-cloud env-var/secret model is web-config; the engine's CI secrets are GitHub Actions secrets
  (CLAUDE_CODE_OAUTH_TOKEN, MEMORY_VAULT_TOKEN, GITHUB_TOKEN in audit-prep.yml), a different surface.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Codex cloud environments/tasks are unused; local worktrees + scheduled Automations are the engine's story.

### `codex/cloud-web/environments-universal-image`

```yaml
id: codex/cloud-web/environments-universal-image
provider: codex
family: codex / cloud-web
name: Cloud environments and universal image
what: Each cloud task runs in a container. The default image is `universal`, pre-installed with common languages, packages, and tools; each repository's environment can pin package versions, run a setup script at container creation, and run an optional maintenance script when resuming a cached container.
surfaces: [cloud-web]
ownership: project
persistence: cloud
mechanics: `universal` image has a reference Dockerfile at the openai/codex-universal GitHub repo for local testing. "Set package versions" pins Python/Node/etc. Setup scripts run at container creation in a separate Bash session with internet enabled (so `export` does not persist to the agent phase); maintenance scripts run on resume of a cached container. Package managers (npm/yarn/pnpm/pip/pipenv/poetry) auto-install; AGENTS.md is discovered for project commands. Container cache lasts up to 12 hours and invalidates when setup/maintenance scripts, env vars, or secrets change; a "Reset cache" option exists per environment; Business/Enterprise caches are workspace-shared. Configured at chatgpt.com/codex/settings/environments.
sources:
  - url: https://learn.chatgpt.com/docs/environments/cloud-environment
    retrieved: 2026-08-02
    note: universal image, package versions, setup/maintenance scripts, auto package-manager install, caching (12h, reset, workspace-shared), AGENTS.md.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  No Codex cloud environment / universal image / setup+maintenance scripts. The engine's runtime is the local
  uv-materialized `.engine` tool-runtime, and AGENTS.md is the engine floor (not a Codex-cloud project-commands
  source).
note: none
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Codex cloud environments/tasks are unused; local worktrees + scheduled Automations are the engine's story.

### `codex/cloud-web/task-start-integrations`

```yaml
id: codex/cloud-web/task-start-integrations
provider: codex
family: codex / cloud-web
name: Start and review cloud tasks from other surfaces
what: Codex cloud tasks can be started or reviewed from surfaces beyond the web app — GitHub pull requests and issues, Linear issues, and Slack — and tasks can be begun or reviewed from the browser or the Codex CLI when away from the development machine.
surfaces: [cloud-web, github-ci, cli]
ownership: mixed
persistence: cloud
mechanics: "Start work from GitHub pull requests, Linear issues, and Slack" without context switching; "Begin and review tasks from the browser or Codex CLI." Per-integration detail lives on third-party/github, third-party/linear, third-party/slack pages (not fetched this run).
sources:
  - url: https://developers.openai.com/codex/cloud
    retrieved: 2026-08-02
    note: Names GitHub/Linear/Slack start points and browser/CLI access to cloud tasks.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: No GitHub/Linear/Slack Codex-cloud task-start integrations referenced or configured.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Codex cloud environments/tasks are unused; local worktrees + scheduled Automations are the engine's story.


## Family: github-ci

### `codex/github-ci/automatic-pr-review`

```yaml
id: codex/github-ci/automatic-pr-review
provider: codex
family: github-ci
name: "Automatic reviews"
what: >
  When enabled, Codex posts a review automatically whenever someone opens a new pull
  request, without any @codex mention. Documented as reviewing "every pull request
  automatically" once Codex is added to the GitHub organization or repository.
surfaces: [github-ci, cloud-web]
ownership: org
persistence: cloud
mechanics: >
  Opt-in toggle labeled "Automatic reviews" in Codex settings, alongside the per-repo
  "Code review" toggle. Once on, Codex posts a review on each newly opened PR. Setting
  is administered in Codex settings (web) and scoped to the repository/organization.
sources:
  - url: https://developers.openai.com/codex/integrations/github
    retrieved: 2026-08-02
    note: >
      Documents the "Automatic reviews" toggle and the "post a review whenever someone
      opens a new PR" behavior. Redirects to learn.chatgpt.com/docs/third-party/github.
  - url: https://developers.openai.com/codex/use-cases/github-code-reviews
    retrieved: 2026-08-02
    note: >
      Corroborates that Codex "can automatically review every pull request" once added
      to a GitHub org/repository. Redirects to learn.chatgpt.com/use-cases/github-code-reviews.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  Codex automatic PR review is a Codex-cloud web-settings toggle, so it cannot be enabled from repo files, and
  it is not referenced. The engine's own PR-review coverage is the persona pre-submission gate
  (build-orchestration.md + .claude/agents/engine-qa-review-*.md and their .codex/agents/*.toml renders).
note: none
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `OPTIONAL INTEGRATION`** — Codex review-on-GitHub (mentions, automatic review, repo setup) is the Codex face of the native-review pairing — same complementary logic, org-enabled, never a gate.

### `codex/github-ci/codex-mentions`

```yaml
id: codex/github-ci/codex-mentions
provider: codex
family: github-ci
name: "@codex mentions in pull requests"
what: >
  Mentioning @codex in a GitHub pull request comment triggers Codex. "@codex review"
  requests a code review; "@codex fix ..." (e.g. "@codex fix the P1 issue",
  "@codex fix the CI failures") has Codex address flagged problems. Mentioning @codex
  with anything other than "review" starts a Codex cloud chat that uses the pull
  request as context.
surfaces: [github-ci]
ownership: user
persistence: none
mechanics: >
  Comment-triggered on a PR. "@codex review" invokes the reviewer; "@codex fix <target>"
  drives a fix/update pass on prior findings; any non-"review" mention opens a cloud
  chat scoped to the PR for arbitrary tasks. Requires Codex cloud set up for the repo
  and code review turned on (see repo-setup record). Each mention is an ad-hoc trigger;
  resulting work runs in Codex cloud.
sources:
  - url: https://developers.openai.com/codex/integrations/github
    retrieved: 2026-08-02
    note: >
      Documents the @codex command set (review, fix, general cloud chat) and the
      "anything other than review -> cloud chat with the PR as context" behavior.
      Page 308-redirects to learn.chatgpt.com/docs/third-party/github (the content read).
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  No `@codex` mention appears anywhere in the repo (grep across .github/.engine/.codex is empty). No PR/issue
  automation triggers Codex. Codex presence in-repo is limited to the engine's OWN Codex adapter surfaces
  (.codex/agents/*.toml generated by .engine/tools/codex_gen.py, .codex/config.toml, .codex/hooks.json), which
  are the engine's subagent/hook mirrors, not Codex cloud PR triggers.
note: none
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `OPTIONAL INTEGRATION`** — Codex review-on-GitHub (mentions, automatic review, repo setup) is the Codex face of the native-review pairing — same complementary logic, org-enabled, never a gate.

### `codex/github-ci/repo-setup`

```yaml
id: codex/github-ci/repo-setup
provider: codex
family: github-ci
name: "Codex code review setup for a repository"
what: >
  Enabling Codex code review on a GitHub repository requires Codex cloud set up for that
  repository and access to Codex code review settings, then turning on "Code review" for
  the repository in Codex settings. An AGENTS.md file is an optional prerequisite for
  repository-specific review guidance.
surfaces: [github-ci, cloud-web]
ownership: org
persistence: cloud
mechanics: >
  Prerequisites stated: (1) Codex cloud set up for the target repository, (2) access to
  Codex code review settings, (3) optional AGENTS.md for repo-specific rules. Setup steps:
  set up Codex cloud, open Codex settings, turn on "Code review" for the repository.
  Automatic reviews is a further opt-in on top of this. Configuration lives in the Codex
  settings (web), scoped per repository.
sources:
  - url: https://developers.openai.com/codex/integrations/github
    retrieved: 2026-08-02
    note: >
      Documents prerequisites and the "Turn on Code review for your repository" setup
      step. Redirects to learn.chatgpt.com/docs/third-party/github.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  AGENTS.md exists at repo root but is the engine's managed instruction floor (BEGIN engine-managed block:
  floor), the Codex analog of CLAUDE.md — it contains NO "## Code Review Rules" section and is not used to
  configure Codex code review. No Codex-cloud "Code review" enablement is present (it is web-config, not
  in-repo).
note: none
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `OPTIONAL INTEGRATION`** — Codex review-on-GitHub (mentions, automatic review, repo setup) is the Codex face of the native-review pairing — same complementary logic, org-enabled, never a gate.


## Family: handoff

### `codex/handoff/ide-to-cloud-delegation`

```yaml
id: codex/handoff/ide-to-cloud-delegation
provider: codex
family: codex / handoff
name: Delegate a task from the IDE to Codex cloud
what: From the IDE extension you delegate a longer task to Codex cloud without leaving the editor — keeping quick iterations local, or connecting Codex web when a task needs more time and room — then review cloud results, ask follow-ups, and apply the resulting diffs locally. Chat/conversation context carries across the local-to-cloud boundary.
surfaces: [ide, cloud-web]
ownership: user
persistence: cloud
mechanics: "Keep quick iterations local, or connect Codex web when a task needs more time and room." The editor exposes the cloud run option; results return to the IDE for review and local application of diffs.
sources:
  - url: https://developers.openai.com/codex/ide
    retrieved: 2026-08-02
    note: Documents delegating longer tasks from the IDE to Codex web and returning to review in-editor.
  - url: https://developers.openai.com/codex/cloud
    retrieved: 2026-08-02
    note: Confirms cloud tasks are reviewable from the IDE/CLI/browser surfaces.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: No IDE→Codex-cloud delegation; the engine has no Codex cloud usage at all.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Host/thread handoff surfaces unused; state travels through git, not threads.

### `codex/handoff/local-worktree-handoff`

```yaml
id: codex/handoff/local-worktree-handoff
provider: codex
family: codex / handoff
name: Handoff between Local and a Git worktree
what: Within the Codex desktop app, move a chat between your Local checkout and a Git worktree, so background or parallel chats run in isolated worktrees without disturbing your current setup and can be pulled back to Local for inspection, testing, or collaboration. Explicitly available only in Codex in the ChatGPT desktop app — not the CLI, IDE extensions, or cloud.
surfaces: [desktop]
ownership: user
persistence: local-config
mechanics: New chat → select "Worktree" and a starting Git branch → Codex creates a worktree in detached-HEAD state; handoff manages Git operations safely because a branch can be checked out in only one place at a time. Codex-managed worktrees are lightweight and stored in `$CODEX_HOME/worktrees` (default retention of the 15 most recent, configurable); permanent worktrees are created via the project menu. `.worktreeinclude` lists ignored files (e.g. `.env`) to copy into managed worktrees; "Create branch here" converts a detached HEAD into a named branch.
sources:
  - url: https://learn.chatgpt.com/docs/environments/git-worktrees
    retrieved: 2026-08-02
    note: Worktree/Local handoff mechanics, $CODEX_HOME/worktrees, .worktreeinclude, desktop-app-only availability.
  - url: https://developers.openai.com/codex/changelog
    retrieved: 2026-08-02
    note: Local↔Worktree handoff introduction (2026-03-17) and worktree/VS Code handoff (2026-04-01).
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  The engine has its OWN worktree-isolation model — /Users/shanekidd/Developer/engine-template/.claude/worktrees/
  (~11 live), plus the parallel-workers isolated-worktree build strategy in
  .engine/operations/build-orchestration.md step 4 — but this is plain git worktrees under Claude Code's build
  flow, NOT Codex's desktop-app Local↔Worktree handoff ($CODEX_HOME/worktrees, .worktreeinclude, detached-HEAD
  handoff).
note: >
  Hand-rolled parallel: both isolate work in worktrees, but the engine's is a git-native, PR-only build-isolation
  discipline owned by build-orchestration; Codex's is a desktop-app-managed chat-handoff feature. The engine
  neither uses nor depends on the Codex mechanism.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Host/thread handoff surfaces unused; state travels through git, not threads.

### `codex/handoff/remote-control-from-mobile`

```yaml
id: codex/handoff/remote-control-from-mobile
provider: codex
family: codex / handoff
name: Remote control of a host from the ChatGPT mobile app
what: The ChatGPT mobile app connects to a Mac or Windows host running the Codex desktop app to start or continue Codex work on that host, steer active work, approve commands, review output, and receive notifications when a task completes or needs attention.
surfaces: [desktop]
ownership: mixed
persistence: session
mechanics: Requires Codex access in the account/workspace, the latest mobile app with the Remote feature, and the latest desktop app running on the host (awake, online, signed in to the same account); a workspace admin may need to enable Remote Control access. "Start or continue work on a connected Mac or Windows host, review progress, and approve actions from your phone." Introduced 2026-05-14 (Mac), Windows support 2026-05-29, expanded 2026-06-25. (Note: the controlling surface is the ChatGPT mobile app, which is not one of this schema's surface enum values; the controlled host is desktop.)
sources:
  - url: https://learn.chatgpt.com/docs/remote-connections
    retrieved: 2026-08-02
    note: Mobile-to-host remote control capabilities, setup requirements, admin enablement.
  - url: https://developers.openai.com/codex/changelog
    retrieved: 2026-08-02
    note: Dated mobile remote-control entries incl. Windows support.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: No ChatGPT-mobile → desktop-host remote control usage.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Host/thread handoff surfaces unused; state travels through git, not threads.

### `codex/handoff/thread-handoff-between-hosts`

```yaml
id: codex/handoff/thread-handoff-between-hosts
provider: codex
family: codex / handoff
name: Thread handoff between local and remote hosts
what: Move a chat/thread together with its Git state between a local host and a connected remote host by handing it off to a matching project on the destination host and continuing it there; Codex can coordinate the handoff. Codex creates or reuses a worktree, transfers the chat and Git state, and switches the session to the new host.
surfaces: [desktop, cli]
ownership: user
persistence: local-config
mechanics: Prerequisites — connect the destination host and save matching projects on both hosts. In the desktop app footer, select the current run location, choose the destination host, review details, and select "Hand off." Remote hosts are connected over SSH (via ~/.ssh/config); the app starts the remote Codex app-server through SSH using the remote user's login shell. Changelog: thread handoff between local and remote hosts (2026-06-18), earlier Local↔Worktree handoff (2026-03-17) and handoff across worktrees and the VS Code extension (2026-04-01).
sources:
  - url: https://learn.chatgpt.com/docs/remote-connections
    retrieved: 2026-08-02
    note: Handoff prerequisites, footer flow, "Hand off" control, worktree + Git-state transfer, SSH host connection.
  - url: https://developers.openai.com/codex/changelog
    retrieved: 2026-08-02
    note: Dated handoff entries (local/remote, Local/Worktree, VS Code extension).
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: No SSH remote-host thread handoff; unattended work is the routine skill, not Codex host handoff.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Host/thread handoff surfaces unused; state travels through git, not threads.


## Family: ide

### `codex/ide/editor-context`

```yaml
id: codex/ide/editor-context
provider: codex
family: codex / ide
name: Prompt with editor context
what: Lets you reference an open file, a code selection, or a recent chat directly from the composer, so Codex starts from code already visible in the editor instead of re-explained context.
surfaces: [ide]
ownership: user
persistence: session
mechanics: "Add an open file, a selection, or a recent chat to the composer." Detailed on the prompting page (use-editor-context) linked from the IDE doc.
sources:
  - url: https://developers.openai.com/codex/ide
    retrieved: 2026-08-02
    note: Documents adding open file / selection / recent chat to the composer.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: IDE composer/editor-context feature; nothing in-repo configures or depends on it.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — IDE-extension surfaces are environments the engine's files load under, not surfaces it configures.

### `codex/ide/extension-core`

```yaml
id: codex/ide/extension-core
provider: codex
family: codex / ide
name: Codex IDE extension
what: Embeds the Codex coding agent as a sidebar chat inside supported editors so you run Codex next to your code. Available for Visual Studio Code, VS Code Insiders, Cursor, and Windsurf as a marketplace extension, plus native integrations for Xcode and JetBrains IDEs.
surfaces: [ide]
ownership: user
persistence: local-config
mechanics: Install from the VS Code Marketplace (Xcode/JetBrains use native integrations); sign in through the extension; open via the Codex icon or the "Codex: Open Codex Sidebar" command. The extension shares the same agent settings surface as the CLI/desktop app through config.toml.
sources:
  - url: https://developers.openai.com/codex/ide
    retrieved: 2026-08-02
    note: Names supported editors, sidebar chat, sign-in, and the open-sidebar command (fetched via 308 redirect to learn.chatgpt.com/docs/codex/ide; .md variant corroborates the editor list).
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  No IDE-extension configuration; the engine's Codex surfaces are CLI/agent-runtime (.codex/config.toml,
  .codex/agents, .codex/hooks.json), not editor-embedded. `chatgpt.*` editor keys are absent.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — IDE-extension surfaces are environments the engine's files load under, not surfaces it configures.

### `codex/ide/extension-settings`

```yaml
id: codex/ide/extension-settings
provider: codex
family: codex / ide
name: IDE extension settings and settings sync
what: The extension exposes editor-specific settings under `chatgpt.*` keys plus the shared agent settings (model/reasoning, approvals, sandbox, MCP) through config.toml, and synchronizes key settings between the Codex app and the VS Code extension.
surfaces: [ide]
ownership: mixed
persistence: local-config
mechanics: Editor keys include chatgpt.commentCodeLensEnabled, chatgpt.openOnStartup, chatgpt.followUpQueueMode, chatgpt.composerEnterBehavior, chatgpt.reviewDelivery, chatgpt.localeOverride, chatgpt.runCodexInWindowsSubsystemForLinux. Shared agent behavior (model selection, reasoning effort, approval policy, sandbox, MCP servers, branch/commit-message generation) lives in config.toml. Settings precedence: command-line flags → project config → user defaults → system → built-in. Settings sync between the Codex app and VS Code extension added 2026-03-23, with a settings entry point in the extension.
sources:
  - url: https://developers.openai.com/codex/developer-settings
    retrieved: 2026-08-02
    note: Lists the chatgpt.* keys, config.toml agent settings, and precedence order.
  - url: https://developers.openai.com/codex/changelog
    retrieved: 2026-08-02
    note: 2026-03-23 settings-sync entry.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: >
  Partially adjacent, not the IDE half. The engine uses the SHARED config.toml agent-settings surface — 
  /Users/shanekidd/Developer/engine-template/.codex/config.toml carries the engine-MCP server blocks and
  default_tools_approval_mode="auto", and per-agent model_reasoning_effort/sandbox_mode live in
  .codex/agents/*.toml (generated by .engine/tools/codex_gen.py from model-bindings). But NONE of the
  editor-specific `chatgpt.*` keys or the app↔extension settings-sync are used.
overlap: >
  The engine's model/effort/sandbox governance is its own (.engine/policies/model-bindings.json,
  .engine/tools/agent_bindings.py) stamped into the config.toml/agent files, overlapping the "shared agent
  settings via config.toml" concept but not the IDE-extension settings surface.
note: The record's `chatgpt.*` editor keys and settings precedence chain are not exercised.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — IDE-extension surfaces are environments the engine's files load under, not surfaces it configures.

### `codex/ide/in-editor-review`

```yaml
id: codex/ide/in-editor-review
provider: codex
family: codex / ide
name: In-place code review (IDE)
what: Review Codex's proposed changes inside the editor — a concise summary plus a focused diff of changed lines — and follow up in the same chat without a separate navigation pane. Also supports inspecting GitHub pull requests in the sidebar (PR Chat) and reviewing diffs across multiple repositories in a multi-folder project.
surfaces: [ide]
ownership: user
persistence: session
mechanics: "Read the summary, inspect a focused diff, and follow up in the same chat." The `chatgpt.reviewDelivery` setting selects inline vs detached review chats. PR-in-sidebar review (2026-04-23), PR Chat (2026-07-23), and multi-repo diff review (2026-07-30) added over 2026 per the changelog.
sources:
  - url: https://developers.openai.com/codex/ide
    retrieved: 2026-08-02
    note: Summary + focused diff + same-chat follow-up.
  - url: https://developers.openai.com/codex/developer-settings
    retrieved: 2026-08-02
    note: chatgpt.reviewDelivery (inline vs detached review).
  - url: https://developers.openai.com/codex/changelog
    retrieved: 2026-08-02
    note: PR sidebar review, PR Chat, and multi-repo review entries with dates.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  No chatgpt.reviewDelivery / PR-Chat / multi-repo-diff config. In-editor Codex review is not part of the
  engine's build-time persona gate.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — IDE-extension surfaces are environments the engine's files load under, not surfaces it configures.


## Family: instructions-skills

### `codex/instructions-skills/agents-md`

```yaml
id: codex/instructions-skills/agents-md
provider: codex
family: instructions-skills
name: AGENTS.md instruction files
what: Codex reads AGENTS.md Markdown files to load project- and user-level standing instructions before doing work, implementing the open AGENTS.md standard (a "README for agents"). A global file, project-root file, and nested per-directory files combine into one instruction chain, with the nearest file taking precedence and explicit user chat prompts overriding everything.
surfaces: [cli, ide, cloud-web, desktop]
ownership: mixed
persistence: committed
mechanics: >
  Codex builds an instruction chain once per run. Global scope first: in the Codex home
  directory (defaults to ~/.codex unless CODEX_HOME is set) it reads AGENTS.override.md if
  present, otherwise AGENTS.md. Project scope next: it walks from the Git root down to the
  current working directory, and in each directory checks AGENTS.override.md, then AGENTS.md,
  then any names listed in project_doc_fallback_filenames; it stops at the cwd. Files
  concatenate from root downward, closer files override earlier guidance, empty files are
  skipped, and at most one file per directory is used. config.toml keys:
  project_doc_fallback_filenames (extra filenames to try when AGENTS.md is missing) and
  project_doc_max_bytes (per-file read limit, 32 KiB default). The AGENTS.md standard itself
  is plain Markdown with no required fields, placed at repo root and optionally nested per
  package, and is stewarded by the Agentic AI Foundation under the Linux Foundation with 25+
  supporting tools.
sources:
  - url: https://learn.chatgpt.com/docs/agent-configuration/agents-md
    retrieved: 2026-08-02
    note: Codex-specific discovery/precedence, global vs project chain, override files, config keys (render host for developers.openai.com/codex/guides/agents-md)
  - url: https://learn.chatgpt.com/docs/config-file/config-reference
    retrieved: 2026-08-02
    note: exact config.toml keys project_doc_fallback_filenames and project_doc_max_bytes and their meanings
  - url: https://learn.chatgpt.com/docs/config-file/config-basic
    retrieved: 2026-08-02
    note: config lives in ~/.codex/config.toml (personal) and .codex/config.toml (project override)
  - url: https://agents.md
    retrieved: 2026-08-02
    note: the open AGENTS.md standard — nearest-file precedence, chat overrides, nesting, AAIF/Linux Foundation stewardship, 25+ tools
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: >
  Used. Root /Users/shanekidd/Developer/engine-template/AGENTS.md is the Codex instruction file,
  a keyed engine-managed floor block (BEGIN engine-managed block: floor) installed as the second
  instruction-floor half by instantiator.py (_ROOT_AGENTS_REL = "AGENTS.md", L1283). Project scope
  only; single root file (only nested AGENTS.md copies are inside .claude/worktrees/, i.e. build
  worktrees, not real nesting).
overlap: >
  Parallel to the Claude CLAUDE.md floor (see agents-md-interop). Kept coherent by tooling, not by
  a shared source. The two carry deliberately runtime-specific bodies.
note: >
  Several documented AGENTS.md mechanics are left at defaults / unused: no AGENTS.override.md and
  no .codex/AGENTS.md; config.toml sets neither project_doc_fallback_filenames nor
  project_doc_max_bytes (the only keys present are the two [mcp_servers.*] blocks), so the 32 KiB
  read limit and fallback-filename discovery run on defaults. AGENTS.md is 7.5 KB, well under the
  limit. The engine also does not exploit nesting/precedence — it relies on the single root file
  plus runtime hooks.
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `CORE`** — The Codex instruction floor, second half of the two-floor design; discovery keys left at defaults.

### `codex/instructions-skills/custom-prompts`

```yaml
id: codex/instructions-skills/custom-prompts
provider: codex
family: instructions-skills
name: Custom prompts (deprecated)
what: Custom prompts turn individual Markdown files into reusable prompts invoked as slash commands in the Codex CLI and IDE extension. They are local-only (not shared through a repository) and are now deprecated in favor of skills.
surfaces: [cli, ide, desktop]
ownership: user
persistence: local-config
mechanics: >
  Prompt files live in ~/.codex/prompts/ (e.g. ~/.codex/prompts/draftpr.md) inside the local
  Codex home, so they are not shared via the repository. Each is a Markdown file with YAML
  frontmatter fields description (shown in the command menu) and argument-hint (documents
  expected parameters). Invoked by typing /prompts:commandname. Arguments support positional
  $1–$9 and $ARGUMENTS, named uppercase placeholders (e.g. $FILE), and $$ for a literal dollar
  sign. Documentation status: "Deprecated. Use skills for reusable prompts"; guidance is to use
  skills to share a prompt or have Codex invoke it implicitly.
sources:
  - url: https://learn.chatgpt.com/docs/custom-prompts
    retrieved: 2026-08-02
    note: file location, slash-command invocation, frontmatter/argument syntax, deprecation notice pointing to skills
  - url: https://learn.chatgpt.com/docs/changelog
    retrieved: 2026-08-02
    note: 2026-01-22 entry "Custom prompts are now deprecated. Use skills for reusable instructions and workflows instead."
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  The engine uses skills for reusable prompts instead — the documented replacement. No
  .codex/prompts/ in the repo, and the personal ~/.codex/prompts/ path is out of a template's
  reach.
note: >
  Already on the non-deprecated path; nothing depends on custom prompts. No conflict.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Deprecated surface; the engine is already on skills.

### `codex/instructions-skills/skills`

```yaml
id: codex/instructions-skills/skills
provider: codex
family: instructions-skills
name: Agent skills
what: Skills package instructions plus optional scripts, references, and assets into a SKILL.md-rooted folder that Codex and ChatGPT can follow to complete a task reliably, per the open agent skills standard. They can be invoked explicitly by the user or implicitly when a task matches the skill's description.
surfaces: [cli, ide, cloud-web, desktop]
ownership: mixed
persistence: committed
mechanics: >
  A skill folder contains SKILL.md (required) with YAML frontmatter fields name and
  description, plus optional scripts/, references/, assets/, and agents/openai.yaml.
  Discovery scans, by precedence: REPO $CWD/.agents/skills, $CWD/../.agents/skills,
  $REPO_ROOT/.agents/skills; USER $HOME/.agents/skills; ADMIN /etc/codex/skills; SYSTEM
  built-in (OpenAI-bundled). Explicit invocation: ChatGPT via @ mention; Codex CLI/IDE via $
  or the /skills command. Implicit invocation activates when a task matches the description.
  Progressive disclosure loads at most 2% of the context window or 8,000 characters for the
  skills list, then the full SKILL.md on selection. config.toml controls per-skill state via
  [[skills.config]] with path and enabled=false to disable; features.skill_mcp_dependency_install
  (default true) governs prompting/installing missing MCP dependencies. Optional
  agents/openai.yaml sets display_name, short_description, icon_small, icon_large, brand_color,
  default_prompt, policy.allow_implicit_invocation (default true), and dependencies.tools for
  MCP server declarations. Distribution: package as plugins bundling multiple skills with
  optional connectors; install curated skills via the $skill-installer.
sources:
  - url: https://learn.chatgpt.com/docs/build-skills
    retrieved: 2026-08-02
    note: full skills spec — SKILL.md frontmatter, folder layout, discovery paths/scopes, invocation, config keys, openai.yaml metadata, distribution (render host for developers.openai.com/codex/skills)
  - url: https://learn.chatgpt.com/docs/config-file/config-reference
    retrieved: 2026-08-02
    note: config.toml skills.config[].path/enabled and features.skill_mcp_dependency_install
  - url: https://learn.chatgpt.com/docs/changelog
    retrieved: 2026-08-02
    note: 2025-12-19 entry announcing agent skills as reusable instruction bundles
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: >
  Used, as a generated twin of the Claude skills. 13 skills under .agents/skills/*/SKILL.md, each
  with YAML frontmatter reduced to the spec fields `name` + `description`, plus an
  agents/openai.yaml carrying policy.allow_implicit_invocation. All are GENERATED from
  .claude/skills/*/SKILL.md by .engine/tools/codex_gen.py (banner header in every file). Discovery
  path used is the repo scope $REPO_ROOT/.agents/skills. allow_implicit_invocation is set true for
  model-invocable skills (engine-recall openai.yaml) and false for operator-typed ones (engine-start
  openai.yaml), derived from the Claude source's invocation/disable-model-invocation.
overlap: >
  Duplication of the .claude/skills/ set, mechanically maintained: codex_gen.py generates the twin,
  and coherence is enforced by .engine/tools/codex_skill_coherence_check.py plus the provider
  parity tooling (provider_parity_check.py, provider_vocab_check.py, providers.py). The SKILL.md
  bodies are byte-identical to the Claude sources; only frontmatter is stripped and the openai.yaml
  is added.
note: >
  Two documented config.toml skills controls are unused: [[skills.config]] path/enabled (no skills
  block in .codex/config.toml — grep confirmed) and features.skill_mcp_dependency_install (default
  relied on). The engine uses the openai.yaml policy.allow_implicit_invocation field correctly per
  spec, but the invocation intent originates from the non-standard `invocation` key it added to the
  Claude source — so the whole invocation-control chain (Claude custom field → codex_gen →
  openai.yaml) is engine-built scaffolding over the platform's real fields.
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `CORE`** — Generated skill twins with correct allow_implicit_invocation mapping via the engine's own chain.

### `codex/instructions-skills/team-config`

```yaml
id: codex/instructions-skills/team-config
provider: codex
family: instructions-skills
name: Team Config (repository-scoped configuration)
what: Team Config groups the files a team uses to standardize Codex across repositories and machines — config.toml defaults, a rules/ directory for command controls, and a skills/ directory for reusable workflows — checked into the repository so Codex picks them up automatically. It shares defaults, rules, and skills without duplicating setup per user.
surfaces: [cli, ide, cloud-web]
ownership: mixed
persistence: committed
mechanics: >
  Repository-scoped configuration is committed under the project's .codex or .agents directory
  per each feature's documented location: config.toml sets repository defaults for supported
  local clients; rules/ controls commands that require approval outside the sandbox; skills/
  makes repository workflows available. A committed project .codex/config.toml outranks each
  developer's personal ~/.codex/config.toml, establishing a team baseline. Project .codex/
  layers load only when the project is trusted. Repository configuration can supply defaults and
  reusable workflows but cannot grant workspace, model, Platform API, or connected-system access
  — those stay with workspace administrators (see Managed configuration / requirements.toml).
sources:
  - url: https://learn.chatgpt.com/docs/enterprise/admin-setup
    retrieved: 2026-08-02
    note: repository-scoped config groups (config.toml, rules/, skills/), committed to .codex/.agents, trust requirement, and the boundary against granting access (render host for developers.openai.com/codex/enterprise)
  - url: https://learn.chatgpt.com/docs/changelog
    retrieved: 2026-08-02
    note: 2026-01-23 Team Config release entry naming config.toml defaults, rules/, and skills/
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: >
  Partially used. Of the three Team Config groups the doc names (config.toml defaults, rules/,
  skills/), the engine commits two: .codex/config.toml (project-scope, treated as a protected file
  per AGENTS.md L90-92) and repository skills at .agents/skills/. The committed config.toml is
  narrow — it holds only the two engine-managed [mcp_servers.*] blocks
  (engine-knowledge-graph, engine-memory) with default_tools_approval_mode="auto", not general
  team defaults.
overlap: >
  The doc's rules/ group (commands that require approval outside the sandbox) is NOT used — no
  .codex/rules/ or .agents/rules/ (ls → absent). That command-approval-control role is instead
  hand-built as the engine's PreToolUse write-gate and mode machinery: .codex/hooks.json →
  codex-hook-runner.sh → .engine/tools/modes.py / validate.py, mirroring the Claude hooks. Skills
  live at .agents/skills/ (not the .codex/skills/ the record's team-config phrasing also allows).
note: >
  Native-vs-hand-built gap: Codex Team Config offers rules/ for approval controls, but the engine
  reimplements approval/write gating through its own hooks + modes.py rather than adopting rules/.
  Also, the engine leans on config.toml's protected-file status and the workspace-write /
  approval_policy=never posture (README L285-289) for unattended runs, which the record correctly
  flags as workspace-admin-gated — a dependence on org policy the engine cannot itself grant.
```

**Assessment:** action mode blocks/intercepts; enforcement deterministic where the platform honors it; the engine treats it as best-effort; failure mode open; portability provider-specific behind the engine's adapter seam.

**Disposition: `ADAPTER`** — Half-adopted: config.toml + repo skills committed; the rules/ approval-control group is reimplemented as engine hooks instead.


## Family: mcp

### `codex/mcp/authentication`

```yaml
id: codex/mcp/authentication
provider: codex
family: codex / mcp
name: MCP server authentication
what: Streamable HTTP MCP servers can authenticate with a bearer token from the environment, an OAuth flow, ChatGPT session auth for trusted first-party servers, or static/dynamic HTTP headers. OAuth credentials are stored and managed through the CLI.
surfaces: [cli, desktop, ide]
ownership: user
persistence: local-config
mechanics: >
  HTTP-server auth keys: `bearer_token_env_var` (env var supplying a bearer token),
  `auth` = "oauth" | "chatgpt" (fallback/auth mode), `http_headers` (static headers),
  and `env_http_headers` (headers sourced from env). OAuth is the default for HTTP
  servers. CLI: `codex mcp login <name>` starts the OAuth login for a streamable HTTP
  server and `codex mcp logout <name>` clears stored credentials.
sources:
  - url: https://developers.openai.com/codex/extend/mcp
    retrieved: 2026-08-02
    note: bearer_token_env_var, auth oauth/chatgpt, http_headers/env_http_headers, codex mcp login/logout.
  - url: https://developers.openai.com/codex/config-file/config-reference
    retrieved: 2026-08-02
    note: bearer_token_env_var and auth = "oauth"|"chatgpt" documented as MCP sub-keys.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  N/A to stdio servers — no bearer_token_env_var, auth=oauth|chatgpt, http_headers/env_http_headers, or
  codex mcp login/logout. The engine servers are locally launched processes with no auth surface.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Auth and server-exposure features not applicable to local stdio servers.

### `codex/mcp/client-server-config`

```yaml
id: codex/mcp/client-server-config
provider: codex
family: codex / mcp
name: MCP server configuration (client)
what: Codex acts as an MCP client, connecting to external MCP servers over stdio (locally launched processes) or streamable HTTP (remote URLs) to add tools and context. Servers are configured in TOML or managed through CLI subcommands.
surfaces: [cli, desktop, ide]
ownership: user
persistence: local-config
mechanics: >
  Defined under `mcp_servers.<id>` in config.toml. STDIO keys: `command` (required),
  `args`, `env`, `env_vars`, `cwd`, `enabled`, `required`, `experimental_environment`.
  HTTP keys: `url` (required) plus auth keys. Shared: `startup_timeout_sec` (default 10s;
  `startup_timeout_ms` alias) and `tool_timeout_sec` (default 60s). CLI management:
  `codex mcp add <name> -- <command>`, `codex mcp get <name>`, `codex mcp list`,
  `codex mcp remove <name>`. Codex also reads each server's MCP `instructions` string
  (first ~512 chars weighted) to guide cross-tool use.
sources:
  - url: https://developers.openai.com/codex/extend/mcp
    retrieved: 2026-08-02
    note: stdio vs streamable HTTP transports, TOML keys, codex mcp add/list, server instructions.
  - url: https://developers.openai.com/codex/config-file/config-reference
    retrieved: 2026-08-02
    note: mcp_servers.<id> sub-keys command/url/args/env/cwd/startup_timeout_sec/tool_timeout_sec and defaults.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: >
  Used, stdio subset. /Users/shanekidd/Developer/engine-template/.codex/config.toml defines
  [mcp_servers.engine-knowledge-graph] and [mcp_servers.engine-memory] with command="uv" and an args
  list (stdio). Only command + args (+ default_tools_approval_mode) are used; the record's other stdio
  keys — env, env_vars, cwd, enabled, required, experimental_environment, startup_timeout_sec,
  tool_timeout_sec — are all omitted (defaults).
overlap: >
  These blocks are engine-written managed blocks (BEGIN/END engine-managed markers), rendered
  deterministically by .engine/tools/wiring.py render_codex_mcp_body and applied through
  wiring/module_manager/boot, NOT via the documented `codex mcp add/get/list/remove` CLI. Parity with
  the Claude .mcp.json is enforced by .engine/tools/provider_parity_check.py.
note: >
  render_codex_mcp_body supports far more than the committed config uses (arbitrary scalar keys,
  sub-tables, per-tool approval dict-of-dicts); the shipped definition exercises only the minimal stdio
  trio.
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `CORE`** — The Codex half of the MCP registration, engine-written managed blocks rendered by wiring.py (not the codex mcp CLI).

### `codex/mcp/codex-as-mcp-server`

```yaml
id: codex/mcp/codex-as-mcp-server
provider: codex
family: codex / mcp
name: Codex as an MCP server
what: Codex can itself run as an MCP server, exposing Codex over the protocol to other MCP clients rather than only consuming external servers. An experimental local app-server is also available.
surfaces: [cli, sdk-api]
ownership: user
persistence: session
mechanics: >
  `codex mcp-server` runs Codex itself as an MCP server over stdio. `codex app-server`
  launches the app server for local development (experimental). These are separate from
  the client-side `codex mcp add/list/...` management subcommands.
sources:
  - url: https://developers.openai.com/codex/cli/reference
    retrieved: 2026-08-02
    note: "codex mcp-server — Run Codex itself as an MCP server over stdio; codex app-server (experimental)."
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  `codex mcp-server` / `codex app-server` are not used. As on the Claude side, the engine is the MCP
  server author/consumer, not exposing Codex itself over the protocol.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Auth and server-exposure features not applicable to local stdio servers.

### `codex/mcp/tool-approval-integration`

```yaml
id: codex/mcp/tool-approval-integration
provider: codex
family: codex / mcp
name: MCP tool approval and gating
what: MCP tools feed into Codex's approval system: a server or per-tool approval mode decides whether calls run automatically or prompt the user, tool visibility can be allow/deny-listed, and MCP elicitations (server-initiated prompts) are surfaced through the approval policy.
surfaces: [cli, desktop, ide]
ownership: user
persistence: local-config
mechanics: >
  Per-server `default_tools_approval_mode` = `auto` | `prompt` | `writes` | `approve`,
  overridable per tool via `approval_mode`. Tool surface is controlled with
  `enabled_tools` (allowlist) and `disabled_tools` (denylist). Server-initiated
  elicitations are gated by the approval_policy granular flag
  `approval_policy.granular.mcp_elicitations`.
sources:
  - url: https://developers.openai.com/codex/extend/mcp
    retrieved: 2026-08-02
    note: default_tools_approval_mode values, per-tool approval_mode, enabled_tools/disabled_tools.
  - url: https://developers.openai.com/codex/config-file/config-reference
    retrieved: 2026-08-02
    note: granular flag mcp_elicitations under approval_policy governs MCP prompts.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: >
  Partially used — server-level only. Both blocks in .codex/config.toml set
  default_tools_approval_mode = "auto". Per-tool approval_mode overrides, enabled_tools/disabled_tools
  allow/deny lists, and the approval_policy.granular.mcp_elicitations flag are all unused.
overlap: >
  wiring.render_codex_mcp_body can render per-tool approval sub-tables (dict-of-dicts), but the committed
  definition uses only the flat server-level "auto".
note: >
  The engine chooses "auto" (run without prompting) for its own trusted read-mostly servers, mirroring
  the Claude side where the tools are pre-approved in settings.local.json.
```

**Assessment:** action mode blocks/intercepts; enforcement deterministic where the platform honors it; the engine treats it as best-effort; failure mode open; portability provider-specific behind the engine's adapter seam.

**Disposition: `CORE`** — Server-level auto-approval mirrors the Claude-side pre-approval for the engine's own trusted servers.


## Family: review

### `codex/review/agents-md-review-rules`

```yaml
id: codex/review/agents-md-review-rules
provider: codex
family: review
name: "Code Review Rules in AGENTS.md"
what: >
  Repository-specific review guidance is expressed in AGENTS.md files under a
  "## Code Review Rules" section, which Codex applies when reviewing. Repository-wide
  rules go in the root AGENTS.md; service-specific rules go in nested files (e.g.
  services/experiment_reporting/AGENTS.md).
surfaces: [github-ci]
ownership: project
persistence: committed
mechanics: >
  Codex discovers AGENTS.md files and reads the "## Code Review Rules" section. Guidance:
  keep rules concise and scoped, describe the behavior to flag plus any safe path or
  exception, focus on consequential repo-specific behavior, and leave formatting/lint
  checks to CI. Root file covers the whole repository; nested AGENTS.md files scope rules
  to their subtree.
sources:
  - url: https://developers.openai.com/codex/integrations/github
    retrieved: 2026-08-02
    note: >
      Documents the "## Code Review Rules" AGENTS.md heading, root-vs-nested scoping, and
      rule-authoring guidance. Redirects to learn.chatgpt.com/docs/third-party/github.
  - url: https://developers.openai.com/codex/use-cases/github-code-reviews
    retrieved: 2026-08-02
    note: >
      Corroborates customizing review guidance via AGENTS.md "## Code Review Rules" with
      root/nested placement. Redirects to learn.chatgpt.com/use-cases/github-code-reviews.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  The native "## Code Review Rules" slot in AGENTS.md is NOT used: the root AGENTS.md carries the engine floor,
  not review rules, and no nested AGENTS.md review-rules files exist. The engine expresses its review guidance
  instead inside the persona mandates (.claude/agents/engine-qa-review-*.md / .codex/agents/*.toml) and
  build-orchestration.md.
note: >
  Concrete "native slot the engine bypasses": Codex would read repo-specific review guidance from AGENTS.md
  "## Code Review Rules," but the engine keeps its review doctrine in persona files + the orchestration runbook,
  leaving that native heading unused.
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `OPTIONAL INTEGRATION`** — The native '## Code Review Rules' AGENTS.md slot is unused; if Codex review is ever enabled, engine review doctrine could project into it.

### `codex/review/code-review`

```yaml
id: codex/review/code-review
provider: codex
family: review
name: "Code review (/review)"
what: >
  Codex's reviewer inspects code changes and surfaces prioritized, actionable findings
  (e.g. regressions, missing tests, documentation issues) without changing the working
  tree. It runs against the state of the Git repository, over pull requests, branches,
  commits, or files, per selected review criteria.
surfaces: [cli, desktop, ide, cloud-web, github-ci]
ownership: mixed
persistence: local-config
mechanics: >
  Started with the "/review" command/preset in the ChatGPT desktop app, CLI, and IDE
  extension; findings appear in web chat as a turn (inline comments) where you can ask
  for evidence, request a narrower follow-up, or ask for revised files. Documented review
  scopes: "Review against a base branch" (merge base + branch diff), "Review uncommitted
  changes" (staged, unstaged, untracked), "Review a commit" (exact change set), and
  "Custom review instructions". Config keys: "review_model" in config.toml selects a
  different model from the current session for reviews; IDE-extension setting
  "chatgpt.reviewDelivery" set to "detached" starts a separate review chat. A Git
  repository is required locally for desktop/IDE; GitHub surface uses gh CLI auth.
sources:
  - url: https://developers.openai.com/codex/code-review
    retrieved: 2026-08-02
    note: >
      Documents the /review command, the four review scopes, config keys review_model
      (config.toml) and chatgpt.reviewDelivery (detached), and the surfaces reviews run
      on. Redirects to learn.chatgpt.com/docs/code-review.
  - url: https://developers.openai.com/codex/use-cases/github-code-reviews
    retrieved: 2026-08-02
    note: >
      Corroborates review output (regressions, missing tests, documentation issues) and
      the "@codex fix it" follow-up. Redirects to learn.chatgpt.com/use-cases/github-code-reviews.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  Codex's native `/review` (and review_model / chatgpt.reviewDelivery settings) is not used. The engine's review
  is its own persona set; on the Codex runtime those same reviewers are the committed renders under
  /Users/shanekidd/Developer/engine-template/.codex/agents/engine-qa-review-*.toml and engine-design-review-*.toml
  (generated from the .claude/agents/*.md source by .engine/tools/codex_gen.py), invoked through
  build-orchestration.md — not Codex's built-in reviewer.
note: >
  .codex/config.toml holds only engine-MCP server blocks and sandbox/approval defaults; it sets no `review_model`
  and no reviewDelivery, confirming the native reviewer is not wired.
```

**Assessment:** action mode advises; enforcement advisory; failure mode open; portability provider-specific.

**Disposition: `OPTIONAL INTEGRATION`** — As with the Claude native reviewers: complementary, operator-enabled.


## Family: sdk

### `codex/sdk/cli-wrapping-config`

```yaml
id: codex/sdk/cli-wrapping-config
provider: codex
family: sdk
name: CLI-wrapping architecture and config passthrough
what: The SDK does not reimplement Codex; it spawns the codex CLI and exchanges events with it, and it exposes hooks to redirect the working directory, inject environment, and override CLI configuration.
surfaces: [sdk-api]
ownership: user
persistence: local-config
mechanics: >
  The TypeScript SDK "wraps the `codex` CLI from `@openai/codex`", spawning it and exchanging
  JSONL events over stdin/stdout. Thread-creation options `workingDirectory` (defaults to
  requiring a Git repo) and `skipGitRepoCheck` (bypass that check). Client options `env`
  (control CLI environment variables — the SDK still injects required vars such as
  `CODEX_API_KEY`), `config` (override Codex CLI settings via dotted-path JSON), and `baseUrl`
  (passed as `--config openai_base_url=...`).
sources:
  - url: https://github.com/openai/codex/tree/main/sdk/typescript
    retrieved: 2026-08-02
    note: README describes CLI-spawn/JSONL-over-stdio architecture and the workingDirectory, skipGitRepoCheck, env, config, and baseUrl options plus CODEX_API_KEY injection.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  The engine configures the codex CLI through committed config files rather than
  the SDK's spawn-and-JSONL wrapper: .codex/config.toml (managed MCP-server
  blocks + default_tools_approval_mode), .codex/agents/*.toml (sandbox_mode),
  .codex/hooks.json (via .engine/tools/codex-hook-runner.sh). Generation by
  .engine/tools/codex_gen.py.
note: >
  Both the SDK and the engine ultimately configure the same codex CLI, but the
  engine does it declaratively via checked-in TOML/JSON the CLI reads, not by an
  SDK process that injects workingDirectory/env/config/baseUrl at spawn time.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Same posture as the Agent SDK: config-file-driven integration, no embedded loop.

### `codex/sdk/multimodal-input`

```yaml
id: codex/sdk/multimodal-input
provider: codex
family: sdk
name: Structured / multimodal input entries
what: A prompt can be supplied as structured input entries rather than a plain string, including attaching a local image alongside text.
surfaces: [sdk-api]
ownership: user
persistence: session
mechanics: >
  TypeScript input entries carry a `type`: `{ type: "text", text: "..." }` and
  `{ type: "local_image", path: "./path/to/image" }` for attaching a local screenshot/image
  to the turn.
sources:
  - url: https://github.com/openai/codex/tree/main/sdk/typescript
    retrieved: 2026-08-02
    note: README documents the text and local_image structured input entry types.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  No structured/local_image input entries; no Codex SDK and no image-attachment
  path in the engine's text/YAML/JSON toolchain.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Same posture as the Agent SDK: config-file-driven integration, no embedded loop.

### `codex/sdk/python-sdk`

```yaml
id: codex/sdk/python-sdk
provider: codex
family: sdk
name: Codex Python SDK (openai-codex)
what: A Python library for embedding Codex programmatically, with synchronous and asynchronous clients as context managers. Creates threads and runs prompts, with per-thread and per-turn sandbox control.
surfaces: [sdk-api]
ownership: user
persistence: local-config
mechanics: >
  Installed via `pip install openai-codex` (`pip install --pre openai-codex` for prereleases);
  requires Python 3.10+. Classes `Codex` and `AsyncCodex` are context managers
  (`with Codex() as codex:` / `async with AsyncCodex() as codex:`). `codex.thread_start(model=..., sandbox=...)`
  creates a thread; `thread.run(prompt)` executes it. Observed parameters: `model`
  (e.g. "gpt-5.6-terra") and `sandbox`.
sources:
  - url: https://developers.openai.com/codex/sdk
    retrieved: 2026-08-02
    note: SDK doc page documents the Python SDK — install, Codex/AsyncCodex context managers, thread_start/run, model and sandbox params.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  No openai-codex dependency in /Users/shanekidd/Developer/engine-template/.engine/pyproject.toml
  and no Codex/AsyncCodex import. The engine's Python is its own tool runtime
  (uv-managed .engine/.venv), not a Codex-embedding host.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Same posture as the Agent SDK: config-file-driven integration, no embedded loop.

### `codex/sdk/sandbox-controls`

```yaml
id: codex/sdk/sandbox-controls
provider: codex
family: sdk
name: Sandbox controls via SDK
what: The SDK lets the embedding program choose the filesystem-access sandbox for the agent, both when starting a thread and per individual turn.
surfaces: [sdk-api]
ownership: user
persistence: session
mechanics: >
  Python exposes a `Sandbox` enum with presets `read_only` (read files, no writes),
  `workspace_write` (read plus write inside the workspace and configured writable roots), and
  `full_access` (no filesystem restrictions). Set at thread start
  (`thread_start(sandbox=Sandbox.workspace_write)`) and overridable per turn
  (`thread.run(prompt, sandbox=Sandbox.read_only)`).
sources:
  - url: https://developers.openai.com/codex/sdk
    retrieved: 2026-08-02
    note: SDK doc page documents the Python Sandbox enum, its three presets, and per-turn override.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  Sandbox posture is set declaratively per-agent via sandbox_mode in
  .codex/agents/*.toml (e.g. read-only), mapped from Claude permissions by
  .engine/tools/codex_gen.py, plus the runtime write-gate .engine/tools/modes.py
  and .engine/operations/routine-entry.md's auto-approve posture for routines.
note: >
  The Python Sandbox enum (read_only/workspace_write/full_access) set via SDK
  thread_start/run is unused; the engine expresses the equivalent read-only vs
  writable stance in committed agent TOML and its own operating-mode gate.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Same posture as the Agent SDK: config-file-driven integration, no embedded loop.

### `codex/sdk/streaming-events`

```yaml
id: codex/sdk/streaming-events
provider: codex
family: sdk
name: Streamed turn events (runStreamed)
what: Instead of a single buffered result, a turn can be run as an async stream of structured events, letting a program observe intermediate items and end-of-turn usage as they arrive.
surfaces: [sdk-api]
ownership: user
persistence: session
mechanics: >
  TypeScript `thread.runStreamed(prompt, options?)` returns an async iterable of events.
  Observed event types: `item.completed` (carries `event.item`) and `turn.completed`
  (carries `event.usage`). Consumed with `for await (const event of events) { switch (event.type) ... }`.
sources:
  - url: https://github.com/openai/codex/tree/main/sdk/typescript
    retrieved: 2026-08-02
    note: README documents runStreamed and the item.completed / turn.completed event types with the for-await consumption pattern.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  No runStreamed / event-iteration usage (no Codex SDK present). Engine tools are
  synchronous Python invoked per-hook; no streamed-turn consumption exists.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Same posture as the Agent SDK: config-file-driven integration, no embedded loop.

### `codex/sdk/structured-output`

```yaml
id: codex/sdk/structured-output
provider: codex
family: sdk
name: Structured output (outputSchema)
what: A turn can be constrained to return output matching a caller-supplied JSON Schema, so the SDK returns machine-parseable structured data rather than free-form text.
surfaces: [sdk-api]
ownership: user
persistence: session
mechanics: >
  TypeScript: pass `outputSchema` in the run options — either a plain JSON Schema object or a
  Zod schema converted via `zod-to-json-schema` with `target: "openAi"`
  (`thread.run(prompt, { outputSchema: zodToJsonSchema(schema, { target: "openAi" }) })`).
  The result is read from `turn.finalResponse`.
sources:
  - url: https://github.com/openai/codex/tree/main/sdk/typescript
    retrieved: 2026-08-02
    note: README documents the outputSchema run option, JSON-Schema and Zod (target:"openAi") forms, and finalResponse access.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  The engine gets machine-parseable structure via JSON Schema validation of its
  own artifacts (.engine/schemas/*.json, .engine/check/*.json enforced by
  .engine/tools/validate.py using jsonschema 2020-12), not via an SDK
  outputSchema run option.
note: >
  Same end (structured/validated data) reached by hand-built schema validation
  of files rather than constraining an SDK turn's output.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Same posture as the Agent SDK: config-file-driven integration, no embedded loop.

### `codex/sdk/thread-persistence-resume`

```yaml
id: codex/sdk/thread-persistence-resume
provider: codex
family: sdk
name: Thread persistence and resumption
what: SDK conversation threads are persisted to disk and can be reconnected later by thread ID, so a program (or a subsequent CI job) can resume a prior session's state.
surfaces: [sdk-api]
ownership: user
persistence: local-config
mechanics: >
  Threads are stored under `~/.codex/sessions`. The caller externally saves the thread's id
  (README example stores it in an env var such as `CODEX_THREAD_ID`) and later calls
  `codex.resumeThread(threadId)` (TypeScript) to reconstruct the thread and continue with `run()`.
sources:
  - url: https://github.com/openai/codex/tree/main/sdk/typescript
    retrieved: 2026-08-02
    note: README states threads persist in ~/.codex/sessions and documents resumeThread(threadId) with externally stored id.
  - url: https://developers.openai.com/codex/sdk
    retrieved: 2026-08-02
    note: Doc page documents resumeThread(threadId) for resuming an existing thread.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: >
  Conversation persistence/resume is hand-built over the engine's own memory
  ledger (.engine/memory/ledger.ndjson + .engine/tools/memory/*), not
  ~/.codex/sessions or resumeThread(threadId).
note: >
  No resumeThread usage. For unattended resumption the engine instead relies on
  git state + Issue scope-lock re-discovery described in
  .engine/operations/routine-entry.md (steps 4–5), not SDK thread ids.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Same posture as the Agent SDK: config-file-driven integration, no embedded loop.

### `codex/sdk/typescript-sdk`

```yaml
id: codex/sdk/typescript-sdk
provider: codex
family: sdk
name: Codex TypeScript SDK (@openai/codex-sdk)
what: A Node.js/TypeScript library for embedding Codex programmatically in applications and CI. Exposes a Codex client that creates and drives conversation threads, running prompts and reading back a final response.
surfaces: [sdk-api]
ownership: user
persistence: local-config
mechanics: >
  Installed via `npm install @openai/codex-sdk`; requires Node.js 18+. Entry class `Codex`
  (`new Codex()`); `codex.startThread()` returns a Thread; `thread.run(prompt, options?)`
  executes a buffered turn and returns a result whose `finalResponse` holds the answer.
  Repeated `run()` calls on the same Thread continue the conversation. Client options observed:
  `env`, `config`, `baseUrl`.
sources:
  - url: https://developers.openai.com/codex/sdk
    retrieved: 2026-08-02
    note: Canonical SDK doc page (308-redirects to learn.chatgpt.com/docs/codex-sdk); shows Codex/startThread/run and finalResponse.
  - url: https://github.com/openai/codex/tree/main/sdk/typescript
    retrieved: 2026-08-02
    note: Package README (read via raw.githubusercontent.com); documents install, Node 18+, class/method names and client options.
```

**Reconciliation (engine-template@cdbbc335):**

```yaml
repo_usage: unused
overlap: none
note: >
  No @openai/codex-sdk anywhere; the engine ships no Node/TypeScript at all (no
  package.json embedding this SDK). Codex integration is entirely file-based:
  .codex/agents/*.toml, .codex/config.toml, .codex/hooks.json — generated/mirrored
  by Python tooling (.engine/tools/codex_gen.py, codex-hook-runner.sh), not by
  embedding the Codex SDK. The adjacent hand-built machinery is the Codex
  surface-generation toolchain, which drives the codex CLI's config files rather
  than programmatically starting threads.
```

**Assessment:** action mode n/a; enforcement none — not in use; failure mode n/a; portability provider-specific.

**Disposition: `REJECT`** — Same posture as the Agent SDK: config-file-driven integration, no embedded loop.
